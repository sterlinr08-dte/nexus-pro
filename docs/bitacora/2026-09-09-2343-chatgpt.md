## ChatGPT — 2026-09-09 23:43

# Continuación autorizada — fix financiero + Fase A publicada y aplicada

El dueño indicó **“Continúa”** después de leer las bitácoras de Claude. Se tomó como autorización para ejecutar, en orden, las correcciones ya preparadas y luego continuar con la Fase A que había pasado QA.

## 1. Bug de reconciliación automática — corregido

Claude había detectado que `reconciliarPagosClientes()` podía volver a sumar cobros en estado `Reversado` y registros `tipo='deuda_anterior'`, deshaciendo decisiones financieras válidas.

Se abrió y fusionó el PR #314:

- Rama: `claude/review-claude-md-jpzk7k`
- PR: `#314 Corregir reconciliación automática de pagos reversados`
- Cambio real en `index.html`: `reconciliarPagosClientes()` ahora excluye `REVERSADO` y `deuda_anterior`.
- Merge a `main`: `4fa5d04fbd28cfd781fc8031bff14b7631f5753f`.
- El preview Cloudflare del commit de Claude (`c130bc82`) desplegó correctamente; el merge de `main` contiene el mismo árbol de código.

## 2. Dato real del cliente — corregido después del código

Antes de tocar el dato se verificó:

- `clientes.pagado = 24000`
- `deuda_total = 24000`
- suma real de cobros no reversados = `17500`

Se aplicó una corrección puntual, con guarda idempotente y auditoría:

- cliente `e36a72b6-629a-4601-9e4f-2bd2e74fa967`
- `pagado: 24000 -> 17500`
- deuda pendiente resultante: `6500`
- auditoría creada con acción `PAGADO_CORREGIDO`

Verificación posterior:

- `seguros_diagnostico_financiero().ok = true`
- `pagado_descuadra = 0`
- saldo ESTERLIN = `1,370,310`
- saldo ROBINSON = `323,190`

La corrección del cliente no alteró la custodia de agentes.

## 3. Fase A — PR #313 rebasado y publicado

El PR #313 estaba 6 commits detrás de `main` después del fix financiero. Para no perder el cambio publicado, se reconstruyó la rama `chatgpt/fase-a-resumen-ciclo` sobre el `main` estable, conservando únicamente:

- `docs/bitacora/2026-09-09-2205-chatgpt.md`
- `docs/bitacora/2026-09-09-2236-chatgpt.md`
- `supabase/migrations/20260909220000_seguros_resumen_ciclo_agentes.sql`
- `supabase/migrations/20260909222500_seguros_resumen_ciclo_reversas.sql`

Nuevo head de rama: `57d5b106ba6e59eebf34e53631e55de2a298999c`.

El PR se marcó listo para revisión y se actualizó indicando:

- QA sintético 20/20;
- 6/6 reconciliaciones reales en RD$0;
- revisión cruzada de Claude sin bloqueantes;
- bug financiero independiente ya corregido antes del merge.

PR #313 fusionado a `main`:

- merge `5ff26b127da9216c8b8db7b1cf74b0a703171abd`.

## 4. Migraciones Fase A — aplicadas en Supabase producción

Proyecto: `tnwsgcxurfyuszxsewsn`.

Se aplicaron en orden con `apply_migration`:

1. `seguros_resumen_ciclo_agentes`
2. `seguros_resumen_ciclo_reversas`

Supabase registró versiones de ejecución:

- `20260910034046` — `seguros_resumen_ciclo_agentes`
- `20260910034117` — `seguros_resumen_ciclo_reversas`

Nota: los nombres de archivo del repo mantienen sus timestamps de diseño (`20260909220000` y `20260909222500`), mientras que `apply_migration` registró las versiones reales de ejecución del 10-sep UTC.

## 5. Verificación post-migración

- `seguros_ciclo_limites('2026-08')` devuelve inicio 20-ago, fin 20-sep y corte actual.
- `seguros_saldo_validado_agente_asof(..., now())` coincide exactamente con `seguros_acumulado_validado_agente(...)`:
  - ESTERLIN: `1,370,310` vs `1,370,310`, diferencia 0.
  - ROBINSON: `323,190` vs `323,190`, diferencia 0.
- `seguros_diagnostico_financiero().ok = true` después de aplicar ambas migraciones.

## 6. Fase A — estado

**CERRADA / aplicada a producción.**

La capa de lectura para custodia y resumen por ciclo ya existe en producción. Las transferencias internas continúan moviendo custodia sin aumentar el total cobrado del negocio.

## 7. Pendientes antes de Fase C

### Decisión de negocio: fecha de ciclo para pagos bancarios

El SQL actual (igual que la fuente de verdad de producción) asigna un pago bancario al ciclo según `validado_at`, no según la fecha original en que el cliente transfirió.

Ejemplo: cliente transfiere el 19 y el admin valida el 21 -> hoy el sistema lo cuenta en el ciclo siguiente.

Esto debe confirmarse con el dueño antes de congelar snapshots/cierres de Fase C.

### Agente desactivado con custodia viva

El QA de Claude confirmó que si un agente se desactiva conservando dinero, el consolidado admin actual puede dejar de mostrarlo porque filtra `activo=true`. Debe resolverse en Fase C para que un agente histórico/inactivo con movimientos del ciclo siga apareciendo en el cierre.

## 8. Siguiente paso recomendado

Entrar a **Fase B — notificaciones WhatsApp de transferencias y resumen de ciclo**, reutilizando las automatizaciones existentes y evitando mensajes duplicados. Paralelamente, antes de Fase C, obtener la decisión del dueño sobre la fecha que manda en pagos bancarios (pago vs validación).
