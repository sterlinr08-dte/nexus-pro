## ChatGPT — 2026-08-11 20:22

### SUBFASE 2A — migración aplicada en producción, pendiente validación final

Revisé cruzadamente el diseño de Claude de `claude/subfase-2a-rpc-fiscales` / SHA `7391df00d89c2976f572d5587449e7f5eca29252` contra el estado REAL de Supabase y coincide con producción.

Apliqué únicamente la migración DDL de Subfase 2A en Supabase:

`seguros_subfase_2a_hardening_rpc_fiscales`

No toqué RLS de tablas, Fase 2B, datos históricos, UI ni otros módulos.

### Estado verificado después de aplicar

ACL real:
- `crear_factura_auto_tx(...)` → SOLO `postgres`, `service_role`.
- `siguiente_ncf(text)` → `postgres`, `authenticated`, `service_role`, con guard interno de organización `nexus-pro`.
- `next_recibo()` → `postgres`, `authenticated`, `service_role`, con guard interno `nexus-pro`.
- `next_recibo_anio(integer)` → `postgres`, `authenticated`, `service_role`, con guard interno `nexus-pro`.
- `seguros_diagnostico_financiero()` → `postgres`, `authenticated`, `service_role`, con guard interno `nexus-pro` + `admin-only` para tráfico autenticado normal.

`crear_factura_auto_tx` permanece `SECURITY INVOKER`; no se tocó su cuerpo. Las otras funciones preservan su semántica original y solo ganaron guard/ACL según el diseño aprobado.

Diagnóstico financiero inmediatamente después de la migración:
- `ok: true`
- deuda_descuadra: 0
- pagado_descuadra: 0
- asientos_desbalanceados: 0
- asientos_no_positivos: 0
- ast_baja: 0

La deuda histórica sigue igual y FUERA DE 2A: 1 abono huérfano, 3 facturas huérfanas, 8 cobros sin referencia, 10 transferencias sin banco, 2 cobros sin agente.

### Claude: validación final requerida — NO implementar nada nuevo

Valida exclusivamente la Subfase 2A ya aplicada:

1. Relee las definiciones/ACL REALES de las cinco funciones en producción y confirma que coinciden con el diseño revisado.
2. Prueba, sin persistencia real (BEGIN...ROLLBACK o método seguro), la matriz mínima:
   - admin `nexus-pro`: permitido en siguiente_ncf / next_recibo_anio / diagnóstico.
   - agente `nexus-pro`: permitido en siguiente_ncf / next_recibo_anio; diagnóstico DENEGADO.
   - usuario autenticado de otra organización (por ejemplo bayolsale): DENEGADO en siguiente_ncf / next_recibo / next_recibo_anio / diagnóstico.
   - anon: DENEGADO en las cinco RPC.
   - service_role: permitido en `crear_factura_auto_tx` y confirmar que la auto-facturación puede completar factura + NCF + asiento dentro de rollback.
3. Confirma que el frontend actual de Seguros sigue pudiendo generar NCF y número de recibo con los roles que hoy lo hacen.
4. Ejecuta `seguros_diagnostico_financiero()` después de las pruebas y confirma `ok:true` y cero residuo.
5. No tocar main funcionalmente, no abrir 2B y no mezclar otros trabajos en esta validación.

Deja una entrada NUEVA `docs/bitacora/AAAA-MM-DD-HHMM-claude.md` con el resultado y marca claramente `SUBFASE 2A CERRADA` solo si todo pasa.