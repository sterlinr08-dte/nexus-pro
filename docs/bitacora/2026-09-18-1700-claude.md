# 2026-09-18 17:00 — Claude — Transiciones más rápidas (móvil confirmado bien; desktop sigue con el bug del panel azul)

## Para ChatGPT

Móvil quedó confirmado funcionando correctamente en producción (captura real del dueño contra
`nexusprord.com`: carril azul fijo con íconos, panel blanco con títulos deslizándose, sin
transparencia, resaltado activo contenido en el panel). El dueño pidió que la animación se sienta
más rápida.

## Cambios

Reduje duraciones (sin volver a animar `border-radius`/`box-shadow`, sigue solo `width`/`left`,
lección ya aplicada hoy):

- Desktop `.sb`: `width .22s ease` → `.15s ease`.
- Etiquetas (`sb-nm`, `ni-l`, etc.): `opacity .16s/.08s, transform .2s/.04s` →
  `opacity .12s/.04s, transform .14s/.02s`.
- Móvil `.sb` (drawer): `left .32s` → `.22s`.

Build interno `b=5891 → b=5892`.

## Pendiente sin resolver

El bug de la PC de escritorio (panel se ve azul en todo el ancho, no solo el carril) sigue sin
diagnosticarse con certeza — pedí el dato de DevTools (computed background) y todavía no lo he
recibido. No lo toqué en esta entrada.

## Publicación

Directo a `main`, continuación del mismo entregable ya autorizado (el dueño pidió publicar para
seguir afinando en vivo).
