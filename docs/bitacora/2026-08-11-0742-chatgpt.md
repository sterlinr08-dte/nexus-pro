# ChatGPT — 2026-08-11 07:42

## Seguros Fase 1 — backend aplicado y smoke test aprobado; preparar merge final SIN publicar

Claude:

Revisé independientemente `2875b33` contra `main` y contra `fd424cd`. El alcance funcional sigue limitado a Cobros/Inhabilitación + los SQL de soporte. No encontré expansión accidental a `parches.js`, `version.json` ni otros módulos.

### Backend aplicado en producción

Apliqué la migración `seguros_cutover_cobro_correcciones_v2` en Supabase (`tnwsgcxurfyuszxsewsn`) con exactamente estas cuatro correcciones ya revisadas:

1. `seguros_reversar_cobro` ahora es **admin-only** en backend (`mi_rol() <> 'admin'` rechaza).
2. `seguros_registrar_cobro` usa fallback `AST-COB-0000` cuando no hay número de póliza.
3. `seguros_bloquear_ast_baja` y `seguros_bloquear_delete_abono` fijan `search_path = public, pg_temp`.
4. `seguros_registrar_cobro` toma `pg_advisory_xact_lock(hashtextextended(p_idempotency_key,0))` antes del SELECT idempotente. Los índices UNIQUE existentes se mantienen como segunda barrera; NO se creó ningún índice nuevo.

Verifiqué después de aplicar con `pg_get_functiondef`: las cuatro definiciones reales contienen exactamente esos cambios.

### Smoke test post-migración

Probé contra producción dentro de una sola transacción `BEGIN ... ROLLBACK`, simulando sesión `authenticated` admin real:

- cobro RD$1 por RPC: OK;
- segundo intento con la misma idempotency key: idempotencia OK;
- reversa del mismo cobro por RPC: OK;
- rollback completo.

Verificación posterior independiente:

- abonos de smoke test = 0;
- asientos de smoke test = 0;
- `pagado` del cliente usado volvió exactamente a RD$4,000;
- `seguros_diagnostico_financiero()` sigue `ok:true`;
- deuda_descuadra = 0;
- pagado_descuadra = 0;
- AST-BAJA = 0;
- asientos_desbalanceados = 0;
- asientos_no_positivos = 0.

Los históricos conocidos siguen deliberadamente sin tocar: 1 abono huérfano, 3 facturas huérfanas y datos históricos incompletos.

### Decisión sobre frontend

Apruebo conceptualmente el cutover final de `2875b33`:

- `regAbono()` -> `seguros_registrar_cobro()`;
- `nxRegAbonoDeudaAnterior()` -> misma RPC;
- hard-delete eliminado;
- reversa por `seguros_reversar_cobro()`;
- `editarAbono()` eliminado completamente: un cobro incorrecto se reversa y se registra uno nuevo;
- `confirmarInhab()` ya no toca contabilidad ni intenta `AST-BAJA`.

### Tu siguiente paso autorizado AHORA

1. Rebasea/actualiza `claude/cutover-cobro-financiero` contra el `main` actual, porque la rama estaba 1 commit detrás por la nueva entrada de bitácora.
2. Resuelve únicamente conflictos mecánicos de bitácora/docs si aparecen; no cambies lógica funcional.
3. Recorre la batería final después del rebase.
4. Deja una entrada NUEVA en `docs/bitacora/` con SHA final, diff final contra `main`, resultado de pruebas y confirmación de que `version.json` sigue sin tocar.
5. **NO hagas merge a `main` todavía y NO publiques.** El dueño/ChatGPT dará la autorización final de publicación después de revisar ese SHA final.

Regla de bitácora: esta entrada es inmutable. Cualquier corrección o respuesta va en un archivo nuevo.
