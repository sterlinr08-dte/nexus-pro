## ChatGPT — 2026-09-19 08:00 RD

# Sidebar móvil — pasada final según captura real de iPhone

## Solicitud del dueño

El dueño mostró una captura real de NEXUS PRO en iPhone/Safari y pidió ejecutar las mejoras visuales detectadas sobre esa captura.

La captura confirma que la arquitectura de dos capas ya está presente, pero todavía había diferencias visibles frente a la referencia:
- iconos multicolor heredados del sidebar base;
- cabeza azul poco redonda;
- curva inferior demasiado larga/diagonal;
- panel blanco con poca lectura de redondeo;
- profundidad insuficiente entre azul/blanco;
- labels/footer comprimidos;
- activo demasiado rectangular.

El dueño dijo **“Hazlo”**. Esta entrega se prepara en rama para revisión; no se publica a `main` sin una autorización explícita posterior.

## Rama

`chatgpt/sidebar-mobile-finish-pass`

Base:
`main@5980a7f9f85e4430599de274610179c875780163`

## Fuente de verdad / diagnóstico

Se revisaron:
- `CLAUDE.md`;
- `docs/bitacora/README.md`;
- `docs/bitacora/2026-09-19-0749-chatgpt.md`;
- `parches-sidebar-curva.css`;
- `parches-seguros-base.js`.

Se confirmó la causa exacta de los iconos multicolor: `parches-seguros-base.js` mantiene 8 grupos con selectores
`nav.sb .ni:has(.ti-...) .ni-i` y `!important`. La regla móvil anterior
`nav.sb .ni .ni-i` también llevaba `!important`, pero tenía menor especificidad y por eso no ganaba la cascada.

## Cambios implementados

Archivo:
`parches-sidebar-curva.css`

Solo dentro de `@media (max-width:768px)`:

1. **Iconos blancos reales**
   - nuevo selector móvil con ID:
     `body nav#sbEl.sb .ni > i.ni-i`;
   - fuerza `background:transparent !important`;
   - desactiva `background-image`, sombras y `backdrop-filter`;
   - desactiva el reflejo global `.ti::after`;
   - el icono activo conserva únicamente una cápsula azul translúcida.

2. **Cabeza azul más redonda**
   - se redibuja la máscara SVG para ampliar el hombro superior y suavizar la transición al tramo vertical.

3. **Curva inferior más corta/controlada**
   - la salida inferior empieza más tarde;
   - reduce la diagonal larga de la captura;
   - termina de forma más compacta y redondeada.

4. **Panel blanco más evidente**
   - `top/bottom:10px`;
   - `right:8px`;
   - `border-radius:28px`;
   - borde sutil y sombra propia;
   - mantiene el solape bajo el azul y la animación de salida.

5. **Más profundidad azul/blanco**
   - sombra azul refinada hacia la derecha;
   - el panel blanco conserva sombra independiente.

6. **Activo tipo píldora**
   - `spring-ind` pasa a radio 18px;
   - se elimina la línea lateral dura;
   - comienza 6px después del carril azul.

7. **Espaciado / tipografía**
   - labels: +12px desde el icono;
   - títulos de sección se desplazan al panel blanco;
   - menor densidad visual y letter-spacing más discreto.

8. **Footer**
   - avatar 31px;
   - wrapper de texto con `min-width:0`;
   - nombre/rol usan ellipsis controlado;
   - tipografía compactada para reducir el truncado agresivo.

## Build

`5898 -> 5899`

## No tocado

- desktop;
- navegación real / `mob-open`;
- Supabase;
- cobros;
- transferencias;
- ciclo 20→20;
- CRM/WhatsApp;
- POS;
- permisos;
- lógica de negocio.

## QA requerido antes de publicar

En iPhone/Safari:
- confirmar que desaparecieron los fondos multicolor;
- confirmar activo azul translúcido únicamente;
- revisar cabeza superior y curva inferior;
- confirmar redondeo visible del panel blanco;
- verificar footer y labels;
- revisar overlay/safe-area;
- comprobar que no existe overflow horizontal;
- confirmar que panel blanco + textos siguen saliendo desde debajo del azul.

## Estado

Implementado en rama y listo para preview.
**No fusionar a `main` sin autorización explícita del dueño.**
