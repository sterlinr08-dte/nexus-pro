## ChatGPT — 2026-09-10 19:10

# WhatsApp Inbox — falso “0 conversaciones” — FIX PREPARADO

## Reporte
El dueño mostró una captura de producción donde WhatsApp/Inbox aparece con 0 conversaciones y “Todavía no han llegado mensajes”.

## Verificación en producción (solo lectura)
- `public.whatsapp_hilos`: 20 filas.
- `public.whatsapp_hilo_mensajes`: 106 filas.
- último mensaje existente: 2026-09-10 17:28:54 UTC.
- `authenticated` conserva SELECT sobre `whatsapp_hilos`.
- policy `whatsapp_hilos_lectura` sigue presente.

No hay pérdida de chats. Es un falso cero de frontend.

## Causa concreta
`parches-whatsapp-inbox.js` entra con `hilos=[]`, ejecuta `render()` y después `cargar()`. Si la primera consulta `A.get('whatsapp_hilos', ...)` falla, el código hace `catch (e) { return; }`. El comentario dice que se reintenta, pero ese camino no programa ningún reintento inmediato. Resultado: la pantalla puede permanecer en cero hasta reapertura o un evento posterior de Realtime.

## Fix preparado
Rama: `chatgpt/fix-whatsapp-carga-cero`.

Nuevo archivo `parches-whatsapp-carga-resiliente.js`:
- detecta exclusivamente el falso cero del Inbox;
- verifica por API si realmente existen hilos;
- reintenta con backoff corto (máximo 4 intentos);
- si backend tiene chats y UI sigue en cero, vuelve a abrir el Inbox para disparar su carga normal;
- si la consulta sigue fallando, cambia el mensaje engañoso por “No se pudieron cargar las conversaciones” y ofrece botón Reintentar;
- no inventa datos ni copia la lógica de render de conversaciones.

`parches-seguros.js` carga la capa al final y usa build 5853 en esta rama.

## Alcance / seguridad
No toca:
- Supabase schema/data;
- pagos/cobros;
- Zernio;
- mensajes ni webhooks;
- Realtime;
- scroll del chat;
- lógica financiera;
- contenido de conversaciones.

## Estado
PREPARADO, NO PUBLICADO. Requiere revisión cruzada de Claude y autorización del dueño antes de `main`.
