# 2026-09-17 15:46 — ChatGPT — Prototipo UI del ciclo 20→20 por agente

## Para Claude

Implementación preparada en `chatgpt/reporte-ciclo-agentes`. **No publicada a `main`.**

### Qué cambia

Se agregó `parches-reporte-ciclo-agentes.js` y se registra desde `parches-seguros.js` (build interno `b=5883`).

No crea otra vista de navegación: se integra dentro del `Reporte por agente` existente (`v-rep-agente`) y reutiliza el selector de agente ya presente como filtro visual.

### Fuente de verdad

La pantalla NO suma abonos, transferencias ni entregas en JavaScript.

Modo `Ciclo`:
1. intenta `seguros_cierre_ciclo_admin(p_periodo)`;
2. si existe snapshot, muestra ese cierre inmutable;
3. si no existe snapshot, usa `seguros_resumen_ciclo_admin(p_periodo)` como lectura viva.

Modo `Consolidado`:
- usa `seguros_reporte_mensual_admin(p_mes)`.

### Datos mostrados

Por agente:
- cobrado validado;
- transferido confirmado;
- recibido confirmado;
- entregado admin/directo;
- saldo/custodia final;
- diferencia de reconciliación;
- histórico aproximado cuando la RPC lo indica.

A nivel ciclo:
- `total_negocio_cobrado` / `total_negocio_cobrado_ciclo` directo del servidor;
- en snapshot cerrado, `total_saldo_final` y `total_deuda_agentes` directos del cierre, sin netear deuda contra custodia.

### Protecciones

- Solo se monta para `sesion.rol === 'admin'`, coherente con las RPC admin.
- No toca Supabase/migraciones ni ninguna función de escritura.
- No toca `transferencias_crear/aceptar/rechazar`.
- No toca cobros ni validaciones.
- Cambios rápidos de pestaña/período están protegidos con un `req` secuencial para que una respuesta vieja no pinte datos de otro período.
- Exportar CSV usa exactamente los valores ya devueltos por las RPC.

### Revisión solicitada a Claude

Antes de cualquier publicación, verificar:
1. que el orden snapshot→lectura viva sea correcto;
2. que no haya matemática financiera nueva en frontend;
3. que `seguros_reporte_mensual_admin` se esté presentando con su semántica real (mes de apertura del ciclo);
4. que el wrapper de `rRepAgt` no rompa el reporte legado ni su selector;
5. que la vista móvil no produzca overflow fuera de su contenedor (la tabla debe desplazarse solo dentro del bloque);
6. si conviene mantener `Consolidado` en esta primera versión o dejar únicamente `Ciclo`.

No mergear a `main` sin autorización explícita del dueño.
