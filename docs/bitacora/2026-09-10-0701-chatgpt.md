## ChatGPT — 2026-09-10 07:01 RD

# Fase C — correcciones posteriores a revisión de Claude (PREPARADO, NO PUBLICADO)

El dueño autorizó continuar después de revisar los pendientes de Fase C.

Claude revisó el PR #317 y confirmó que los 8 puntos principales pasan, pero detectó dos riesgos antes de producción:

1. `seguros_cerrar_ciclo_core()` podía congelar un snapshot con `diferencia_reconciliacion != 0`.
2. `seguros_reporte_mensual_admin(p_mes)` filtraba por el mes de `ciclo_fin`, aunque en NEXUS PRO `YYYY-MM` identifica el mes en que ABRE el ciclo 20→20.

## Correcciones preparadas

Migración nueva en la rama `chatgpt/fase-c-cierre-ciclo-20`:

`supabase/migrations/20260910070100_seguros_cierre_ciclo_guardas.sql`

### 1. Guarda de reconciliación antes de congelar

Antes de insertar un cierre nuevo, `seguros_cerrar_ciclo_core()` recorre los agentes y consulta el mismo core matemático de Fase C. Si cualquier agente tiene una diferencia absoluta superior a RD$0.005, el cierre se detiene con excepción y enumera los agentes descuadrados.

La verificación ocurre solamente para un cierre nuevo. Si el periodo ya tiene snapshot, se conserva la idempotencia: devuelve el mismo `cierre_id` y no recalcula el cierre histórico.

### 2. Semántica mensual alineada

`seguros_reporte_mensual_admin(p_mes)` ahora filtra por:

`c.periodo = p_mes`

Así `2026-06` representa el ciclo que abrió el 20-jun y cerró el 20-jul, igual que `seguros_cierre_ciclo_admin('2026-06')` y el resto de Fase C.

## Estado

- Cambios únicamente en la rama del PR #317.
- Nada de esta corrección fue aplicado a Supabase producción.
- No se fusionó el PR #317.
- No se modificaron cobros, saldos, transferencias ni datos financieros.

## Siguiente paso

Claude debe revisar la migración correctiva, en especial:

- que la guarda realmente impida congelar un descuadre;
- que un cierre ya existente conserve idempotencia e inmutabilidad;
- que el filtro por `periodo` resuelva la semántica mensual sin alterar los totales;
- que no se introduzca dependencia de JWT en el cron.

Si la revisión queda limpia, hacer QA final con rollback y entonces esperar autorización explícita del dueño antes de publicar Fase C.
