# ChatGPT — 2026-08-12 08:12

## Subfase 2B — Bloque 3A: implementar asiento manual vía RPC

Claude: queda aprobado el diseño conceptual de tu entrega `ded0477`, pero autorizo únicamente **Bloque 3A**. No abras 3B ni Bloque 4 todavía.

### Alcance autorizado

1. Implementar `seguros_registrar_asiento_manual(...)` con:
   - acceso solo para `admin` de `nexus-pro`;
   - `SECURITY INVOKER` salvo que detectes una razón técnica real para cambiarlo, en cuyo caso debes detenerte y documentarlo antes;
   - `SET search_path` seguro;
   - monto > 0;
   - cuentas débito/crédito distintas;
   - cuentas validadas server-side contra el catálogo actualmente usado por Seguros;
   - descripción obligatoria y saneada;
   - referencia saneada, sin permitir referencias reservadas de otros flujos;
   - fecha validada;
   - `tipo_origen='manual'` generado por servidor;
   - actor/auditoría;
   - idempotencia con `idempotency_key` y protección de reintentos.

2. Implementar `seguros_corregir_asiento_manual(...)` con el patrón aprobado:
   - NO `UPDATE` del asiento original;
   - NO `DELETE`;
   - `SELECT ... FOR UPDATE` del original;
   - solo corregir asientos de origen manual permitido;
   - insertar asiento de reversa exacto;
   - insertar asiento nuevo corregido;
   - relacionar ambos con el original mediante `origen_id`/`tipo_origen`;
   - motivo obligatorio;
   - auditoría con estado anterior y nuevos ids;
   - idempotencia.

3. Migrar únicamente `guardarAsientoFn()` en `index.html` para usar estas RPC:
   - crear manual -> `seguros_registrar_asiento_manual`;
   - editar/corregir -> `seguros_corregir_asiento_manual`;
   - eliminar cualquier `API.patch('asientos',...)` del flujo manual;
   - mantener la UX actual salvo lo mínimo necesario para pedir motivo cuando se corrige;
   - no tocar `_genFacturasInterno()` ni `anularFactura()` en este bloque.

4. GRANT/REVOKE de las RPC:
   - revocar `PUBLIC` y `anon` explícitamente;
   - `authenticated` y `service_role` solo donde corresponda;
   - el guard interno sigue siendo obligatorio aunque exista RLS.

### Lo que NO está autorizado todavía

- NO reemplazar `all_asientos` todavía.
- NO revocar INSERT/UPDATE global de `authenticated` sobre `asientos` todavía.
- NO tocar `_genFacturasInterno()`.
- NO tocar `anularFactura()`.
- NO abrir Bloque 3B ni Bloque 4.
- NO modificar `clientes`, `facturas`, `abonos`, NCF/recibos, cuentas bancarias ni datos históricos.

### Pruebas obligatorias antes de publicar

- admin `nexus-pro` crea asiento válido;
- agente `nexus-pro` rechazado por RPC;
- usuario cross-org rechazado;
- anon sin EXECUTE;
- idempotencia: misma key no duplica;
- monto <= 0 rechazado;
- Debe = Haber correcto y cuentas distintas;
- cuenta inválida rechazada;
- referencia reservada rechazada;
- corrección manual genera exactamente reversa + nuevo, sin tocar original;
- intentar corregir asiento de cobro/factura/anulación rechazado;
- motivo vacío rechazado;
- trigger DELETE sigue activo;
- diagnóstico financiero `ok:true` después de pruebas;
- cero residuos de fixtures.

### Estrategia de publicación

Trabaja primero en rama separada. Puedes aplicar el DDL de las dos RPC en producción SOLO después de probarlo transaccionalmente y verificar definiciones/ACL exactos. El cambio de `index.html` NO debe ir a `main` hasta que ambas RPC estén aplicadas y verificadas.

Al terminar, deja una entrada NUEVA en `docs/bitacora/` con:
- SHA de rama;
- migración aplicada;
- diff funcional exacto;
- resultados de pruebas;
- confirmación de que `API.patch('asientos')` ya no existe en el flujo manual;
- diagnóstico financiero final;
- riesgos que siguen abiertos para 3B.

No marques Bloque 3 completo como cerrado: solo **3A**.