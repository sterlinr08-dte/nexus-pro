## ChatGPT — 2026-09-19 08:13 RD

# Publicación — rellenar hueco inferior entre carril azul y panel blanco

## Solicitud del dueño

El dueño mostró una captura real del sidebar móvil donde quedaba un espacio gris/transparente entre la curva azul inferior y el panel blanco, y pidió **publicarlo corregido**.

## Diagnóstico

La capa blanca móvil (`.sb::after`) empezaba demasiado a la derecha:

`left: calc(var(--nx-rail) - 18px)`

Por eso, cuando la máscara azul se recoge hacia la izquierda en la parte inferior, quedaba expuesto el fondo detrás del drawer.

## Corrección

Rama:
`chatgpt/sidebar-mobile-fill-gap`

Base:
`main@5c0fe71d4cce70f4b9c33963cf94cde8a0b0d4a2`

Cambios en `parches-sidebar-curva.css`:

- el panel blanco ahora solapa más por debajo del azul:
  - `left: calc(var(--nx-rail) - 34px)`;
  - `transform: translateX(calc(-100% + 34px))`;
- radio inferior izquierdo del blanco aumentado:
  - `border-radius: 30px 30px 34px 40px`;
- footer reajustado:
  - `margin-left: 58px`;
  - `padding: 10px 10px`.

Con esto, la capa blanca rellena el vacío que aparecía cuando el carril azul se estrecha en la parte baja, manteniendo el azul visualmente por encima.

Build:
`5900 -> 5901`

## Alcance

No se modificó:

- desktop;
- navegación;
- Supabase;
- cobros;
- CRM/WhatsApp;
- POS;
- lógica de negocio.

## Publicación

Autorizada explícitamente por el dueño con **"Publícalo"**.
Se publicará mediante PR/merge a `main` y deploy por la integración Git existente de Cloudflare Workers.
