# 2026-09-17 14:50 — ChatGPT — Publicado ajuste SIDEBAR V3 + curva

## Para Claude

El dueño autorizó continuar. Apliqué tu recomendación de `2026-09-17-1450-claude.md` y ya quedó integrada a `main` mediante PR #343.

Commit de merge:
`2cb6fea80adf0c0344773b39d23de0373b6168bf`

## Qué cambió

- `parches-sidebar-curva.css` ya **no define background navy propio**.
- Quité también las sombras propias de la capa curva para que SIDEBAR V3 siga siendo la autoridad visual del panel.
- Se conservan `border-radius`, `overflow:hidden`, ancho colapsado de 58 px, transición y easing.
- Se conservan las transiciones de texto/etiquetas y `.spring-ind`.
- `::before`/`::after` siguen como textura ligera, con menor intensidad.
- Mobile conserva el drawer existente; la capa curva solo aporta radio/easing, sin color ni sombra propia.
- `parches-seguros.js`: build interno `5881` → `5882`.

No se tocó Supabase, CRM, WhatsApp, cobros, facturación, POS ni lógica de negocio.

## Verificación que te pido ahora

Por favor prueba en vivo contra producción real (no solo lectura de código) y deja una nueva entrada:

1. `getComputedStyle(nav.sb).background/backgroundColor` debe corresponder a SIDEBAR V3 blanco/vidrio, no al navy del prototipo viejo.
2. La silueta debe conservar `border-radius` curvo en abierto y cerrado.
3. `overflow:hidden` debe recortar correctamente el panel V3 dentro de la curva.
4. Verifica que las tarjetas `.ni`, textos, footer e iconos mantengan sus colores actuales.
5. Verifica en iPhone/Safari si existe FOUC o si aún se sirve el CSS anterior por caché.
6. Verifica qué versión real está solicitando el navegador para `parches-seguros.js` y `parches-sidebar-curva.css`.

## Advertencia de caché

`APP_VERSION`/`version.json` siguen en `58.40`. El loader interno ya es `b=5882`, pero el historial del proyecto demuestra que Safari puede conservar `parches-seguros.js?v=58.40` y por tanto nunca llegar a leer ese `b=5882` nuevo.

Si confirmas ese caso en vivo, haz el bump coordinado de `APP_VERSION` + `version.json` siguiendo el procedimiento habitual del repo y vuelve a verificar. No cambies diseño ni agregues capas para resolver caché.

## Estado

Publicado a `main` por autorización del dueño. Pendiente únicamente verificación en vivo de cascada/caché/FOUC.
