## ChatGPT — 2026-09-17 14:23 RD

# Publicación autorizada — sidebar curva NEXUS PRO Seguros

## Autorización del dueño

El dueño revisó la vista previa de la rama `chatgpt/sidebar-curva-prototype` y autorizó explícitamente publicar esta entrega con el mensaje: **“Súbelo”**.

## Alcance que se publica

- Sidebar curva para NEXUS PRO Seguros.
- Reutiliza el mecanismo existente `toggleSB()`, `.sb.col` y `.sb.mob-open`.
- No crea una segunda sidebar ni duplica navegación.
- Mantiene intacta la relación `nav.sb .ni > i.ti-xxx.ni-i`, por lo que conserva el sistema de color de iconos.
- Desktop: 210 px abierto / 58 px colapsado, con borde derecho curvo y transición suave.
- Mobile: conserva el drawer existente y solo aplica la identidad curva/transición.
- Sin cambios de Supabase, cobros, CRM, WhatsApp, facturación ni lógica de negocio.

## Revisión previa

Claude revisó el prototipo en `docs/bitacora/2026-09-17-1340-claude.md` y no encontró bloqueantes de lógica ni conflictos con el sistema de iconos. Señaló como riesgo técnico la carga tardía de la hoja visual y posible FOUC; este riesgo queda documentado para seguimiento visual en producción.

## Publicación

PR: `#342` — `Prototipo visual: sidebar curva NEXUS PRO Seguros`.

Rama origen: `chatgpt/sidebar-curva-prototype`.

Rama destino: `main`.

El merge a `main` queda autorizado por el dueño en esta entrega concreta. El deploy de producción debe ocurrir por la integración Git existente de Cloudflare Workers; no usar GitHub Actions.

## Verificación posterior requerida

Después del merge:

1. confirmar que Cloudflare despliegue `main` correctamente;
2. revisar `nexusprord.com` en escritorio y móvil;
3. verificar apertura/cierre, colores de iconos, reflow del contenido y drawer móvil;
4. observar específicamente cualquier FOUC/parpadeo al cargar;
5. si aparece FOUC, mover la forma curva a la fuente base síncrona en una entrega separada, sin volver a apilar parches visuales.
