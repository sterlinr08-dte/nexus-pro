## Claude — 2026-08-12 01:40

### Subfase 2B, bloque #1 — APLICADO a producción (solo este bloque, nada más)

El dueño autorizó continuar ("Continuamos") tras entregar el mapa de auditoría
(`2026-08-12-0056-claude.md`). Apliqué **exclusivamente** el primer bloque que dejé propuesto en
`2026-08-12-PROPUESTA-2B-primer-bloque.sql` — el de riesgo verificado en cero. No toqué nada del
resto de 2B (ni `clientes`/`facturas`/`asientos` más allá del trigger, ni
`mis_cuentas_bancarias`, ni las 2 RPC de cobro del hallazgo central).

### Migración aplicada

`seguros_subfase_2b_bloque1_ncf_recibo_asientos` — transaccional, un solo bloque:

1. `REVOKE INSERT, UPDATE, DELETE, TRUNCATE ON secuencias_ncf FROM authenticated, anon` (SELECT
   se queda).
2. Mismo REVOKE sobre `recibo_contador`.
3. `seguros_bloquear_delete_asiento()` + `trg_seguros_bloquear_delete_asiento` — trigger
   `BEFORE DELETE` en `asientos`, mismo patrón que `trg_seguros_bloquear_delete_abono` (Fase 1).

### Metodología — branch de Supabase falló, mismo problema ya documentado

Intenté crear un branch de Supabase primero (`subfase-2b-primer-bloque`) para probar ahí antes de
tocar producción — quedó en `MIGRATIONS_FAILED`, la misma limitación de branches con el historial
de migraciones de este proyecto que ChatGPT ya había aceptado como conocida en
`2026-08-11-2038-chatgpt.md` ("la fuente de verdad debe ser producción + SQL capturado de
producción, no ese branch obsoleto"). Borré el branch fallido (evitar seguir cobrando) y usé el
mismo método que cerró 2A: **aplicar dentro de `BEGIN...ROLLBACK` contra producción real**, medir,
y solo si todo cuadra, aplicar la migración transaccional real.

### Prueba dentro de `ROLLBACK` (antes de aplicar de verdad)

| Chequeo | Antes | Después (dentro del ROLLBACK) |
|---|---|---|
| `authenticated` INSERT en `secuencias_ncf` | `true` | `false` |
| `anon` UPDATE en `recibo_contador` | `true` | `false` |
| `authenticated` SELECT en `secuencias_ncf` (no debe cambiar) | `true` | `true` |
| `DELETE` sobre un asiento real | — | bloqueado: *"No se permite eliminar asientos contables. Registre un asiento de reversión."* |
| `siguiente_ncf('B02')` como `service_role` tras el revoke | — | `B0200000609` — sigue funcionando |
| `next_recibo_anio(2098)` como `service_role` tras el revoke | — | `1` — sigue funcionando |

Los 6 resultados calzan con lo esperado. Confirmé con un `SELECT` aparte que el `ROLLBACK` no dejó
ningún residuo (el REVOKE y el trigger seguían en su estado original antes de aplicar de verdad).

### Aplicado en real — verificación post-aplicación

```
auth_insert_ncf: false · auth_select_ncf: true · anon_insert_ncf: false
auth_update_recibo: false · anon_update_recibo: false · trigger_exists: 1
```

Y, dentro de un segundo `BEGIN...ROLLBACK` (solo para verificar, sin escribir nada persistente),
confirmé con las identidades REALES del sistema — no solo `service_role` — que el flujo normal de
NCF/recibo sigue funcionando tal cual estaba:

- `siguiente_ncf('B02')` como `robinson` (agente real de nexus-pro) → `B0200000609` — igual que
  antes del bloque.
- `seguros_diagnostico_financiero()->>'ok'` como `sterlin08` (admin real de nexus-pro) → `true`.

### Advisors de seguridad — sin hallazgos nuevos

Corrí `get_advisors(security)` después de aplicar. Los WARN que aparecen son exactamente los mismos
ya documentados en fases anteriores (funciones SECURITY DEFINER con guard interno propio —
`mi_rol`/`mi_organizacion`/`siguiente_ncf`/`next_recibo*`/`pos_siguiente_ncf`/`pos_transferir_stock`/
etc. —, `cron_secretos` sin policy por diseño, protección de contraseñas filtradas desactivada, un
pendiente del dueño ya conocido). Ninguno nuevo por este cambio; `seguros_bloquear_delete_asiento()`
ni siquiera aparece en la lista — es una función de trigger interna, no expuesta como RPC.

### Lo que queda pendiente de 2B (sin tocar, a propósito)

Del orden de migración gradual (`2026-08-12-0056-claude.md`, punto 7): quedan los pasos 2-6
(`mis_cuentas_bancarias` admin-only, RPC de asiento manual balanceado, RPC de
corrección-de-precio/anulación de factura, separación de campos financieros de `clientes`, y al
final el par `seguros_registrar_cobro`/`seguros_reversar_cobro` con su guard nuevo). Ninguno se
tocó — cada uno necesita su propio diseño/RPC antes de poder cerrar REST sin romper funciones en
uso, tal como quedó documentado.

Sin cambios a UI, `version.json`, workflows ni `wrangler.jsonc`. Sin tocar `mi_rol()`/
`mi_organizacion()`. Sin abrir 2C.
