# ChatGPT — 2026-08-12 09:16

## Subfase 2B — Bloque 3B: migrar flujos automáticos de asientos a servidor — SOLO auditoría/diseño primero

Claude: Bloque 3A queda CERRADO. Abro exclusivamente el Bloque 3B. NO cerrar todavía el RLS global de `asientos` y NO abrir Bloque 3C ni Bloque 4.

### Objetivo
Eliminar las dos escrituras directas restantes sobre `asientos` desde el frontend de Seguros:

1. `_genFacturasInterno()` — actualmente genera facturas y luego hace INSERT directo de asientos.
2. `anularFactura(facturaId)` — actualmente genera el asiento inverso con INSERT directo.

La meta es que ambos efectos contables pasen a operaciones server-side transaccionales, con guardias reales de organización/rol, trazabilidad e idempotencia, antes de cerrar la escritura REST directa de `asientos` en 3C.

### Primera tarea: AUDITORÍA + DISEÑO, NO implementar todavía

1. Relee las funciones exactas actuales de `_genFacturasInterno()` y `anularFactura()` en `index.html`, incluyendo TODO lo que tocan además de `asientos`: clientes, facturas, NCF, deuda, estado, WhatsApp, UI, etc.
2. Identifica qué parte debe quedar frontend/post-commit y qué parte debe entrar en una transacción PostgreSQL.
3. Compara con `crear_factura_auto_tx(...)` ya existente. Determina si conviene:
   - reutilizar/endurecer esa RPC para generación manual;
   - crear una RPC nueva para lote/manual;
   - o separar una RPC por factura y coordinar el lote desde frontend.
   No duplicar lógica fiscal/contable innecesariamente.
4. Diseña la anulación como operación atómica: estado de factura + reverso de deuda/saldos aplicables + asiento de reversa + auditoría, sin hard-delete ni UPDATE destructivo del asiento original.
5. Verifica comportamiento con facturas pagadas/parcialmente pagadas/anuladas, deuda anterior, NCF ya emitido y reintentos.
6. Define idempotencia real para generación y anulación; un doble click/reintento no puede duplicar factura, NCF, deuda ni asiento.
7. Revisa qué rol real debe poder ejecutar cada flujo. No asumir que porque hoy el botón sea alcanzable por agente deba conservarse: documenta uso actual y propone el mínimo privilegio razonable sin romper operación.
8. Verifica `SECURITY INVOKER/DEFINER`, `search_path`, grants y cómo funcionará cuando en 3C se retire INSERT/UPDATE directo a `authenticated` sobre `asientos`.
9. Incluye `BEGIN...ROLLBACK`/`SET ROLE authenticated` con identidades reales para probar admin nexus-pro, agente nexus-pro, cross-org, anon y service_role según aplique.
10. No tocar todavía `all_asientos` ni revocar su escritura global.

### Entregable requerido
Nueva entrada en `docs/bitacora/` con:

- mapa exacto del flujo actual de generación y anulación;
- riesgos actuales CRÍTICO/ALTO/MEDIO;
- arquitectura propuesta y justificación;
- definición exacta de RPC(s) propuesta(s);
- matriz actor × operación;
- estrategia de idempotencia;
- SQL propuesto en archivo separado marcado `NO APLICAR`;
- cambios frontend exactos que luego serían necesarios;
- plan de rollback;
- matriz de pruebas;
- qué quedaría pendiente para 3C.

### Reglas duras
- NO aplicar SQL persistente todavía.
- NO modificar `main` funcionalmente.
- NO publicar.
- NO tocar el RLS global de `asientos`.
- NO tocar Bloque 3A salvo bug demostrado.
- NO abrir 3C ni Bloque 4.
- NO mezclar otras fases/módulos.

Cuando entregues diseño + pruebas de 3B, haré revisión cruzada antes de autorizar producción.