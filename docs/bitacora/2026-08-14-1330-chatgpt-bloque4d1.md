# ChatGPT — Subfase 2B / Bloque 4D-1 — `entregas_admin` — AUDITORÍA / DISEÑO, NO IMPLEMENTACIÓN

Fecha: 2026-08-14 13:30 RD

## Estado
Bloques 4A y 4B cerrados. Auditoría 4D (`6d8f44d`) confirmó que `entregas_admin` afecta directamente el KPI operativo “Dinero en Mano” y que hoy sus controles de rol dependen demasiado del cliente. 4D-2 (`cuadre_tss_historial`) y 4D-3 (`pagos`) quedan pendientes. 4C permanece cerrado/pending y NO se toca.

## Objetivo de 4D-1
Diseñar el hardening definitivo de `entregas_admin` sin romper ninguno de sus dos caminos legítimos de creación:
1) entrega manual administrada;
2) entrega automática vinculada a un cobro/deposito directo.

La solución debe eliminar escritura REST financiera directa, preservar trazabilidad e idempotencia y definir explícitamente qué actor puede hacer cada transición.

## Trabajo requerido — SOLO AUDITORÍA / DISEÑO
1. Releer producción y `parches.js` actual. No trabajar de memoria.
2. Documentar el modelo de estados implícito actual de `entregas_admin`: creado, confirmado, depositado, anulado/eliminado, y cualquier combinación real existente en las 187 filas. Cuantificar combinaciones y anomalías sin corregirlas.
3. Determinar exactamente cómo `calcularPorAgente()` y cualquier otro KPI consume `entregas_admin`: qué filas cuentan, desde qué momento y con qué signo. Proponer cómo debe comportarse una fila anulada para no distorsionar “Dinero en Mano”.
4. Diseñar transición de DELETE físico → anulación trazable. El registro original debe sobrevivir. Debe quedar actor, fecha, motivo y old/new data suficiente en auditoría.
5. Diseñar RPC separadas, salvo que la evidencia justifique otra forma:
   - `seguros_registrar_entrega_admin_manual(...)` — solo admin;
   - `seguros_registrar_entrega_directa_cobro(...)` — operación automática vinculada a un `abono` real y al actor/agente correcto;
   - `seguros_confirmar_entrega_admin(...)` — decidir rol exacto con evidencia;
   - `seguros_depositar_entrega_admin(...)` — decidir rol exacto con evidencia;
   - `seguros_anular_entrega_admin(...)` — previsiblemente admin, con motivo obligatorio.
   Si alguna transición no merece RPC propia, justificarlo.
6. Para la creación automática vinculada al cobro: NO confiar en `sesion.agente_id`, `sesion.nom`, `miCuentaId()` ni otros datos de memoria JS. Resolver identidad desde backend (`auth.uid()`/profiles/agente_id/relaciones reales) y validar que el cobro/abono existe, pertenece a nexus-pro y corresponde al contexto esperado.
7. Revisar si `cobro_id`, `cliente_id` y `agente_id` deben adquirir FK formales. No agregar FK a ciegas: primero demostrar que los 187 registros actuales cumplen integridad o cuantificar excepciones.
8. Diseñar idempotencia por operación. Doble clic/reintento no puede duplicar una entrega ni repetir confirmación/deposito/anulación.
9. Revisar concurrencia: dos confirmaciones/depositos/anulaciones simultáneas deben producir un único resultado válido.
10. Definir ACL/RLS final: navegador sin INSERT/UPDATE/DELETE/TRUNCATE financiero directo; `authenticated` conservar SELECT solo si el uso real lo necesita. `anon` cero. RPC `SECURITY DEFINER` con `search_path` fijo, guard org/rol y EXECUTE mínimo; `REVOKE ... FROM PUBLIC, anon` explícito.
11. Revisar si la tabla requiere `estado` explícito o si los flags actuales (`confirmado`,`depositado`) + nuevas columnas de anulación son suficientes. Preferir el modelo mínimo que evite estados imposibles. Si propone `estado`, definir máquina de estados y CHECKs.
12. Proponer constraint/checks necesarios: monto > 0, coherencia de timestamps/flags, `es_directo` vs `cobro_id`, etc. Solo con evidencia del modelo actual.
13. Revisar `comprobante_url`: hoy se actualiza por PATCH posterior. Determinar si puede migrarse dentro de RPC sin romper el flujo; si debe quedar metadata column-scoped temporalmente, justificarlo exactamente como se hizo en 4A.
14. Revisar auditoría actual y diseñar eventos: ENTREGA_REGISTRADA, CONFIRMADA, DEPOSITADA, ANULADA, con actor fuerte y datos suficientes.
15. Entregar SQL exacto propuesto + rollback, pero NO aplicarlo. Para cualquier CREATE OR REPLACE de función existente, diff fresco contra `pg_get_functiondef()`.
16. Proponer cambios frontend mínimos: reemplazo de POST/PATCH/DELETE directos, eliminación de gates de seguridad usados como autoridad real (pueden quedar como UX), manejo visible de errores, idempotency keys y visualización de anulados.

## Matriz obligatoria de pruebas de diseño
Usar `docs/METODOLOGIA-PRUEBAS-DESTRUCTIVAS.md` y rollback estructural forzado:
- admin crea entrega manual válida;
- agente NO puede crear manual por RPC;
- creación automática desde cobro legítimo funciona para el actor permitido;
- creación automática con cobro inexistente/ajeno/incorrecto se bloquea;
- cross-org bloqueado;
- anon bloqueado;
- monto <= 0 bloqueado;
- doble clic/reintento de cada operación no duplica;
- confirmar/depositar/anular transición válida;
- transición inválida (ej. depositar anulada, anular dos veces con distinta key, confirmar lo ya anulado) bloqueada o responde idempotente según diseño;
- REST directo INSERT/UPDATE/DELETE/TRUNCATE bloqueado en diseño final;
- cálculo de “Dinero en Mano” antes/después de crear/anular mantiene semántica correcta;
- cero residuos tras rollback;
- `seguros_diagnostico_financiero()` sigue `ok:true` sin modificar su definición.

## Preguntas que Claude debe responder con evidencia
- ¿Quién debe poder confirmar y depositar: solo admin o existe un actor/agente legítimo adicional?
- ¿La entrega automática debe nacer ya `confirmado=true` en algún caso? Si sí, qué condición SERVER-SIDE lo decide.
- ¿`depositado` cambia alguna cifra financiera o es solo seguimiento operativo?
- ¿Una entrega anulada debe excluirse de todos los cálculos? Enumerar cada consumidor a modificar.
- ¿Qué integridad real tienen hoy `agente_id`, `cliente_id`, `cobro_id`? Cuántas filas inválidas/huerfanas existen.
- ¿Hay entregas duplicadas probables hoy? Proponer criterio de detección sin borrar nada.

## Límites duros
- NO aplicar SQL/migraciones.
- NO publicar frontend.
- NO modificar datos reales persistentemente.
- NO tocar 4D-2, 4D-3 ni 4C.
- NO crear asientos contables para entregas salvo que el sistema existente demuestre que corresponde; si falta contabilidad, documentar para decisión separada.
- NO cambiar `seguros_diagnostico_financiero()`.
- NO limpiar duplicados/anomalías históricas.

## Entrega
Crear entrada NUEVA e inmutable `docs/bitacora/AAAA-MM-DD-HHMM-claude-bloque4d1.md` + SQL propuesto NO APLICAR si corresponde, con evidencia, matriz, máquina de estados/transiciones, ACL/RLS, idempotencia, rollback y recomendación de implementación. Esperar revisión cruzada de ChatGPT antes de aplicar.