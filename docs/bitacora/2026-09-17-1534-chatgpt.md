# 2026-09-17 15:34 — ChatGPT — Auditoría de acumulados/transferencias/cierre y siguiente bloque real

## Para Claude

Continuación autorizada por el dueño después de cerrar la iniciativa del sidebar curvo.

Revisé `CLAUDE.md`, las bitácoras de septiembre y las migraciones reales de Fase A/B/C. La sección de `CLAUDE.md` que presenta **acumulados, transferencias y cierre por ciclo** como “próxima iniciativa” quedó desfasada respecto al estado real del repositorio: el backend ya fue implementado, endurecido y publicado.

### Ya existe y NO debe duplicarse

Fuente financiera real:
- `abonos`
- `transferencias_agentes`
- `entregas_admin`

RPC/lecturas ya existentes:
- `seguros_resumen_ciclo_agente(uuid,text)`
- `seguros_resumen_ciclo_admin(text)`
- `seguros_cierre_ciclo_admin(text)`
- `seguros_reporte_mensual_admin(text)`
- transferencias por `transferencias_crear/aceptar/rechazar`

Cierre:
- ciclo contable fijo día 20 00:00 → día 20 00:00, `America/Santo_Domingo`;
- snapshot inmutable;
- cierre bloqueado si `abs(diferencia_reconciliacion) > 0.005`;
- custodia positiva y deuda de agentes se agregan por separado;
- transferencias internas mueven custodia pero NO aumentan `total_negocio_cobrado`.

No se va a crear una segunda contabilidad, otro ledger ni fórmulas financieras en JavaScript.

## Brecha de producto encontrada

Las RPC de Fase A/C existen, pero la UI normal de NEXUS PRO todavía no expone de forma directa el consolidado 20→20 y el snapshot del cierre. El `Reporte por agente` actual es legado y no está conectado a estas RPC nuevas.

Por tanto, el siguiente trabajo será **solo lectura/UI** dentro del reporte existente `v-rep-agente`, sin crear un módulo de navegación paralelo.

Debe permitir ver, por ciclo:
- cobrado validado;
- transferido confirmado;
- recibido confirmado;
- entregado a admin/directo;
- custodia/saldo final;
- diferencia de reconciliación;
- marca de histórico aproximado;
- total real cobrado por el negocio, sin sumar transferencias.

Para ciclos cerrados debe preferirse el snapshot inmutable `seguros_cierre_ciclo_admin`. Para un ciclo abierto se usa `seguros_resumen_ciclo_admin`. El frontend mostrará las cifras devueltas por servidor: **no recalculará dinero**.

## Alcance de implementación

Voy a preparar en rama de revisión un bloque compacto “Ciclo 20→20” integrado al `Reporte por agente` existente. Sin tocar Supabase, cobros, transferencias, cierres, contabilidad, WhatsApp ni POS.

Antes de producción: Claude debe revisar específicamente que la UI use las RPC correctas, que no mezcle snapshot cerrado con cálculo vivo y que no duplique ningún flujo financiero.
