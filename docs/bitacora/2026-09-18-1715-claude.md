# 2026-09-18 17:15 — Claude — spring-ind (indicador de resorte) más rígido

## Para ChatGPT

El dueño reportó que el pill que se desliza entre ítems de menú activo (`spring-ind`) se mueve muy
lento. No es una transición CSS — es un resorte físico simulado en JS dentro de `index.html`
(comentario propio del código: "física, no CSS transitions — mockup aprobado 12-ago-2026"), con
`requestAnimationFrame` y constantes de rigidez/amortiguación.

## Cambio

`index.html`, dentro del IIFE de `nxSidebarSpringSync` (línea ~3017):

```js
var STIFF = 210, DAMP = 22;   // antes
var STIFF = 380, DAMP = 30;   // ahora
```

Subí `STIFF` (rigidez del resorte, hace que llegue más rápido al objetivo) y escalé `DAMP`
proporcionalmente (`22 × √(380/210) ≈ 30`) para mantener aproximadamente la misma razón de
amortiguamiento (≈0.77, subamortiguado con un rebote sutil) y que no se vuelva más "brincón" al
subir la rigidez — solo más rápido.

No toqué nada de `parches-sidebar-curva.css` en esta entrada (ese archivo solo ajusta color/forma
del pill, no su física de movimiento).

## Estado

Editado directo en `index.html` (no hay capa de parche para sobrescribir constantes de JS inline).
Sin bump de `APP_VERSION` — este cambio es dentro del propio `index.html`, no de un archivo de
parche cacheado por `b=`, y `index.html` se sirve fresco en cada request según lo observado toda
esta sesión.

## Publicación

Directo a `main`, continuación del mismo entregable ya autorizado.
