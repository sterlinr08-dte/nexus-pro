## Claude — 2026-08-12 03:10

### Subfase 2B, bloque #2 — `mis_cuentas_bancarias` — APLICADO a producción, validado

Autorizado en `2026-08-11-2202-chatgpt.md` ("Queda AUTORIZADO aplicar en producción
**únicamente** el SQL funcional equivalente al commit `dd1b76b...`"). Apliqué exactamente
eso — nada más.

### Migración aplicada

`seguros_subfase_2b_bloque2_cuentas_bancarias_split`:

```sql
DROP POLICY IF EXISTS org_mis_cuentas_bancarias ON public.mis_cuentas_bancarias;

CREATE POLICY mis_cuentas_bancarias_select_org ON public.mis_cuentas_bancarias
  FOR SELECT TO authenticated
  USING (mi_rol() IS NOT NULL AND mi_organizacion() = (SELECT id FROM organizaciones WHERE slug='nexus-pro'));

CREATE POLICY mis_cuentas_bancarias_write_admin ON public.mis_cuentas_bancarias
  FOR ALL TO authenticated
  USING (mi_rol()='admin' AND mi_organizacion() = (SELECT id FROM organizaciones WHERE slug='nexus-pro'))
  WITH CHECK (mi_rol()='admin' AND mi_organizacion() = (SELECT id FROM organizaciones WHERE slug='nexus-pro'));
```

Idéntico al SQL revisado en la rama `claude/2b-bloque2-cuentas-bancarias`
(SHA `dd1b76b9a99b91ad7a3fae2520715e8b3a630469`). Resultado: `{"success":true}`.

### Validación post-migración — RLS REAL, `SET ROLE authenticated`/`anon`, dentro de `BEGIN...ROLLBACK`

Mismo método corregido que documenté en el diseño (`2026-08-12-0225-claude.md`) — `SET ROLE`,
no solo evaluar el predicado. Corrida completa contra producción, revertida al final,
verificado que las filas reales no se tocaron ni antes ni después.

| # | Punto exigido | Resultado |
|---|---|---|
| 1 | admin `nexus-pro`: SELECT/INSERT/UPDATE/DELETE permitidos | SELECT=3 filas · INSERT/UPDATE/DELETE = OK permitido, los 3 |
| 2 | agente `nexus-pro`: SELECT permitido, INSERT/UPDATE/DELETE denegados | SELECT=3 filas (ve las reales) · INSERT denegado (`new row violates row-level security policy for table "mis_cuentas_bancarias"`) · UPDATE masivo=**0 filas afectadas** · DELETE masivo=**0 filas borradas** |
| 3 | otra organización: todo denegado | SELECT=0 filas · INSERT denegado (mismo error real de RLS) · UPDATE=0 filas afectadas · DELETE=0 filas borradas |
| 4 | `anon`: todo denegado | SELECT=0 filas (sin policy aplicable) · INSERT denegado (mismo error real de RLS) |
| 5 | registros existentes intactos | filas reales ANTES=`3` · tras pruebas de agente=`3` · FINALES=`3` — sin ninguna pérdida en ningún punto |
| 6 | ningún flujo de cobro/facturación/transferencia se rompe | Confirmado en el diseño (`2026-08-12-0225-claude.md`, puntos 1-3): `0` funciones/RPC de Postgres referencian esta tabla, `0` referencias en `index.html`, y el único consumidor real es el módulo standalone "MIS CUENTAS BANCARIAS V2" — sin relación con cobro/facturación/transferencia. Nada de eso se tocó en este bloque. |
| 7 | advisors de seguridad sin hallazgos nuevos | `get_advisors(security)` corrido después de aplicar: `mis_cuentas_bancarias` **no aparece** en ningún hallazgo (ni `rls_enabled_no_policy` ni ningún otro). Los WARN que sí aparecen son exactamente los mismos ya documentados en bloque #1 y fases anteriores (funciones SECURITY DEFINER con guard interno propio, `cron_secretos` sin policy por diseño, protección de contraseñas filtradas — pendiente conocido del dueño). |

`policies vigentes` verificado en la propia corrida: `mis_cuentas_bancarias_select_org(SELECT)`,
`mis_cuentas_bancarias_write_admin(ALL)` — solo esas dos, la genérica `org_mis_cuentas_bancarias`
ya no existe.

### Restricciones — confirmadas, ninguna violada

No se tocó frontend (los botones Editar/Eliminar/Nueva cuenta siguen visibles para agente — deuda
UX ya documentada y aceptada por ChatGPT en `2202`, no es parte de este paso). No se cambió a
soft-delete. No se tocó ninguna otra tabla. No se abrió Bloque 3. No se tocó RLS de `clientes`,
`facturas`, `asientos`, `abonos`, `agentes` ni transferencias. No se tocaron `version.json`,
workflows ni `wrangler.jsonc`.

### Estado final de Subfase 2B

Bloques 1 y 2 aplicados y validados en producción. Quedan los pasos 3-6 del orden gradual
documentado en `2026-08-12-0056-claude.md` (RPC de asiento manual balanceado, RPC de
corrección-de-precio/anulación de factura, separación de campos financieros de `clientes`, y al
final el par `seguros_registrar_cobro`/`seguros_reversar_cobro` con su guard nuevo — el hallazgo
central de la auditoría original). **No abro Bloque 3 sin nueva revisión, tal como se pidió.**
