## ChatGPT — 2026-09-10 00:08

# Hotfix Fase B — Meta/Zernio rechazó las plantillas por variable al final

Después de publicar la Fase B y ejecutar `asegurar_plantillas`, Zernio respondió HTTP 200 pero rechazó la creación de las dos plantillas nuevas:

- `transferencia_confirmada_emisor`
- `transferencia_recibida`

Error exacto en ambas: **“Las variables no pueden estar al principio ni al final de la plantilla.”**

La causa fue que los dos textos terminaban, semánticamente, con `{{4}}` (solo seguido de punto). No es un problema del trigger, de Supabase ni de la transferencia financiera; es una regla de validación de plantillas de Meta/Zernio.

## Corrección

Rama: `chatgpt/fase-b-fix-plantillas-transferencias`.

Se modificó únicamente el texto de las dos plantillas para agregar contenido fijo después de la última variable:

- Emisor: `... ahora es RD$ {{4}}. Consulta el detalle en NEXUS PRO.`
- Receptor: `... ahora es RD$ {{4}}. Consulta el detalle en NEXUS PRO.`

No se cambió:

- la cantidad ni el orden de variables;
- el trigger `trg_whatsapp_transferencia_aceptada`;
- la matemática de custodia;
- ninguna tabla financiera;
- ninguna transferencia real.

## Siguiente paso

Fusionar este hotfix, desplegar nuevamente `whatsapp-pagos-agente-notificar` y volver a ejecutar `asegurar_plantillas`. El resultado esperado es que las dos plantillas nuevas queden creadas con estado `PENDING` mientras Meta las revisa.
