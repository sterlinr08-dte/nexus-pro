# 2026-09-18 13:30 — Claude — Prototipo: sidebar desktop con carril azul curvo + panel blanco recto (NO publicado)

## Para ChatGPT

El dueño dio una instrucción exacta y detallada: el diseño actual (panel azul único que cambia de
ancho) no es lo que pidió — quiere **dos piezas visuales dentro del mismo componente**: un carril
azul curvo de ancho fijo (solo íconos, igual colapsado o expandido) y, a su derecha, un panel
blanco recto/lineal que aparece con las etiquetas de texto al expandir. Solo para desktop; móvil no
se toca.

## Rama

`claude/sidebar-desktop-rail-panel` (desde `origin/main`). **No publicada.** El dueño pidió
explícitamente ver vista previa antes de aprobar.

## Qué cambia (`parches-sidebar-curva.css`, reescrito)

- **Una sola pieza de sidebar** (`nav.sb`/`#sbEl`), sin duplicar markup ni montar otra capa. El
  carril azul es un `::before` (pseudo-elemento) de ancho fijo `--nx-rail:68px`, con su propia
  curva (`border-radius:0 30px 30px 0`) — es la ÚNICA parte con curva. El resto de `nav.sb` es
  `background:#fff` sin `border-radius` (recto).
- **Mismo ancho de carril colapsado y expandido** (68px) — lo que cambia es si el panel blanco de
  la derecha tiene ancho (210px total expandido) o no (68px total colapsado, carril solo).
- Íconos (círculos de color existentes), logo y avatar quedan centrados dentro del carril con
  `margin-left` calculado, en ambos estados — no se tocó el sistema de íconos de colores.
- Textos (`sb-nm`, `ni-l`, etc.) pasan a colores oscuros sobre blanco (antes blanco sobre azul).
  Ítem activo: tinte azul claro (`rgba(37,99,235,.10)`) + texto azul, en vez del pill blanco sobre
  azul de la versión anterior.
- Transición: solo `width` (0.22s ease) — se mantiene la lección del fix de rendimiento de hoy, sin
  animar `border-radius` ni `box-shadow`.
- **Móvil intacto**: dupliqué explícitamente dentro de `@media (max-width:768px)` exactamente las
  reglas de color que ya están en producción (panel azul único, pill blanco activo, tarjeta de
  perfil blanca) — cero cambios visuales en el drawer móvil. El bloque de forma/transición móvil
  tampoco se tocó.

Build interno `b=5886 → b=5887`.

## Pendiente

- Verificar visualmente en vista previa (colapsado y expandido) antes de mostrar al dueño.
- Confirmar que el centrado del ícono dentro del carril se vea bien (cálculo aproximado, puede
  necesitar un ajuste fino de 2-4px tras verlo renderizado).
- Confirmar que el contenido principal se reacomoda correctamente (heredado del layout flex
  existente, no debería requerir cambios, pero se verifica).

## Estado

Solo en rama. No publicado a `main`. Esperando aprobación explícita del dueño tras ver la vista
previa, como pidió.
