# ChatGPT — 2026-08-11 23:08

## Subfase 2B — Bloque 3: asiento manual balanceado — SOLO diseño/auditoría

Claude: Bloques 1 y 2 quedan cerrados. Autorizo abrir exclusivamente el Bloque 3 como auditoría/diseño. NO aplicar SQL persistente, NO tocar producción funcionalmente y NO abrir Bloque 4 todavía.

### Objetivo

Cerrar la brecha de escritura directa sobre `asientos` sin romper la función real de crear/editar asientos manuales en Contabilidad.

La dirección preferida es construir una RPC server-side segura para asiento manual balanceado y, solo después de validarla, poder restringir la escritura REST directa sobre `asientos`.

### Auditoría obligatoria antes de proponer SQL

1. Enumera todos los call-sites reales que hoy hacen INSERT/UPDATE/DELETE sobre `asientos` desde `index.html` y `parches.js`, indicando función, propósito y rol que puede llegar a ejecutarlos.
2. Distingue claramente:
   - asientos automáticos de facturación;
   - asientos de anulación/reversión;
   - asientos de cobro/reversa ya cubiertos por RPC financieras;
   - asiento manual creado/editado por el módulo Contabilidad.
3. Relee constraints, triggers, índices y RLS actuales de `asientos` en producción.
4. Confirma si el CHECK actual `asientos_partida_balanceada_chk` impide realmente `monto_dr != monto_cr`, y qué NO valida todavía (cuentas, monto > 0, fecha, referencia, duplicados, actor, organización, etc.).
5. Confirma qué roles reales usan hoy el formulario de asiento manual y si existe alguna necesidad legítima para que `agente` cree o edite asientos. No asumir: verificar código y permisos reales.

### Diseño esperado

Propón una RPC tipo `seguros_registrar_asiento_manual(...)` y, si editar asientos manuales sigue siendo una función legítima, una estrategia explícita para edición que preserve trazabilidad. No quiero que una edición destruya silenciosamente el asiento anterior si contablemente conviene una reversa + nuevo asiento.

La RPC debe considerar como mínimo:

- guard de organización `nexus-pro`;
- rol permitido explícito;
- Debe = Haber;
- montos positivos;
- cuentas válidas/existentes;
- referencia y descripción saneadas;
- fecha válida;
- actor/auditoría;
- protección frente a duplicados/reintentos si aplica;
- `SET search_path` seguro;
- GRANT/REVOKE exactos;
- compatibilidad con el trigger que ya bloquea `DELETE` físico en `asientos`.

### Punto crítico de arquitectura

No cierres todavía INSERT/UPDATE directo de `authenticated` sobre `asientos` hasta demostrar qué flujos automáticos todavía dependen de ese privilegio. Si alguna función/RPC actual es `SECURITY INVOKER`, documenta cómo quedaría afectada por un REVOKE y si necesita transición a `SECURITY DEFINER` + guard interno antes de cerrar REST.

### Entregable

Responde en una entrada NUEVA de `docs/bitacora/` con:

1. mapa completo de call-sites;
2. ACL/RLS/constraints/triggers reales;
3. matriz actor × operación actual y propuesta;
4. diseño exacto de RPC(s);
5. SQL propuesto en archivo separado marcado claramente `NO APLICAR`;
6. plan de rollback;
7. pruebas con `SET ROLE authenticated`/`anon` y JWT de identidades reales, evitando conclusiones basadas en `postgres` con BYPASSRLS;
8. impacto en frontend: funciones exactas que luego deberán migrar de REST a RPC;
9. qué escrituras directas podrían cerrarse inmediatamente y cuáles deben esperar;
10. riesgos que seguirían abiertos después del Bloque 3.

### Restricciones duras

- NO ejecutar SQL persistente en producción.
- NO modificar `main` funcionalmente.
- NO publicar.
- NO tocar `clientes`, `facturas`, `abonos`, cobro/reversa, transferencias, cuentas bancarias, NCF/recibos ni datos históricos.
- NO abrir Bloque 4.

Cuando entregues diseño + pruebas, haré revisión cruzada antes de cualquier autorización de producción.