# STUDIO — base propia creada, esquema POS aplicado, semilla y admin (Fase 2)

**Fecha:** 2026-09-21
**Autor:** Claude
**Rama:** `claude/review-claude-md-jpzk7k` — **no publicado a `main`** (pendiente de autorización)
**Bitácora previa:** `2026-09-21-1945-claude.md`

## Hecho

1. **Proyecto Supabase** creado por el dueño desde el panel (el MCP `create_project` falló 4 veces):
   **STUDIO RD**, ref `edbknlkjnlfmkkiizdbe`, `https://edbknlkjnlfmkkiizdbe.supabase.co`, us-east-1, Postgres 17.
   Costo aceptado: US$10/mes.
2. **Esquema aplicado** (migraciones `studio_01_extensiones` → `studio_10_ajustes_studio`, archivos de
   `supabase/studio/`), sin un solo error ni archivo modificado. Verificado en la base nueva:
   66 tablas (66 con RLS), 81 políticas, 73 funciones (md5 idéntico a la madre, 73/73), 72 triggers,
   63 FKs, 3 secuencias, uuid-ossp + pgcrypto, buckets `comprobantes` (público) y `documentos`.
3. **Semilla** (`studio_11_semilla`, archivo `11_semilla_studio.sql`): organización `studio` con id fijo
   `e404d1c4-24c5-4e17-88f6-84bef09d6d19` (mismo id que su fila en la madre y que usan las funciones
   contables), `usuarios_sistema` admin (`login=admin`), `pos_config` (CO/CR, mora 0), Almacén Principal,
   12 secuencias de documentos, plan de cuentas 1101…5101 + 4103 (mora). **Sin NCF**: el rango DGII lo carga
   el cliente desde Ajustes.
4. **Usuario Auth del admin** creado por SQL (no por migración, para no versionar la clave):
   `auth.users` + `auth.identities` + `profiles` (`must_change_password=true`).
   Login en la app: **`admin`** (o `admin@studio`); correo sintético `admin@nexus-pro.local` (convención
   fija de `doLoginAuth` en `index.html`). Clave provisional entregada al dueño por chat; **no está en el repo**.
5. **Prueba**: sesión simulada por SQL (`request.jwt.claims` del admin) → `mi_organizacion()` = STUDIO,
   `mi_rol()` = admin, RLS devuelve solo sus filas. El login HTTP no se pudo probar desde el entorno
   (proxy bloquea `*.supabase.co` directo), se prueba en el navegador tras publicar.
6. **Frontend**: `NX_BASES_POR_HOST` en `index.html` ya apunta `studiord.net`, `www.studiord.net` y
   `studio.nexusprord.com` a la base STUDIO (commit `76df408`). Otros hosts: base madre, sin cambios.
7. **Dominio de marca** `studiord.net` comprado por el dueño en Namecheap (orden 214730287); en proceso
   de agregarse a Cloudflare.

## Estado de la base madre

Fila `organizaciones.slug='studio'`: `tipo='externa'`, `dominio='studiord.net'`, **`activo=false`**.
Se activará (y se llenará `auth_url`/`auth_key`/`email_dominio='@nexus-pro.local'` para SSO) cuando el host
que sirve la app tenga el `index.html` con el mapa por dominio.

## Pendiente

- **Autorización del dueño para publicar a `main`** el mapa por dominio (inocuo para nexusprord.com).
- Cloudflare: custom domain `studio.nexusprord.com` en el Worker `nexus-pro` (para probar ya) y
  `studiord.net` + `www` cuando los nameservers estén en Cloudflare.
- Tras publicar: activar la org en la madre, probar login `admin` en el navegador, cambiar clave,
  venta de prueba, NCF del cliente, logo/color de STUDIO.

## Riesgos

- Ninguna tabla de la madre modificada salvo la fila `organizaciones` (inactiva).
- La anon key de STUDIO en `index.html` es pública por diseño (igual que la de la madre); la seguridad
  la dan RLS y Auth, ya activos en las 66 tablas.
