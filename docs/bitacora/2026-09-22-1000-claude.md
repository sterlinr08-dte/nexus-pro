# STUDIO sale de nexus-pro: repositorio `studio-rd`, Worker propio y dominios movidos

**Fecha:** 2026-09-22
**Autor:** Claude
**Rama:** `claude/review-claude-md-jpzk7k` — **no publicado a `main`** (pendiente de "publícalo")

## Solicitud del dueño

"Tenemos que cambiar todo porque STUDIO no tiene un repositorio independiente." Decisiones tomadas con el dueño:
base = copia del POS de NEXUS PRO, deploy = Cloudflare Worker propio, nombre `studio-rd`. Luego "Hazlo, tienes el acceso".

## Qué se hizo (fuera de este repositorio)

1. El dueño creó `sterlinr08-dte/studio-rd` (público) en GitHub. Claude subió a su `main` una copia del runtime de
   NEXUS PRO (141 archivos, commit `16287f9` sobre el "Initial commit" de GitHub), tomada del commit `6328e66` de esta
   rama (58.84 + login STUDIO) sin bitácoras, blueprints, Edge Functions ni migraciones de Seguros. Detalle en
   `studio-rd/docs/bitacora/2026-09-22-0940-claude.md`.
2. El dueño creó el Worker `studio-rd` en Cloudflare por "Import a repository" (rama `main`, sin build,
   `npx wrangler deploy`). Primer deploy 04:03 UTC; `studio-rd.sterlinr08.workers.dev` sirve el POS de STUDIO
   (título "Studio — Hogar, movilidad y tecnología", base `edbknlkjnlfmkkiizdbe`, sin rastro de la base madre).
3. Claude movió por API los dominios personalizados `studiord.net` (id `5d4308eb…`) y `www.studiord.net`
   (id `c5b76f07…`) del Worker `nexus-pro` al Worker `studio-rd` (`PUT /accounts/…/workers/domains` con
   `override_existing_origin:true`; los certificados se conservaron) y purgó la caché de la zona. Verificado:
   `https://studiord.net/` responde con STUDIO y `https://nexusprord.com/` sigue sirviendo NEXUS PRO 58.84.
   El Worker `nexus-pro` queda solo con `nexusprord.com`.

## Cambios en este repositorio (esta rama, commit nuevo encima de `6328e66`)

- `index.html`: se elimina el mapa `NX_BASES_POR_HOST` / `NX_STUDIO_BASE` (introducido en `63d3f7b`, `76df408`,
  `e43ec0a`). `SUPABASE_URL_FIXED` y `SUPABASE_KEY_FIXED` vuelven a ser constantes de la base madre. NEXUS PRO ya
  no conoce la base de STUDIO. Sin cambios funcionales para nexusprord.com.
- Se revierte el commit `6328e66` (login con piel STUDIO, `logo-studio.png`): esa piel ahora vive en `studio-rd`
  (`index.html` de ese repo, aplicada siempre) y no debe entrar en NEXUS PRO. Su bitácora
  `2026-09-22-0910-claude.md` se conserva como registro histórico.
- Resultado: `index.html` de esta rama = `index.html` de `main` (`3ba4ca1`) menos el mapa por dominio.

## Pruebas

- Los 5 bloques `<script>` inline de `index.html` compilan (`new Function`).
- Ninguna referencia restante a `edbknlkjnlfmkkiizdbe`, `NX_BASES_POR_HOST` ni `nx-studio` en `index.html`.
- Comprobación HTTP de `studiord.net`, `www.studiord.net/version.json` (58.84), `nexusprord.com` y `logo-studio.png`
  desde pg_net (todas 200).

## Riesgos / pendientes

- **Publicar esta limpieza a `main`** requiere "publícalo" del dueño. Mientras tanto nexusprord.com sigue
  llevando el mapa por dominio, que es inofensivo (solo actúa en hosts `studiord.net`, ya servidos por otro Worker).
- El dueño debe rotar la Global API Key de Cloudflare (se mostró en una captura) y actualizar `cloudflare_api_token`
  en el Vault de la base madre. Las Edge Functions de diagnóstico `nx-cf-dominio` (madre y STUDIO) y `nx-env-nombres`
  (BayolCell) pueden borrarse cuando ya no hagan falta.
- Desde ahora, todo lo de STUDIO (Financiamiento bloques B/D/E con ChatGPT, documentos, PWA, retiro de Seguros)
  se trabaja en `sterlinr08-dte/studio-rd`, nunca en nexus-pro.

## Siguiente paso recomendado

Que el dueño entre a `https://studiord.net` (limpiando caché si ve NEXUS), confirme, y autorice publicar esta
limpieza a `main` de nexus-pro.

## Publicación (adenda, misma sesión)

El dueño respondió "Publícalo". Se sube `APP_VERSION` a `58.85`, se anota en `version.json` y se mergea esta rama a
`main` (fast-forward). El Worker `nexus-pro` se redespliega por integración Git. La bitácora de `studio-rd`
(`claude/bitacora-despliegue`) se mergea a `main` de ese repo con la misma autorización.
