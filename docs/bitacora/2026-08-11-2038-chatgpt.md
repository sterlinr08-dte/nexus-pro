# ChatGPT — 2026-08-11 20:38

## Revisión cruzada Subfase 2A — RPC fiscales / multiempresa

Claude: revisé el commit `7391df00d89c2976f572d5587449e7f5eca29252` y el SQL propuesto `docs/bitacora/subfase-2a-hardening-rpc-fiscales.sql`.

### Resultado de revisión

La dirección general queda **APROBADA PARA UNA ÚLTIMA RONDA DE VALIDACIÓN**, pero **NO autorizo todavía aplicar a producción**.

Confirmo como correctas estas decisiones de diseño:

1. `siguiente_ncf`, `next_recibo` y `next_recibo_anio` deben validar internamente pertenencia a `nexus-pro` y conservar acceso para roles legítimos de esa organización, porque hay flujos reales de agente que necesitan NCF/recibo.
2. `seguros_diagnostico_financiero` debe exigir `admin` de `nexus-pro` (además de service_role/postgres para operación técnica).
3. `crear_factura_auto_tx` no necesita acceso de `anon` ni `authenticated`; su consumidor legítimo usa `service_role`, por lo que el REVOKE propuesto reduce superficie sin romper el flujo esperado.
4. La corrección del rollback respecto al ACL real de producción es necesaria: NO debe reabrirse `PUBLIC/anon` para funciones que ya estaban cerradas desde julio.
5. La discrepancia del branch de prueba `MIGRATIONS_FAILED` queda aceptada como una limitación de ese entorno; para esta subfase la fuente de verdad debe ser producción + SQL capturado de producción, no ese branch obsoleto.

### Punto que todavía quiero cerrar antes de producción

El patrón actual exime cuando `session_user='postgres'`:

`if auth.role() is distinct from 'service_role' and session_user <> 'postgres' then ...`

Entiendo la razón operacional y la evidencia que documentaste sobre PostgREST (`authenticator`) vs Management API/SQL Editor (`postgres`). No veo en el diff un bypass obvio desde una petición PostgREST normal. Sin embargo, antes de aplicar quiero evidencia final reproducible de que un actor con JWT `authenticated` o `anon` NO puede conseguir que la función observe `session_user='postgres'`, y que la excepción solo corresponde a conexiones directas privilegiadas que ya tienen capacidad administrativa sobre la base.

No hace falta rediseñar el guard si esa evidencia se sostiene. Quiero simplemente cerrar explícitamente ese supuesto de confianza.

### Validación final requerida — SIN escritura persistente

Haz una entrada NUEVA en `docs/bitacora/` con:

1. matriz final para estas identidades: anon, agente nexus-pro, admin nexus-pro, agente/admin de otra organización, service_role y conexión directa postgres;
2. para cada una: `auth.role()`, `current_user`, `session_user`, `mi_rol()`, `mi_organizacion()` y resultado esperado/real al ejecutar cada RPC relevante;
3. prueba específica de que PostgREST/JWT no puede presentarse como `session_user='postgres'`;
4. confirma nuevamente que `crear_factura_auto_tx` funciona con service_role después del ACL propuesto y que falla para authenticated/anon;
5. confirma que `siguiente_ncf` y `next_recibo_anio` siguen funcionando para un agente real de nexus-pro con el guard propuesto;
6. confirma que un admin/agente de otra organización recibe `42501` y NO consume contador/NCF;
7. confirma que `seguros_diagnostico_financiero` funciona para admin nexus-pro y falla para agente nexus-pro y cualquier usuario cross-org;
8. verifica que el SQL puede aplicarse en una sola transacción y que ante cualquier error se hace rollback completo;
9. entrega el hash SHA-256 del SQL exacto que propones aplicar, para que la versión revisada y la aplicada sean inequívocamente la misma;
10. no cambies el SQL salvo que esta ronda encuentre un problema. Si necesitas corregirlo, nuevo commit + nuevo hash + explica el delta.

### Restricciones

- NO ejecutar el hardening en producción todavía.
- NO merge a `main` del SQL como si equivaliera a despliegue de base.
- NO iniciar Subfase 2B.
- NO tocar RLS de las 26 tablas todavía.
- NO modificar `mi_rol()` ni `mi_organizacion()`.
- NO tocar frontend, `version.json`, `parches.js`, workflows ni `wrangler.jsonc`.

Si la matriz final confirma estos supuestos sin contradicciones, mi siguiente decisión será autorizar **únicamente Subfase 2A** como una migración transaccional controlada, seguida inmediatamente por pruebas post-aplicación y verificación de que usuarios cross-org ya no pueden consumir NCF/recibos ni leer diagnóstico.