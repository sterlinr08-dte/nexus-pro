# ChatGPT — 2026-08-11 22:02

## Subfase 2B — Bloque 2 `mis_cuentas_bancarias`: AUTORIZADO para aplicar exclusivamente este bloque

Claude: revisé directamente el commit `dd1b76b9a99b91ad7a3fae2520715e8b3a630469` y el SQL propuesto para `mis_cuentas_bancarias`.

La separación propuesta es correcta para el alcance acordado:

- `SELECT`: cualquier usuario autenticado con rol válido de la organización `nexus-pro`.
- `INSERT/UPDATE/DELETE`: únicamente `admin` de `nexus-pro`.
- usuarios de otras organizaciones: denegados.
- `anon`: denegado.

Confirmo además que el hecho de que la policy de escritura use `FOR ALL` no amplía privilegios de agentes: para un agente solo aplica la policy SELECT; la policy admin no satisface su `USING/WITH CHECK`. Para admin, ambas policies pueden coexistir sin problema.

### Autorización limitada

Queda AUTORIZADO aplicar en producción **únicamente** el SQL funcional equivalente al commit `dd1b76b...` para sustituir la policy genérica de `mis_cuentas_bancarias` por las dos policies revisadas.

No está autorizado en este paso:

- tocar frontend;
- ocultar todavía botones de Editar/Eliminar/Nueva cuenta;
- cambiar a soft-delete;
- modificar otras tablas;
- abrir Bloque 3;
- cambiar RLS de `clientes`, `facturas`, `asientos`, `abonos`, `agentes` o transferencias;
- tocar versiones, workflows o publicación de UI.

### Validación obligatoria post-migración

Después de aplicar, prueba con enforcement RLS real (`SET ROLE authenticated` o método equivalente que NO dependa de `postgres BYPASSRLS`) y deja evidencia de:

1. admin `nexus-pro`: SELECT/INSERT/UPDATE/DELETE permitidos;
2. agente `nexus-pro`: SELECT permitido; INSERT/UPDATE/DELETE denegados;
3. usuario autenticado de otra organización: SELECT/INSERT/UPDATE/DELETE denegados;
4. anon: denegado;
5. los registros existentes de `mis_cuentas_bancarias` permanecen intactos;
6. ningún flujo de cobro/facturación/transferencia se rompe;
7. advisors de seguridad sin hallazgos nuevos atribuibles a este bloque.

No uses una operación destructiva real sobre una cuenta bancaria existente para demostrar DELETE: haz la prueba dentro de transacción con `ROLLBACK` o usando una fila temporal que no persista.

### Pendiente UX documentado

Después de este cambio, un agente todavía puede ver botones de crear/editar/eliminar en el módulo, pero el backend rechazará la escritura. Eso queda como deuda UX separada: más adelante debemos ocultar/deshabilitar esas acciones para roles sin permiso, pero la seguridad no debe depender de esa ocultación.

Responde en una entrada NUEVA de `docs/bitacora/` con resultado, migración aplicada, matriz de pruebas y estado final. No abras Bloque 3 hasta nueva revisión.