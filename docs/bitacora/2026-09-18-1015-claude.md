# 2026-09-18 10:15 — Claude — Sidebar azul degradado (nueva decisión del dueño, revierte el blanco/vidrio de ayer)

## Para ChatGPT

El dueño compartió un mockup de referencia (NEXUS PRO, sidebar azul degradado, colapsado y
expandido, con tarjeta de perfil blanca flotando en el footer) y pidió implementarlo **solo en el
sidebar** — no toca ticker, dashboard, KPIs ni ninguna otra pantalla.

Confirmé con el dueño explícitamente antes de tocar código porque esto **revierte la decisión de
ayer** (`2026-09-17-1450-claude.md`: "mantener el blanco/vidrio actual"). Su respuesta fue clara:
sí, quiere el azul degradado como nueva dirección, alcance limitado al sidebar.

## Qué cambié

Rama: `claude/sidebar-azul-degradado` (desde `origin/main`, no publicada).

`parches-sidebar-curva.css`:
- Mantiene intacta toda la forma/curva/colapso que ya estaba probada en producción (bordes,
  transición, ocultamiento de texto al colapsar) — no se tocó esa parte.
- Se agregó una sección nueva, sin `@media`, que aplica igual en desktop/móvil y en
  colapsado/expandido (es el mismo `nav.sb`): fondo con degradado azul
  (`#4a7cf0 → #2e5bdb → #1e3fae`), `backdrop-filter:none` (ya no hace falta, el fondo es opaco),
  textos/íconos en blanco o blanco translúcido, ítem activo como pill blanco con texto/ícono azul
  oscuro, indicador `spring-ind` (el sistema de resaltado deslizante que ya existía) recoloreado de
  morado a blanco, badge ADMIN en blanco/azul para contraste, y la tarjeta de perfil del footer
  (`sb-u`) en blanco flotando sobre el azul — igual que el mockup.
- Todo con selectores `nav.sb ...` + `!important` para ganarle de forma predecible al bloque
  `NEXUS_SIDEBAR_V3` de `parches-seguros-base.js`, que carga antes en el DOM (mismo mecanismo ya
  verificado ayer para el fix de la curva).

`parches-seguros.js`: build `b=5883 → b=5884` (cache-bust para esta capa).

## Qué NO toqué

- `parches-seguros-base.js` (el bloque V3 sigue existiendo tal cual, solo queda sobrescrito para
  `nav.sb`).
- Ticker, dashboard, tarjetas KPI, gráficas, tipografía general — nada de eso cambia.
- Ninguna lógica de negocio, Supabase, WhatsApp ni Realtime.

## Pendiente antes de publicar

- Verificación en vivo (getComputedStyle + capturas) en escritorio y en ~390px, igual que se hizo
  con la curva blanca — todavía no la hice en esta entrada.
- Autorización explícita del dueño para publicar a `main` (por ahora solo existe la rama).

## Estado

Código en rama aislada, no publicado. Bitácora dejada antes de pedir la vista previa.
