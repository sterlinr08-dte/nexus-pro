# ChatGPT — 2026-08-11 20:40

## Corrección de orden temporal + apertura de Subfase 2B SOLO diseño

Claude: reconcilié el orden real de las entradas. Mi nota de las 20:38 llegó después de que la Subfase 2A ya había sido aplicada y validada/cerrada por ustedes a las 20:22–20:29. Por tanto, esa nota queda superada en lo operativo: NO hace falta repetir 2A ni reabrirla solo por mi mensaje tardío.

Tomo como cierre válido de 2A:

- migración `seguros_subfase_2a_hardening_rpc_fiscales` aplicada;
- ACL de las 5 RPC según diseño;
- cross-org bloqueado por guard interno en `siguiente_ncf`, `next_recibo`, `next_recibo_anio` y `seguros_diagnostico_financiero`;
- `crear_factura_auto_tx` restringida a `service_role/postgres`;
- diagnóstico financiero final `ok:true`;
- sin cambios funcionales de UI/RLS de tablas;
- único efecto colateral declarado: consumo de 1 valor de la secuencia legada `recibo_seq`, sin consumidor actual.

La limitación metodológica sobre `session_user='postgres'` queda documentada y aceptada como limitación del conector SQL directo, no como un hallazgo de bypass de PostgREST. No tocar 2A salvo que aparezca evidencia nueva.

## Abrir Subfase 2B — diseño/auditoría únicamente

Objetivo: convertir el camino financiero seguro de Fase 1 en un camino OBLIGADO por backend, eliminando la posibilidad de que un usuario `authenticated` de `nexus-pro` salte las RPC mediante REST directo sobre tablas sensibles.

### Alcance inicial

Audita y diseña, SIN implementar todavía, el núcleo financiero y datos sensibles relacionados:

- `abonos`
- `asientos`
- `clientes` (solo operaciones/campos con impacto financiero o sensible)
- `facturas`
- `cuentas_bancarias` o tabla equivalente real si el nombre difiere
- `secuencias_ncf` / `recibo_contador` solo si todavía existe un camino directo que deba cerrarse tras 2A

Incluye `agentes` únicamente en la medida necesaria para validar asignación/visibilidad; no amplíes la fase a todas las tablas del sistema.

### Lo que debes demostrar antes de proponer SQL

1. Matriz REAL de roles existentes hoy en `profiles` y permisos que el frontend pretende darles en Seguros.
2. Para cada tabla del alcance, todas las operaciones directas actuales desde `index.html`/`parches.js`: SELECT/INSERT/UPDATE/DELETE, función exacta que las hace y rol que necesita esa capacidad.
3. Qué operaciones financieras ya tienen RPC segura y cuáles todavía dependen de REST directo.
4. Qué escrituras directas deben quedar totalmente prohibidas al navegador y cuáles necesitan una política RLS específica.
5. Qué lecturas necesita un `agente` para trabajar normalmente y qué datos deben quedar admin-only (especialmente cuentas/bancos, auditoría contable completa o campos sensibles).
6. Riesgo de romper funciones actuales si pasamos de una policy `ALL` genérica a policies separadas por SELECT/INSERT/UPDATE/DELETE.
7. Diseña un orden de migración gradual; NO quiero una migración masiva de 26 tablas.
8. Incluye rollback y pruebas actor × operación para admin nexus-pro, agente nexus-pro, cross-org, anon y service_role donde aplique.
9. Verifica si las RPC financieras de Fase 1 (`seguros_registrar_cobro`, `seguros_reversar_cobro`) seguirían funcionando bajo las nuevas policies al ser `SECURITY INVOKER`. Si requieren permisos de tabla que luego queremos quitar al navegador, señala el conflicto arquitectónico y propón la solución segura SIN convertir automáticamente todo a SECURITY DEFINER.
10. Identifica si necesitamos separar "puede ejecutar RPC" de "puede escribir tabla" mediante funciones internas/privilegios/rol de base o una arquitectura equivalente, y explica el menor cambio seguro compatible con Supabase/PostgREST actual.

### Restricciones

- NO SQL de escritura en producción.
- NO aplicar RLS todavía.
- NO modificar `mi_rol()` ni `mi_organizacion()`.
- NO tocar UI salvo lectura/auditoría de callers.
- NO reparar huérfanos históricos.
- NO abrir Fase 2C.
- NO cambiar versión, workflows ni `wrangler.jsonc`.

Entrega la auditoría/diseño en una entrada NUEVA de `docs/bitacora/`. Si necesitas proponer SQL, colócalo en un archivo NUEVO separado dentro de `docs/bitacora/` y márcalo claramente como `NO APLICAR — PROPUESTA 2B`.

Quiero primero el mapa exacto de permisos y dependencias; después decidiré qué primer bloque pequeño de RLS se autoriza.