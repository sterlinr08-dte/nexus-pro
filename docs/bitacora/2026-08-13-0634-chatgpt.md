# ChatGPT — 2026-08-13 06:34 RD

## Fase 2 → Subfase 2B → Bloque 3C — cierre definitivo de acceso directo a `asientos`

Bloques 3A y 3B quedan cerrados. Abrimos EXCLUSIVAMENTE 3C. No abrir Bloque 4 ni tocar otras tablas.

### Objetivo
Eliminar el acceso directo inseguro a `public.asientos` y dejar la escritura contable únicamente detrás de flujos server-side autorizados, sin romper los consumidores legítimos ya migrados en 3A/3B ni el cron con `service_role`.

### Paso 1 — auditoría final obligatoria, antes de aplicar nada

1. Grep exhaustivo de TODO el repo (`index.html`, `parches.js`, Edge Functions, SQL/migrations, scripts) para encontrar cualquier INSERT/UPDATE/DELETE directo restante sobre `public.asientos` o REST `asientos`.
2. Enumerar todas las funciones/RPC/triggers/cron que escriben en `asientos`, indicando SECURITY INVOKER/DEFINER, owner, grants, rol/organización guard y si dependen de privilegios directos del caller.
3. Verificar el estado REAL de producción: RLS habilitado/forzado, policy `all_asientos`, ACL de tabla para `anon`, `authenticated`, `service_role`, `postgres`, trigger de DELETE, constraints e índices.
4. Confirmar con evidencia que 3A/3B ya no necesitan INSERT/UPDATE directo desde el navegador.
5. Confirmar cómo escriben hoy: cobro/reversa Fase 1, asiento manual/corrección 3A, generación/anulación factura 3B y `crear_factura_auto_tx` del cron.

### Diseño esperado para 3C

La dirección objetivo es:
- eliminar `all_asientos FOR ALL TO public USING(true)`;
- `anon`: cero SELECT/INSERT/UPDATE/DELETE;
- `authenticated` de `nexus-pro`: SELECT solo si existe necesidad funcional real;
- INSERT/UPDATE/DELETE REST directo de `authenticated`: denegado;
- `service_role`/cron: mantener solo lo estrictamente necesario;
- DELETE físico: seguir bloqueado por trigger;
- todas las escrituras de usuario deben pasar por las RPC ya endurecidas.

IMPORTANTE: si alguna RPC actual es SECURITY INVOKER y dejará de poder insertar tras cerrar ACL/RLS, NO aplicar el cierre todavía. Documentar qué RPC debe migrarse a SECURITY DEFINER con guard interno + search_path fijo, o qué alternativa mínima corresponde. No romper una función legítima para ganar una policy bonita.

### Pruebas obligatorias

Usar la metodología `docs/METODOLOGIA-PRUEBAS-DESTRUCTIVAS.md` para cualquier dato sintético. Preferir solo lectura/SET ROLE cuando alcance.

Matriz mínima post-cambio a demostrar:
- admin nexus-pro: SELECT sí; REST INSERT/UPDATE/DELETE no; RPC permitidas sí según cada guard;
- agente nexus-pro: SELECT según necesidad; REST escritura no; asiento manual/corrección no; generar factura sí; anular factura no;
- cross-org authenticated: 0 filas y 0 escrituras;
- anon: 0 filas / 0 escrituras;
- service_role: cron sigue funcionando donde corresponde;
- diagnóstico financiero `ok:true` después.

### Entrega

Primero deja una entrada NUEVA de `docs/bitacora/` con auditoría + SQL exacto propuesto + rollback + matriz de pruebas. NO aplicar producción hasta mi revisión cruzada, salvo que encuentres un riesgo crítico activo que requiera bloqueo inmediato; en ese caso documenta antes y aplica el mínimo reversible.

### Reglas duras

- NO tocar `clientes`, `facturas`, `abonos`, cuentas bancarias, NCF/recibos ni datos históricos.
- NO abrir Bloque 4.
- NO cambios visuales.
- NO borrar asientos históricos.
- NO depender de `tienePermiso()`/localStorage como frontera de seguridad.
- NO cerrar 3C hasta auditoría + implementación + pruebas + verificación producción + bitácora.

Espero auditoría independiente de Claude antes de autorizar el SQL de cierre.