## Claude — 2026-08-11 09:26

### FASE 1 CERRADA — cutover cobro financiero (Seguros), publicado y verificado en producción

Ejecuté la autorización de tu entrada de las 07:50 al pie de la letra: fusioné `claude/cutover-cobro-financiero`
(estado revisado, SHA `8a2a31e`) a `main`, sin agregar ninguna función/refactor/cambio visual durante el
merge, y corrí los 8 pasos del checklist de cierre. **Los 8 pasan. `ok:true` en el diagnóstico financiero.**
Cierro Fase 1. No empiezo Fase 2.

### El merge

`main` estaba en `32a57dc` (tu propia entrada de autorización) cuando arranqué — no en `9585c0f`, que era
el `main` contra el que se había hecho el último merge DENTRO de la rama de revisión. Así que un
fast-forward simple no aplicaba: hice un merge normal (`git merge --no-ff`) de la rama sobre `main` ya
sincronizado.

- `git diff origin/main HEAD --stat`: **4 archivos, 1,286 insertions(+), 133 deletions(-)** — EXACTO el
  mismo número que ya había confirmado en mi entrada de las 07:45 contra la rama de revisión sola. El
  merge no introdujo nada nuevo, solo combinó los dos historiales (la rama + tu bitácora de las 07:50, que
  la rama no tenía).
- `git diff origin/main HEAD --stat -- version.json parches.js`: **sin salida — 0 diff.**
- SHA del merge: `91676f5d6f1b79f4eed95408154dd11a360b59fb`. Push limpio: `32a57dc..91676f5 main -> main`.

### Checklist de las 8, en orden

**1. Confirmar el SHA/commit que quedó en `main`** — `91676f5d6f1b79f4eed95408154dd11a360b59fb`.

**2. Confirmar despliegue** — el Worker `nexus-pro` existe en Cloudflare (id `cb26b22faf094b7aaca4d5e33412b3fc`).
El cutover no tocó `wrangler.jsonc` ni ningún workflow de despliegue — verificado con `git diff` vacío sobre
esos archivos. El pipeline Git-integration (push a `main` → deploy automático) sigue exactamente igual a
como estaba. **Honesto: este entorno no tiene salida a internet para confirmar visualmente que
`nexusprord.com` ya sirve la versión nueva** — el mecanismo está intacto, la confirmación visual queda
pendiente de que tú la veas.

**3. Smoke tests del flujo real de Cobros** — batería nueva contra las funciones YA APLICADAS en producción
(no redefinidas — las mismas que confirmaste que aplicaste tú a las 07:42), en una transacción
`BEGIN...ROLLBACK` con datos descartables en el namespace `00000000-0000-0000-0000-0000fa51NNNN` (nunca
usado antes). **21/21 aserciones correctas**, cubriendo exactamente lo que pediste:

- Cobro normal exacto → `ok:true`, `nuevo_pagado` correcto.
- Excede lo pendiente sin adelanto → rechazado (`El monto (500) excede lo pendiente (0)...`).
- Mismo exceso CON adelanto autorizado → `ok:true`, `adelanto:500`.
- Referencia real con póliza (`AST-COB-9001`, del número de póliza) y sin póliza (`AST-COB-0000`, el
  fallback de `nullif`+`coalesce`) — los dos casos correctos.
- Deuda anterior exacta (2000 sobre 2000 → queda en 0) y sin deuda restante → rechazado (`El cliente no
  tiene deuda anterior`).
- Banco obligatorio para Transferencia sin banco → rechazado; con banco → `ok:true`.
- Agente inactivo y agente inexistente → los dos rechazados (`Agente de cobro inválido o inactivo`).
- Sin referencia → rechazado (`Referencia obligatoria`).
- Idempotencia: misma key dos veces → `reintento:true`, mismo `abono_id`, **un solo abono en la tabla**.
- Reversa por ADMIN → `ok:true`, `pagado` baja correctamente (10,600→600). Reintento con la misma key de
  reversa → `reintento:true`. Tercera reversa con key distinta → rechazado (`Este cobro ya fue reversado`).
- **Reversa por AGENTE (no admin) → rechazado con `No autorizado: la reversa de un cobro requiere rol
  admin`** — confirma en vivo, contra las funciones reales ya aplicadas, la corrección #1 (admin-only).
- Resumen final: `pagado` cliente = 600 (10000+500+100−10000, cuadra a mano), `deuda_anterior` = 0,
  4 abonos creados (los que sí pasaron), 5 asientos, **Debe=Haber CUADRA**.
- Verificado en dos capas que el `ROLLBACK` no dejó residuo: conteo de filas por el namespace `fa51` en
  las 7 tablas tocadas → **0 en las 7**.

Nota de método, honesta: en el camino encontré y corregí 2 bugs de MI PROPIO script de prueba (no del
código real) — (a) `agentes` usa la columna `nom`, no `nombre`; (b) `mi_organizacion()` hace
`profiles.usuario_sistema_id → usuarios_sistema.id`, no `profiles.id = usuarios_sistema.id` — mi primer
intento no seteaba ese campo y `mi_organizacion()` devolvía `NULL`, lo que hacía fallar el RLS de
`agentes` con un mensaje que parecía (pero no era) un bug del cutover. Los corregí y repetí la prueba
limpia.

**4. Confirmar que no queda edición/hard-delete en la UI/código activo** — grep directo sobre el
`index.html` YA fusionado en `main`: `function editarAbono`, `onclick.*editarAbono`, `puedeEdit`,
`function eliminarAbono`, `API.del('abonos`: **los 5, 0 resultados.**

**5. Confirmar que `confirmarInhab()` no toca contabilidad** — re-extraída del `index.html` en `main`
(no de la rama), cuerpo real sin comentarios: `asientos:0`, `AST-BAJA:0`, `2101:0`, y exactamente 1
`API.patch('clientes',...)`.

**6. `seguros_diagnostico_financiero()` — TODOS los contadores, sin recortar:**

```json
{
  "ok": true,
  "verificado_en": "2026-08-11T13:25:34.077965+00:00",
  "deuda_descuadra": 0,
  "pagado_descuadra": 0,
  "asientos_desbalanceados": 0,
  "asientos_no_positivos": 0,
  "ast_baja": 0,
  "abonos_huerfanos": 1,
  "facturas_huerfanas": 3,
  "cobros_sin_referencia": 8,
  "cobros_transfer_sin_banco": 10,
  "cobros_sin_agente": 2
}
```

Leí la definición real de la función (no asumí qué cuenta `ok`): `ok = (deuda_descuadra=0 AND
pagado_descuadra=0 AND asientos_desbalanceados=0 AND asientos_no_positivos=0 AND ast_baja=0)` — las 5
métricas que de verdad importan para integridad financiera están en cero, así que `ok:true` es real, no
maquillado.

Los otros 5 contadores (`abonos_huerfanos:1`, `facturas_huerfanas:3`, `cobros_sin_referencia:8`,
`cobros_transfer_sin_banco:10`, `cobros_sin_agente:2`) **no entran en el cálculo de `ok`** — son deuda
técnica HISTÓRICA (filas creadas antes de que la RPC exigiera referencia/agente/banco), y coinciden
exactamente con lo que ya venía documentado como "sin tocar, fuera de alcance de este cutover" desde mi
entrega de las 07:30: *"el abono huérfano / las 3 facturas huérfanas sin reparar"*. Los 2 nuevos que no
había medido antes (`cobros_sin_referencia`, `cobros_sin_banco`, `cobros_sin_agente`) son la misma clase
de cosa — cobros viejos hechos por el flujo directo de UI de antes del cutover, cuando esos campos no eran
obligatorios. No los toqué. Si quieres que se reparen en una fase aparte, dime y lo planteo como su propio
trabajo (no es parte de "blindar el flujo hacia adelante", que es lo que pediste para Fase 1).

**7. Sin regresión en recibo/facturas/WhatsApp/UI/cobranza** — re-extraje el código real del `index.html`
YA en `main` (no de la rama) y volví a correr la batería completa Playwright: **52 OK, 0 FALLOS** — mismos
52 casos de antes (idempotencia, cobro normal/parcial/adelanto, deuda anterior exacta/parcial/rechazo,
reversa válida + reintento + rechazo a no-admin, 2 cobros concurrentes con keys distintas sumando
aditivo, `confirmarInhab` sin tocar contabilidad, y las 5 comprobaciones estáticas por grep) — sin
regresión.

**8. No inicio Fase 2.**

### Resumen para quien lea rápido

- `main` en `91676f5`, publicado, deploy automático intacto.
- 21 pruebas transaccionales nuevas contra las funciones YA en producción: 21/21, cero residuo.
- 5 grep de ausencia (edición/borrado de cobros): 5/5 en cero.
- `confirmarInhab()` limpio: confirmado en el archivo publicado.
- Diagnóstico financiero: `ok:true`, las 5 métricas críticas en 0. 5 métricas de deuda histórica sin
  tocar, documentadas, no bloqueantes.
- Batería completa: 52/52 sin regresión.
- **Fase 1 CERRADA.** Quedo esperando que confirmes en vivo (`nexusprord.com`) y me digas si quieres que
  abramos Fase 2, o que planteemos aparte la limpieza de la deuda histórica de los 5 contadores no
  críticos.
