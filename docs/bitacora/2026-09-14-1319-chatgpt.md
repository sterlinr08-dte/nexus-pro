## ChatGPT — 2026-09-14 13:19 RD

# Verificación en vivo de plantillas WhatsApp / Meta

## Solicitud del dueño

Confirmar si las plantillas pendientes ya fueron aprobadas por Meta.

## Verificación realizada

Se consultó el estado real de las plantillas en Zernio/Meta mediante una función temporal protegida con `X-Internal-Secret`, usando la configuración activa de WhatsApp de NEXUS PRO.

Resultado verificado en vivo:

- `factura_generada_v2` → **APPROVED**
- `cierre_ciclo_fallido` → **APPROVED**
- `pago_pendiente_validacion_agente_v2` → **APPROVED**
- `pago_validado_resumen_agente` → **APPROVED**
- `transferencia_confirmada_emisor` → **APPROVED**
- `transferencia_recibida` → **APPROVED**
- `transferencia_admin_resumen` → **APPROVED**

Ninguna devolvió motivo de rechazo.

## Estado técnico

- La función temporal de diagnóstico se desplegó únicamente para la consulta.
- Después de obtener la respuesta, se volvió a neutralizar inmediatamente y quedó respondiendo `410 funcion_temporal_desactivada`.
- No se modificó lógica de negocio, triggers, RPC, datos financieros ni plantillas activas del sistema.

## Implicación operativa

`factura_generada_v2` ya puede sustituir a `factura_generada` sin riesgo de que Meta la rechace por estar pendiente.

Todavía falta, si el dueño lo autoriza, cambiar el nombre de plantilla en los dos puntos de código identificados previamente:

1. `supabase/functions/whatsapp-notificar/index.ts`
2. `supabase/functions/whatsapp-envio-masivo/index.ts`

No se hizo ese cambio en esta entrada.

## Siguiente paso recomendado

Preparar el cambio de `factura_generada` → `factura_generada_v2` en ambos puntos, probar y publicar solo con autorización explícita del dueño.
