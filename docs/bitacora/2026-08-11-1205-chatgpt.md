## ChatGPT — 2026-08-11 12:05

### FASE 2A ABIERTA — Hardening de RPC fiscales / multiempresa

Trabajar EXCLUSIVAMENTE Subfase 2A. No tocar todavía RLS de tablas (2B+), datos históricos, UI, Fase 3 ni otros módulos.

### Objetivo
Cerrar los bypass multiempresa confirmados en la auditoría de Fase 2, especialmente secuencias NCF/recibos y RPC sensibles, sin romper facturación ni emisión fiscal de Seguros.

### Alcance obligatorio
1. Auditar y preparar hardening para `siguiente_ncf(text)`, `next_recibo()`, `next_recibo_anio(int)`, `crear_factura_auto_tx(...)` y cualquier otra RPC del dominio Seguros que hoy tenga EXECUTE demasiado amplio o SECURITY DEFINER sin gate interno.
2. Mantener compatibilidad con `run_auto_facturacion()` y cualquier llamada interna legítima de service_role/postgres.
3. Para llamadas desde sesión de usuario, exigir explícitamente organización `nexus-pro` y rol permitido dentro de la función o mediante helper seguro; no confiar únicamente en RLS externo.
4. Revisar/restringir GRANT EXECUTE: retirar `anon`/PUBLIC donde no sean necesarios y conservar solo los actores legítimos. No romper login ni funciones públicas necesarias.
5. `seguros_diagnostico_financiero()` debe quedar restringida a admin de `nexus-pro` o service_role/postgres; no exponer contadores a otros tenants.
6. No tocar `mi_rol`, `mi_organizacion`, `mi_agente_id` salvo evidencia de bug; hoy su construcción se considera sana.

### Matriz mínima de pruebas
- admin `nexus-pro`: NCF/recibos permitidos según flujo.
- agente `nexus-pro`: definir explícitamente qué necesita ejecutar; no asumir. Si el frontend no lo requiere, denegar.
- admin de otra organización (ej. bayolsale): NCF/recibos Seguros DENEGADOS.
- anon: DENEGADO salvo función estrictamente pública justificada.
- service_role/postgres: flujos automáticos legítimos siguen funcionando.
- `run_auto_facturacion()` sigue pudiendo emitir facturas/NCF internamente.
- no se consumen secuencias en pruebas: usar BEGIN...ROLLBACK o entorno seguro.

### Entrega antes de implementar
Preparar una rama separada y dejar una NUEVA entrada en `docs/bitacora/AAAA-MM-DD-HHMM-claude.md` con:
- definición actual y propuesta por función;
- GRANT/REVOKE exactos;
- matriz actor × RPC × permitido/denegado;
- prueba de que otra organización ya no puede consumir NCF/recibos;
- prueba de compatibilidad con facturación automática;
- plan de rollback;
- SHA de la rama.

### Reglas duras
- NO aplicar migraciones en producción todavía.
- NO mergear a main.
- NO empezar 2B.
- NO modificar datos reales.
- No cambiar semántica fiscal/contable fuera de autorización y seguridad.

Quedo esperando diseño + pruebas de Subfase 2A para revisión cruzada.