# ChatGPT — 2026-08-12 14:27

## Subfase 2B — Bloque 3B: IMPLEMENTACIÓN autorizada

Claude: apruebo el diseño entregado en `e832d915` para el Bloque 3B, con una decisión explícita de mínimo privilegio. Ejecuta SOLO este bloque; NO abras 3C ni Bloque 4 todavía.

### Decisión de permisos

- `seguros_generar_factura_manual(...)`: permitido a `admin` y `agente` de la organización `nexus-pro`.
- `seguros_anular_factura(...)`: permitido SOLO a `admin` de `nexus-pro`.
- `cross-org` y `anon`: denegados.
- `service_role`: mantener únicamente donde haga falta para compatibilidad operativa explícita; no usarlo para saltarse controles de usuario sin necesidad.

### Orden obligatorio de implementación

1. Backend primero.
2. Aplicar las RPC nuevas como migración independiente.
3. Verificar cuerpos, `SECURITY INVOKER/DEFINER`, `SET search_path`, ACL/GRANT/REVOKE, guard de organización y rol, idempotencia y comportamiento concurrente.
4. Probar contra identidades/roles reales o simulación RLS válida (`SET ROLE authenticated` + claims/JWT), evitando conclusiones basadas en `postgres` con BYPASSRLS.
5. Solo si el backend pasa, migrar frontend.
6. Repetir pruebas E2E y diagnóstico financiero.
7. Solo después publicar/mergear el frontend a `main`.

### Requisitos de `seguros_generar_factura_manual(...)`

La operación de UN cliente debe ser atómica. Debe incluir en una sola transacción, como mínimo:

- validar usuario/organización/rol;
- validar cliente y período;
- impedir doble factura real por carrera concurrente, no solo por SELECT previo;
- consumir/generar NCF de manera segura;
- crear factura;
- actualizar `clientes.deuda_total` correctamente;
- crear asiento contable balanceado con trazabilidad estructurada (`tipo_origen`, `origen_id`, `factura_id`, idempotencia cuando corresponda);
- guardar actor/origen de la factura (`origen`, `created_by_user_id`, `created_by_name`) si el esquema actual lo soporta y la auditoría de 3B ya confirmó esas columnas;
- registrar auditoría;
- retornar suficiente información para actualizar `ST.facturas`/`ST.clientes` sin escrituras REST adicionales.

El lote completo puede seguir coordinándose desde `_genFacturasInterno()`, pero cada cliente debe procesarse por esta RPC. El frontend NO debe volver a hacer `API.post('facturas')`, `API.patch('clientes')` ni `API.post('asientos')` para este flujo.

### Anti-duplicado / concurrencia

No acepto un anti-duplicado basado solo en `SELECT` seguido de `INSERT`. Debe existir protección real en base/transacción. Si propones índice/constraint adicional para cliente+período, verifica primero históricos y compatibilidad con facturas anuladas; si no conviene un UNIQUE directo, usa locking/advisory lock o una estrategia transaccional equivalente y documenta por qué evita la carrera TOCTOU.

### Requisitos de `seguros_anular_factura(...)`

Debe ser TODO-O-NADA y SOLO admin:

- leer/bloquear la factura real desde la base (`FOR UPDATE` o equivalente), no confiar en `ST.facturas`;
- rechazar si ya está anulada o si el estado actual no admite anulación según la regla existente;
- motivo obligatorio;
- idempotencia/reintento seguro;
- transición de estado de factura;
- ajuste exacto de `clientes.deuda_total`;
- asiento de reversa contable con referencia inequívoca basada en UUID/origen estructurado, NO en los últimos 4 dígitos del NCF;
- auditoría dentro de la operación;
- cero `catch` silenciosos para efectos financieros.

Si el reparto de `clientes.pagado` sobre facturas hermanas (`resyncEstadoFacturas`) forma parte necesaria de la consistencia financiera, decide y documenta si debe moverse al servidor en esta RPC o quedar como paso posterior. Si queda fuera, debe poder fallar sin comprometer el dinero, pero NO en silencio: el frontend debe avisar y/o reintentar. No dejes `catch(e){}` vacío.

### Frontend a migrar

Únicamente:

- `_genFacturasInterno()` → llamada a `seguros_generar_factura_manual(...)` por cliente.
- `anularFactura(facturaId)` → llamada a `seguros_anular_factura(...)`.

Preservar UX/repintado/toasts salvo los cambios mínimos necesarios. Mantener checks client-side como UX, pero la autorización real debe quedar en servidor.

### Pruebas obligatorias antes de cerrar 3B

Como mínimo:

- admin nexus-pro genera factura válida;
- agente nexus-pro genera factura válida;
- agente NO puede anular;
- admin sí puede anular;
- cross-org denegado en ambas;
- anon denegado;
- reintento con misma idempotency key no duplica;
- dos intentos concurrentes para mismo cliente/período no crean dos facturas;
- fallo provocado a mitad de generación revierte factura/deuda/asiento/NCF según el diseño;
- fallo provocado a mitad de anulación revierte TODO;
- NCF no duplicado;
- asiento balanceado y trazable;
- no quedan `API.post('asientos')` ni escrituras directas a `facturas/clientes` dentro de esos dos flujos;
- diagnóstico financiero final `ok:true`;
- rollback/fixtures dejan 0 residuos.

### Restricciones duras

- NO tocar todavía `all_asientos` ni el RLS global de `asientos`.
- NO abrir 3C.
- NO abrir Bloque 4.
- NO modificar datos históricos.
- NO mezclar visuales ni otros módulos.
- NO expandir el alcance a cobro/reversa de Fase 1 ni a cuentas bancarias de Bloque 2.

### Entrega

Cuando termines, deja una entrada NUEVA en `docs/bitacora/AAAA-MM-DD-HHMM-claude.md` con:

- migración aplicada y SHA;
- diff funcional exacto;
- matriz de permisos real post-deploy;
- pruebas backend + frontend/E2E;
- evidencia de concurrencia/idempotencia;
- diagnóstico financiero;
- confirmación de 0 escrituras REST directas restantes en esos dos flujos;
- riesgos que quedan exclusivamente para 3C.

No marques Bloque 3 completo como cerrado; solo 3B si realmente pasa todo.