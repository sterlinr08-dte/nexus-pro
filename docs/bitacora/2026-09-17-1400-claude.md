# 2026-09-17 14:00 — Claude — Pedido del dueño: vista previa del prototipo

## Para ChatGPT

El dueño quiere **ver el prototipo de la sidebar curva** antes de decidir nada más. Pidió
explícitamente que se despliegue una vista previa — no producción.

## Lo que necesito que hagas

Desplegar `chatgpt/sidebar-curva-prototype` (o su estado actual) a algún lugar donde el dueño
pueda verlo con sus propios ojos, **sin tocar `main` ni el dominio de producción
`nexusprord.com`**. Tú conoces la configuración real de Cloudflare de este proyecto mejor que yo
en este momento — algunas opciones típicas, en orden de preferencia si aplican aquí:

1. **Si el proyecto de Cloudflare Workers/Pages ya genera una URL de vista previa automática por
   rama** (algo común en integraciones Git de Cloudflare Pages, tipo
   `<hash-o-rama>.<proyecto>.pages.dev`) — confírmalo y comparte esa URL directamente, sin que
   haga falta ningún deploy manual.
2. Si no hay preview automática por rama, y existe una forma de generar un deploy de vista previa
   sin afectar el dominio de producción (por ejemplo, un ambiente/subdominio de staging ya
   configurado), úsalo.
3. Si ninguna de las dos existe, dilo explícitamente en tu entrada de cierre en vez de inventar
   una — no uses GitHub Actions para publicar (regla ya conocida de `CLAUDE.md`), y no publiques
   a `main` "solo para que se vea" — eso sería producción real, no una vista previa, y el dueño
   todavía no ha aprobado el diseño.

## Qué reportar de vuelta

- La URL de la vista previa (o la razón concreta de por qué no se pudo generar una sin tocar
  producción).
- Confirmación de que la vista previa sigue siendo exactamente el contenido de
  `chatgpt/sidebar-curva-prototype` en el commit `ecc20f2` (o el que sea más reciente si agregaste
  algo), para que lo que el dueño vea sea lo mismo que yo ya revisé.

## Estado

Solo coordinación, ningún archivo tocado. Esperando la URL de vista previa antes de que el dueño
decida.
