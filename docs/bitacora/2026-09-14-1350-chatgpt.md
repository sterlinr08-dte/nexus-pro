## ChatGPT — 2026-09-14 13:50 RD

# `factura_generada_v2` activada en producción + prueba real enviada al dueño

## Solicitud del dueño

El dueño autorizó explícitamente activar la plantilla aprobada `factura_generada_v2` (la versión de factura que incluye las cuentas bancarias) y pidió enviarle un mensaje de prueba a su propio número.

## Cambios realizados

Se cambió la plantilla de factura en los dos puntos reales que ya habían sido auditados:

1. `supabase/functions/whatsapp-notificar/index.ts`
   - evento `factura_generada`
   - antes: `factura_generada`
   - ahora: `factura_generada_v2`

2. `supabase/functions/whatsapp-envio-masivo/index.ts`
   - mapa `PLANTILLAS.factura`
   - antes: `factura_generada`
   - ahora: `factura_generada_v2`

No se cambió el orden de variables. Se conserva:

- `{{1}}` = nombre
- `{{2}}` = monto
- `{{3}}` = período legible

La plantilla aprobada agrega como texto fijo las cuentas bancarias a nombre de Esterlin Espinal.

## GitHub / publicación

Los dos cambios fueron publicados directamente a `main` con autorización explícita del dueño:

- `whatsapp-notificar/index.ts`: commit `efd3a0bdf1510fff65fc2461d7f3da7ef6ef6bc4`
- `whatsapp-envio-masivo/index.ts`: commit `e8d06bbec1c60303eb1085a367eab68ec0277670`

## Supabase producción

Se desplegaron las Edge Functions en `tnwsgcxurfyuszxsewsn`:

- `whatsapp-notificar` → versión 18, ACTIVE, `verify_jwt=false` (mantiene autenticación interna `X-Internal-Secret`)
- `whatsapp-envio-masivo` → versión 7, ACTIVE, `verify_jwt=true`

## Prueba real enviada

Se usó el agente administrador del dueño:

- agente: `ESTERLIN`
- agente_id: `3c975b10-b57b-43d0-9a90-e58a8410cf82`
- número registrado: `8297173291`

Se invocó `whatsapp-notificar` con:

- tipo: `factura_generada`
- monto de prueba: `RD$1,000.00`
- período: `2026-09`

Resultado real de la Edge Function:

- HTTP 200
- `{ "ok": true, "estado": "enviado" }`

Registro en `whatsapp_mensajes`:

- plantilla: `factura_generada_v2`
- variables: `["ESTERLIN", "1,000.00", "septiembre 2026"]`
- estado: `enviado`
- `zernio_message_id`: `wamid.HBgLMTgyOTcxNzMyOTEVAgARGBRDRUZFNkRBODFFQ0Y0QzlBQTQ5NgA=`
- sin error

Esto confirma end-to-end que la nueva plantilla aprobada ya envía correctamente por Zernio/Meta.

## Alcance / seguridad

- No se modificaron facturas reales para hacer la prueba.
- No se creó un cliente ficticio.
- No se tocó lógica financiera, cobros, saldos ni ciclos.
- La prueba se envió al número del propio administrador usando su registro de agente.
- Los cambios de factura normal y envío masivo quedaron alineados con la misma plantilla.

## Nota de herramienta

Durante la operación se creó accidentalmente una entrada de migración no-op de herramienta y se limpió inmediatamente. Se verificó `residual = 0`; no quedó ninguna de esas entradas en `supabase_migrations.schema_migrations` y no hubo cambio de esquema ni de datos de negocio asociado.

## Estado final

`factura_generada_v2` está **activa en producción** para:

- notificación automática al generarse una factura;
- envío masivo de facturas.

La prueba real al número del dueño quedó marcada como `enviado` por Zernio/Meta.