## Claude — 2026-09-10 08:36

# Aviso de cierre fallido — APLICADO en producción

El dueño autorizó publicarlo. Cierra `docs/bitacora/2026-09-10-0755-claude.md` y el hueco señalado en
`0750`: si el cron del día 20 fallaba, nadie se enteraba.

**Aplicado y verificado. Cero incidencias.**

## 1. Orden de aplicación

El mismo orden seguro de la vez anterior, para que nunca existiera una ventana en la que la función
pudiera pedir un tipo de mensaje que la Edge Function todavía no conociera:

| # | Paso | Resultado |
|---|---|---|
| 1 | PR #320 fusionado a `main` | merge `fc81979ab8c350776a76f1674805265a459c2ce2` |
| 2 | Edge Function redesplegada | **versión 7**, `ACTIVE`, `verify_jwt=false` |
| 3 | Migración `seguros_cierre_aviso_fallo` | `success: true` |
| 4 | `asegurar_plantillas` | HTTP 200 — `cierre_ciclo_fallido` **creada**, `PENDING` |

## 2. Verificación en producción

```
la función registra el fallo (CIERRE_CICLO_FALLIDO) = true
la función registra el éxito (CIERRE_CICLO_OK)      = true
automatización activa                               = true  (10 activas, antes 9)
constraint acepta el tipo nuevo                     = true
cron seguros-cierre-ciclo-20 activo, 5 4 20 * *     = true
cierres creados                                     = 0     (correcto: el primero es el 20-sep)
diagnóstico financiero ok                           = true
saldos ESTERLIN / ROBINSON                          = 1,370,310 / 323,190 (sin cambios)
```

### Prueba funcional contra la función ya publicada

No me quedé en verificar que el código estuviera puesto: invoqué la función real, con rollback forzado.
Hoy falla de verdad, porque el ciclo 2026-08 aún no llega a su frontera:

```
no relanzó la excepción = true (devolvió NULL)
registro creado         = true  (auditoría pasó de 0 a 1)
detalle = "El cierre automático del ciclo 2026-08 no se completó: El ciclo 2026-08 cierra el 2026-09-20 04:00"
```

Es exactamente el comportamiento buscado: el fallo deja rastro y no se pierde por rollback. Revertido:
0 registros residuales.

## 3. NOVEDAD — Meta aprobó las 2 plantillas que llevaban 19 horas pendientes

Al someter la plantilla nueva apareció un cambio de estado que nadie había registrado:

| Plantilla | Estado |
|---|---|
| **`pago_pendiente_validacion_agente_v2`** | **APPROVED** ← era PENDING desde ayer |
| **`pago_validado_resumen_agente`** | **APPROVED** ← era PENDING desde ayer |
| `transferencia_confirmada_emisor` | APPROVED |
| `transferencia_recibida` | APPROVED |
| `transferencia_admin_resumen` | APPROVED |
| `cierre_ciclo_fallido` | PENDING (recién creada) |

**Las 5 plantillas anteriores están aprobadas.** Consecuencia operativa concreta: los avisos de pagos
bancarios al agente —los que en la bitácora de las 06:40 se habían perdido 4 veces por no estar
aprobados— **ya salen de verdad**.

Queda solo la nueva en revisión. Mientras tanto, la capa de auditoría cubre: si el cierre falla antes de
que Meta la apruebe, el registro queda igual.

## 4. Un dato que verifiqué antes de darlo por cerrado

Al revisar el estado final aparecieron **18 mensajes de WhatsApp en la última hora**, donde antes había 0.
No los di por buenos sin mirar:

```
recordatorio_atraso · enviado = 12
recordatorio_atraso · error   =  6   ("cliente sin WhatsApp registrado")
todos entre las 12:00:01 y 12:00:15 UTC
```

Es el cron diario de recordatorios de atraso (`whatsapp-detectar-atrasados-diario`, `0 12 * * *`), que
corrió a las 08:00 RD. **Nada que ver con este cambio** — operación normal del sistema. Los 6 errores son
clientes sin teléfono registrado, comportamiento ya conocido.

## 5. Qué pasa ahora el 20 de septiembre

| Escenario | Antes | Ahora |
|---|---|---|
| El cierre sale bien | silencio | `CIERRE_CICLO_OK` en auditoría |
| El cierre falla | **silencio total** | `CIERRE_CICLO_FALLIDO` con el motivo + WhatsApp al admin |
| El cron ni siquiera corre | indistinguible de lo anterior | **se distingue**: no hay ningún registro |

Ese tercer caso es el motivo de registrar también el éxito.

## 6. Rollback

No requiere revert ni migración: la automatización tiene interruptor.

```sql
update public.whatsapp_automatizaciones set activo=false where codigo='cierre_ciclo_fallido';
```

Eso apaga solo el WhatsApp; el registro en auditoría se mantiene, que es lo que conviene. Para revertir
también el registro habría que restaurar la versión anterior de `seguros_cerrar_ciclo_automatico()`
(4 líneas, en la migración `20260910002000`).

---

## Archivos aplicados

- `supabase/migrations/20260910080000_seguros_cierre_aviso_fallo.sql`
- `supabase/functions/whatsapp-pagos-agente-notificar/index.ts` (versión 7 en producción)

## Rama / PR / commit

Rama `claude/review-claude-md-jpzk7k` · PR **#320** · merge `fc81979` en `main`.

## Estado de publicación/deploy

**Publicado y activo.** Cloudflare desplegó `main` por su integración Git habitual.

## Pruebas realizadas

Las 2 de la bitácora de las 07:55 (antes de aplicar) más la invocación real de la función ya publicada,
con rollback forzado, y la verificación de estado y de cero residuo.

## Riesgos / pendientes

- `cierre_ciclo_fallido` en `PENDING` hasta que Meta la apruebe. No bloquea: la auditoría cubre.
- Las 2 observaciones menores de Fase C siguen abiertas (`total_saldo_final` netea deuda contra saldo;
  comentario de `seguros_dia_ciclo_facturacion()`).

## Siguiente paso recomendado

Nada urgente. El 20 de septiembre a las 00:05 corre el primer cierre automático; pase lo que pase,
quedará registrado.
