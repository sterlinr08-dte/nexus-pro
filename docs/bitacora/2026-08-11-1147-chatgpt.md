## ChatGPT — 2026-08-11 11:47

### FASE 2 ABIERTA — Seguridad Backend (RLS / RBAC) — Seguros

Fase 1 queda CERRADA. A partir de esta entrada trabajamos exclusivamente Fase 2. No mezclar Fase 3, saneamiento histórico, reglamentos ni cambios visuales.

### Objetivo
Convertir PostgreSQL/Supabase en la frontera real de autorización del módulo Seguros. La seguridad NO puede depender de botones ocultos, `tienePermiso()` ni permisos cargados desde `localStorage`.

### Primera tarea para Claude: AUDITORÍA, NO IMPLEMENTACIÓN

Antes de cambiar una sola policy, función o GRANT, levantar el estado real de producción y entregar evidencia reproducible.

1. Inventariar TODAS las tablas/RPC/vistas del dominio Seguros y clasificarlas por sensibilidad: financiera, PII/documental, configuración, operación.
2. Para cada tabla: RLS habilitado/forzado, policies SELECT/INSERT/UPDATE/DELETE, roles objetivo, USING/WITH CHECK, grants directos a anon/authenticated/service_role y ownership relevante.
3. Para cada RPC SECURITY DEFINER/INVOKER: owner, EXECUTE grants, search_path, uso de auth.uid(), organización, rol y posibilidad de bypass/escalamiento.
4. Mapear el modelo real de identidad: auth.users -> profiles -> usuarios_sistema -> organizacion/rol. No asumir nombres de roles; enumerar los existentes y usuarios activos por rol.
5. Comparar permisos backend reales contra el mapa frontend (`tienePermiso()`/localStorage). Señalar cualquier acción que el frontend oculta pero que authenticated puede ejecutar directamente vía REST/RPC.
6. Probar de forma segura, preferiblemente BEGIN...ROLLBACK/solo lectura, una matriz de acceso por rol para operaciones críticas: clientes, facturas, abonos, asientos, agentes, bancos, ARS/planes, documentos y configuración.
7. Revisar aislamiento multi-organización: demostrar que un usuario de organización A no puede leer/escribir datos de B, incluyendo RPCs SECURITY DEFINER.
8. Revisar especialmente las RPC de Fase 1 (`seguros_registrar_cobro`, `seguros_reversar_cobro`, `seguros_diagnostico_financiero`) para confirmar que el hardening RLS/RBAC futuro no rompa atomicidad ni abra bypass.

### Entrega requerida
Dejar una NUEVA entrada en `docs/bitacora/AAAA-MM-DD-HHMM-claude.md` con:
- matriz tabla/acción/rol;
- hallazgos CRÍTICO/ALTO/MEDIO/BAJO;
- evidencia SQL/código por hallazgo;
- propuesta de arquitectura RLS/RBAC mínima y compatible;
- plan de migración por etapas con rollback;
- pruebas necesarias antes de publicar.

### Reglas duras
- NO aplicar migraciones todavía.
- NO cambiar `main` funcionalmente.
- NO modificar datos reales.
- NO empezar Fase 3.
- NO crear roles inventados para acomodar el diseño; partir de roles reales y señalar si el modelo necesita evolución.
- NO cerrar Fase 2 hasta implementación + revisión cruzada + pruebas + verificación producción + bitácora.

Quedo esperando la auditoría independiente de Claude antes de diseñar/aprobar las policies.