# 2026-09-18 10:55 — Claude — Verificación en vivo del sidebar azul degradado (rama, no publicada)

## Para ChatGPT

Continúa `2026-09-18-1015-claude.md`. Publiqué la rama `claude/sidebar-azul-degradado` en GitHub
(sin PR abierto, sin merge). Cloudflare Workers Builds generó automáticamente una vista previa en
`https://claude-sidebar-azul-degradado-nexus-pro.sterlinr08.workers.dev`. La probé en vivo contra
esa vista previa (login real, `getComputedStyle`, no solo lectura de código).

## Resultado

- **Escritorio**: fondo, texto, `.ni.on` (pill blanco), `spring-ind`, footer y badge ADMIN
  coinciden exactamente con lo escrito en el CSS. Captura visual confirmada.
- **Colapso (58px)**: confirmado por medición real de layout
  (`getBoundingClientRect().width === 58`, único `nav.sb`/`#sbEl` en el DOM, `offsetParent` no
  nulo → visualmente presente). La primera captura que intenté tomar después del clic resultó ser
  un recorte de la MISMA captura anterior (expandida) — no se generó una imagen nueva esa vez, no
  fue un fallo del CSS. Ya había pasado este mismo malentendido en la verificación de ayer del
  sidebar blanco/vidrio; esta vez lo confirmé con una medición de layout independiente de la
  captura para no repetir la duda.
- **Móvil 390×844**: `border-radius:0 38px 38px 0` correcto, mismo degradado azul, sin overflow
  horizontal (`bodyScrollWidth === innerWidth`).

## Estado

Rama `claude/sidebar-azul-degradado` lista para que el dueño la vea. No se publicó a `main`. Solo
cambia el sidebar (forma + color); nada de ticker, dashboard, KPIs, Supabase ni lógica de negocio.

## Siguiente paso

Esperando que el dueño confirme si quiere publicar a `main` o pedir ajustes de tono/color primero.
