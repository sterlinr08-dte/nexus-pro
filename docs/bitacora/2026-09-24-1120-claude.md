# Fase A — saldo transferible igual a la custodia validada

Referencia: `2026-09-24-1103-claude.md`, hallazgo 1. El dueño autorizó el cambio con «Sí, corrige el saldo transferible».

## Qué cambió

- Migración `supabase/migrations/20260924120000_saldo_transferible_igual_custodia_validada.sql`, **ya aplicada** en Supabase `tnwsgcxurfyuszxsewsn`.
- `transferencias_saldo_disponible_agente(uuid)` ahora devuelve `seguros_acumulado_validado_agente(uuid)`. Hay una sola fórmula y ya no cuenta los abonos bancarios con `validacion_estado='pendiente'`.
- La firma, el nombre, `SECURITY DEFINER` y los permisos quedan igual (EXECUTE solo para `postgres` y `service_role`).
- **No se tocaron datos ni el frontend.**

## A qué afecta (todo en el servidor)

- `transferencias_crear`: bloquea si el saldo es ≤ 0.
- `transferencias_aceptar`: bloquea si el saldo es menor que el monto. Es el control efectivo.
- `seguros_registrar_entrega_admin_manual`: una entrega al admin no puede superar la custodia validada.
- `seguros_anular_entrega_admin` y `seguros_reversar_cobro`: solo registran el saldo antes y después en su auditoría.
- `trg_whatsapp_entrega_confirmada`: el «acumulado» del aviso `entrega_confirmada` ahora muestra la custodia validada.

## Verificación

- Agente vendedor: antes podía transferir RD$346,690 y su custodia era RD$342,190; ahora ambos son **RD$342,190**. La diferencia (RD$4,500) es el depósito del 15-sep que sigue pendiente de validar.
- Admin: sin cambio (RD$1,457,810 en ambos).
- Cuando se valide ese depósito, el monto pasa a ser transferible automáticamente.

## Pendientes observados (no se cambiaron)

- **Pantalla «Dinero en Mano»:** `parches-seguros-base.js` calcula ese saldo en el navegador y todavía incluye los pendientes. Puede mostrar RD$4,500 más de lo que el servidor deja transferir. Corregirlo exige tocar el frontend y publicar.
- **`transferencias_crear`:** no rechaza un monto mayor que el saldo al crear (solo si el saldo es ≤ 0). El rechazo llega al aceptar.
- **`main`:** la migración está en la rama `claude/review-claude-md-jpzk7k`, no en `main`, igual que `20260925010000` y `20260925011000`.

## Reversión

Volver a crear la función con el cuerpo anterior (el mismo SELECT de `seguros_acumulado_validado_agente`, pero sin el filtro `validacion_estado`).
