# Nuevo cliente STUDIO — base Supabase propia (Fase 0/1: registro, dominio, extracción del esquema POS)

**Fecha:** 2026-09-21
**Autor:** Claude
**Rama:** `claude/review-claude-md-jpzk7k` — **no publicado a `main`**

## Solicitud del dueño

Registrar un cliente nuevo, **STUDIO** (venta de motores eléctricos, patinetas eléctricas, aires
acondicionados, televisores, etc.). El dueño eligió explícitamente el modelo **base Supabase propia**
(como Deluxe/Amatista/Geriatra) y un **dominio de marca** para el cliente: **`studiord.net`**
(comprado por el dueño en GoDaddy; la integración GoDaddy solo consulta disponibilidad).

## Diagnóstico / fuente de verdad

- Multiempresa tiene dos modelos coexistentes (`ARQUITECTURA-POS.md`, `CONSULTORIO-CLAUDE.md`):
  (a) base compartida + RLS por `organizacion_id` (Bayolsale es tienda así); (b) proyecto Supabase
  propio por cliente. El POS (`parches-pos.js`) vive en la base madre sobre 51 tablas `pos_*`.
- Para (b) con el POS hay que clonar el esquema a la base nueva y hacer que el mismo `index.html`
  apunte a esa base cuando se sirve en el dominio del cliente. La app ya soporta `API.url/API.key`
  por base y el directorio `organizaciones` (login `usuario@studio` → `dominio`).
- Orgs existentes en la madre: nexus-pro, bayolsale, bayolcell, deluxe, bayolcell-rifa, amatista, geriatra.

## Hecho en esta entrega

1. **Base madre** (`tnwsgcxurfyuszxsewsn`): fila en `organizaciones`
   `slug='studio'`, `nombre='STUDIO'`, `tipo='externa'`, `dominio='studiord.net'`, **`activo=false`**
   (se activa al terminar), `id=e404d1c4-24c5-4e17-88f6-84bef09d6d19`.
2. **Frontend** (`index.html:4335`): mapa `NX_BASES_POR_HOST` → elige la base Supabase por `hostname`.
   Entrada de STUDIO **vacía** hasta tener el proyecto; cualquier host no mapeado usa la base madre
   exactamente como hoy. Commit `63d3f7b`.
3. **Extracción del esquema POS** a `supabase/studio/` (solo lectura sobre la madre, nada aplicado):
   65 tablas, 61 FKs, 78 índices, 3 secuencias, 68 funciones, 72 triggers, 79 políticas, 2 buckets.
   Verificado por md5 contra `pg_get_functiondef()`/DDL de la madre. Ver `supabase/studio/README.md`.
4. Ajustes para STUDIO: uuid de org hardcodeado en 4 funciones contables → id de la org studio;
   4 políticas con slug `nexus-pro` → `studio`; `10_ajustes_studio.sql` crea `operacion_excepciones`.

## Bloqueos / pendientes del dueño

- **Proyecto Supabase STUDIO**: `create_project` vía MCP expiró 3 veces sin crear nada. El dueño lo crea
  en el panel (org `sterlinr08`, us-east-1, nombre `STUDIO`). ~US$10/mes, aceptado por el dueño.
- **Dominio**: comprar `studiord.net` en GoDaddy → agregarlo a Cloudflare → Workers → `nexus-pro` →
  Domains & Routes (`studiord.net`, `www.studiord.net`).

## Siguiente paso

Con el proyecto activo: aplicar `01`→`10` con `apply_migration`; sembrar org (id fijo), admin
provisional (`admin@studio`, clave temporal entregada por chat), `pos_config`, `pos_acceso`,
`pos_cuentas` (incl. 4103), almacén y caja; rellenar `NX_BASES_POR_HOST`; `activo=true` en la madre;
QA de login y venta de prueba en `studiord.net`. **No publicar a `main` sin autorización.**

## Riesgos

- Ninguna base modificada salvo la fila `organizaciones` (inactiva) en la madre.
- El mapa por host con entrada vacía no altera el comportamiento de `nexusprord.com`.
