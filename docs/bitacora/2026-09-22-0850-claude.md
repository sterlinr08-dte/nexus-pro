# Publicación 58.84 a `main` — Compras v2, línea gráfica única y Financiamiento v2 (STUDIO)

**Fecha:** 2026-09-22
**Autor:** Claude
**Autorización del dueño:** "Publica todo" (22-sep-2026)
**Rama:** `claude/review-claude-md-jpzk7k` → `main` (fast-forward)

## Qué se publica

| Entrega | Bitácora | Archivos |
|---|---|---|
| STUDIO solo en `studiord.net` (sin subdominio) | `2026-09-21-2330` | `index.html` |
| Compras v2: ITBIS, costo desembarcado, CxP por factura | `2026-09-22-0130` | `parches-pos.js`, `supabase/studio/13_compras_v2.sql` |
| Línea gráfica única del POS (DESIGN.md §7b) | `2026-09-22-0230` | `parches-pos-stitch-visual.js`, `DESIGN.md` |
| Financiamiento v2 — migración (ya aplicada en STUDIO) | `2026-09-22-0720` | `supabase/studio/14_financiamiento_v2.sql` |
| Financiamiento v2 — frontend bajo bandera | `2026-09-22-0830` | `parches-pos.js` |
| Cloudflare / dominio / bitácoras de coordinación | `2026-09-22-0330 … 0610` | `docs/bitacora/` |

## Versión

- `APP_VERSION` 58.83 → **58.84** en `index.html` (los parches se cargan con `?v=APP_VERSION`, así Safari y la
  CDN piden `parches-pos.js` y `parches-pos-stitch-visual.js` frescos).
- `version.json` → 58.84 con notas del cambio.
- El build `b=` del loader de Seguros (`parches-seguros.js`, 5952) **no** se toca: no cambió ninguna capa de
  Seguros/WhatsApp en esta entrega.

## Efecto por dominio

- `https://studiord.net`: POS con Compras v2, Financiamiento v2 y línea gráfica (banderas `compras_v2` y
  `financiamiento_v2` activas en la base STUDIO).
- `https://nexusprord.com`: Seguros sin cambios funcionales. El POS de Bayolsale recibe solo la línea gráfica
  (CSS) — las banderas no existen en la base madre, así que Compras y Cuotas siguen con la lógica anterior.

## Verificación previa

`node --check` en `parches-pos.js` y `parches-pos-stitch-visual.js`; `version.json` válido; `main` no había
avanzado desde `a92fdc6` (la rama se integra por fast-forward, sin conflictos).

## Después de publicar

1. Dueño en `studiord.net`: recargar (o cerrar y abrir Safari) para tomar 58.84; probar Compras → Nueva compra
   con importación y Cuentas por pagar; Cuotas → Planes → Nueva solicitud → Aprobar → Cobrar cuota.
2. Si algo visual falla en Bayolsale por la línea gráfica, el rollback es quitar la capa `#nxLineaUnicaCSS`
   de `parches-pos-stitch-visual.js` (cambio aislado y reversible).
3. Pendientes de ChatGPT (firma del cliente, WhatsApp, reportes) siguen según `2026-09-22-0610`.
