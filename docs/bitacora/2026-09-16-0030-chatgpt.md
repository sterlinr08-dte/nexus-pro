## ChatGPT — 2026-09-16 00:30 RD

### Autorización

El dueño respondió “Hazlo” a la entrega de seguridad `nexus-smart`, autorizando desplegar la Edge
Function y publicar el pequeño cambio de frontend preparado en
`chatgpt/security-nexus-smart-source`. La autorización se limita a este bloque; no incluye todavía el
Centro de excepciones.

### Preparación

- Se actualizó la rama sobre `origin/main` porque Novedades había avanzado tres commits.
- Rebase limpio, sin conflictos.
- Se repitieron las verificaciones después del rebase:
  - `node scripts/test-nexus-smart-security.mjs` → OK;
  - `node --check parches-seguros-base.js` → OK;
  - parse/bundle TypeScript con esbuild → OK;
  - `git diff --check origin/main...HEAD` → limpio.

### Despliegue Supabase

Se desplegó `supabase/functions/nexus-smart/index.ts` en el proyecto producción
`tnwsgcxurfyuszxsewsn`.

- función: `nexus-smart`;
- versión anterior: 8;
- versión publicada: **9**;
- estado devuelto: `ACTIVE`;
- `verify_jwt:false` conservado de forma intencional: la función distingue dentro del código una
  sesión humana real de la clave pública `anon`, verifica admin activo y organización `nexus-pro`.

Pruebas posteriores contra la versión 9 activa:

- POST sin credencial → HTTP 401;
- POST usando la clave pública `anon` como Bearer → HTTP 401 con mensaje público genérico;
- GET → HTTP 405.

No se ejecutó todavía el camino feliz con una sesión real de administrador porque esta sesión de
ChatGPT no dispone de las credenciales personales del dueño. Esa comprobación debe hacerse desde la
aplicación después del despliegue del frontend.

### Frontend

El cambio en `parches-seguros-base.js` impide invocar Nexus Smart cuando falta `api.token` o cuando
el supuesto token es la misma clave pública. Se publicará a `main` junto con la fuente recuperada, la
prueba de regresión, la política actualizada y ambas entradas de bitácora.

### Pendientes externos

- Rotar la clave histórica de Anthropic si el dueño aún no lo hizo después de su exposición previa.
- Activar Leaked Password Protection en Supabase Auth Dashboard.
- Confirmar una pregunta real desde Nexus Smart con sesión admin después de que Cloudflare publique
  `main`.
