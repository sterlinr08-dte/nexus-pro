# 2026-09-17 13:18 — Claude — Coordinación previa: modificación del Dashboard con ChatGPT

## Para ChatGPT

El dueño quiere modificar el **Dashboard** y pidió que trabajemos esto juntos. Antes de tocar
código, lee esta entrada — el dueño quiere que Claude audite el trabajo antes y después de
implementar, no solo al final.

## Flujo pedido por el dueño

1. **Antes de escribir código**: deja una entrada nueva `docs/bitacora/AAAA-MM-DD-HHMM-chatgpt.md`
   con la propuesta/plan — qué cambia visualmente, por qué, qué archivos vas a tocar, y
   idealmente una descripción o capturas de antes/después. Claude la revisa antes de que exista
   el código, para detectar un problema de diseño o de riesgo temprano.
2. **Implementa** el cambio.
3. **Al terminar**: deja otra entrada de cierre — archivos modificados, verificación hecha,
   rama/commit, y si quedó publicado o no. Nunca edites ni borres una entrada anterior (regla ya
   conocida de `CLAUDE.md`); si hay que corregir algo, entrada nueva que referencia a la anterior.

## Contexto que conviene tener presente para ESTE módulo específico

- **El Dashboard ya tuvo un rollback por un rediseño visual mal calibrado**: PR #311, causaba
  FOUC en iPhone. El dueño confirmó que el sistema volvió al estado anterior. Cambios visuales
  amplios en esta pantalla puntual necesitan más cuidado que en otras — probar en un ancho real
  de iPhone (390–430px) además de escritorio, y evitar parpadeos/saltos al cargar.
- **El sistema de iconos del Dashboard se acaba de auditar y consolidar** (ayer, 16-sep): ver
  `docs/bitacora/2026-09-16-1130-claude.md`, `-1230-`, `-1330-` y `-1400-claude.md`. Vive en
  `parches-seguros-base.js`: una escala de tamaño/color semántico por significado del icono
  (dinero=verde, personas=morado, alerta=naranja, etc.), aplicada a `.kpi/.qa/.sm/.nc`, más una
  capa de fallback por hash de color para lo no clasificado (`SKIP_CTX`, recién corregida para
  incluir `.sm`/`.nc`). Si el cambio de Dashboard toca iconos, tamaños o colores de `.kpi`/`.qa`,
  revisa esas entradas primero — para no reintroducir la inconsistencia que se acaba de arreglar
  ni crear una sexta capa compitiendo con las que ya quedaron consolidadas.
- **No publicar a `main` sin autorización explícita del dueño** — regla ya conocida, se repite
  aquí porque el Dashboard es la pantalla de más impacto si algo sale mal.
- Si el cambio toca cifras/KPIs con datos de dinero (cobrado, pendiente, acumulados), auditar la
  fuente de verdad antes de mostrar un número nuevo — mismo criterio que el resto del proyecto.

## Estado

Solo coordinación, ningún archivo tocado. Pendiente de que ChatGPT deje su entrada de plan antes
de implementar.
