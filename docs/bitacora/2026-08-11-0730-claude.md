## Claude — 2026-08-11 07:30

Rama `claude/cutover-cobro-financiero`, SHA `2875b33` (push hecho: `f60dea9..2875b33`), 3 archivos
tocados. **Nada de esto está en `main` todavía** — sigue en revisión, como pidió el dueño. Respondo
los 6 puntos de tu entrada de las 06:18 en orden.

### 1. Idempotencia — aceptado, sin crear índice, con el lock agregado

Reconfirmado, independiente de tu verificación:

```sql
SELECT indexname, indexdef FROM pg_indexes
WHERE tablename='abonos' AND indexdef ILIKE '%unique%';
```
→ `abonos_pkey`, `abonos_idempotency_key_uq`, `abonos_reversa_idempotency_key_uq`. Los dos UNIQUE
parciales SÍ existen. Mi hallazgo anterior estaba mal por revisar solo `pg_constraint` (no lista un
`CREATE UNIQUE INDEX` hecho sin pasar por `ADD CONSTRAINT`). `HALLAZGO_ADICIONAL_idempotency_key_sin_unique.sql`
quedó con un header "RESUELTO" explicando esto, y una línea explícita: **no crear el índice que
proponía más abajo el propio archivo — quedaría redundante.**

El lock que pediste sí se agregó a `seguros_registrar_cobro`, en `CUTOVER_COBRO_correcciones_rpc.sql`
(v2, corrección #4), justo después de validar que la key no sea NULL/vacía y ANTES del SELECT
idempotente:

```sql
PERFORM pg_advisory_xact_lock(hashtextextended(p_idempotency_key, 0));
```

Verificado en vivo (antes de escribirlo) que `hashtextextended(text,int)` existe en este proyecto
(PostgreSQL 17.6.1.127) y devuelve `bigint` — sin necesitar cast. El UNIQUE se queda como segunda
barrera, sin tocarlo.

### 2. Rumbo del frontend (`fd424cd`) — sin cambios en esta entrega

No tocaste nada de eso, así que no hay nada que reportar aquí — sigue como lo aprobaste
conceptualmente.

### 3. `editarAbono()` — eliminado por completo, no solo de la UI

`index.html`, diff exacto (`git show 2875b33 -- index.html` en la rama):

- La función completa se borró — donde estaba, ahora hay un comentario explicando la decisión
  (reversa + cobro nuevo reemplaza editar).
- Los 3 sitios que la disparaban (`aplicarFiltrosPagos`, `abrirHistorialCobros`, `verHistorialAbonos`)
  perdieron su botón "Editar" Y el `const puedeEdit=tienePermiso('payments_edit')` que ya no se usa
  para nada en esos 3 lugares.
- Evidencia por grep, sobre el archivo real (no la rama, el archivo ya modificado):

```
$ grep -n "function editarAbono" index.html      → 0 resultados
$ grep -n 'onclick.*editarAbono' index.html       → 0 resultados
$ grep -n "puedeEdit" index.html                  → 0 resultados
$ grep -n "function eliminarAbono" index.html     → 0 resultados
$ grep -n "API\.del(.abonos" index.html           → 0 resultados
```

`payments_edit`/`payments_delete` se dejaron como declaraciones de permiso muertas en el listado de
roles (mismo criterio que ya se había aplicado antes con `payments_delete` en un cutover previo) — no
se tocó ese listado general para no ampliar el alcance.

### 4. Las 3 correcciones SQL — incorporadas al mismo archivo, junto con el lock

`CUTOVER_COBRO_correcciones_rpc.sql` pasó a v2: los 3 puntos que ya habías aprobado (reversa
admin-only, `AST-COB-0000` con `nullif`, `search_path` en los 2 triggers) siguen con el mismo cuerpo
exacto — solo se renumeraron — y se les sumó la corrección #4 (el lock). El archivo trae ahora la
explicación completa de por qué el lock hace falta pese al UNIQUE ya existente, y qué garantiza. Sigue
sin aplicarse a producción — es un archivo de preparación, como el dueño pidió.

### 5. `confirmarInhab()` — limpiado, ya no intenta tocar contabilidad

Diff exacto:

```diff
-    if(pend(c)>0){const asi={fecha:hoy(),referencia:'AST-BAJA',...,cuenta_dr_cod:'2101',...};
-      const r=await API.post('asientos',asi);if(r&&r[0]){ST.asientos.unshift(r[0]);recalcularSaldosContables();}}
     c.activo=false;c.estado_cliente='SUSPENDIDO';logAudit('CLIENTE_INHABILITADO',...);
```

Se borró la línea completa que intentaba postear `AST-BAJA` con cuenta `2101`. Lo único que hace
ahora es `activo`/`estado_cliente`/`motivo_inhab`/`nota_inhab` — nada de contabilidad. Evidencia por
grep sobre el CUERPO REAL de la función (sin el comentario explicativo, que sí menciona `AST-BAJA`/
`2101` a propósito, para dejar constancia de la historia):

```
$ node -e "misma lógica: extraer confirmarInhab(), quitar líneas // ..., grep sobre lo que queda"
→ 'asientos' en código real: 0 · 'AST-BAJA': 0 · '2101': 0 · API.patch('clientes',...): SÍ (1)
```

No se reintrodujo `2101 Cuentas por pagar` en ningún lado del flujo de inhabilitación.

### 6. Pruebas — batería completa re-corrida, más la prueba transaccional contra producción

**a) Batería JS/Playwright actualizada** (`extract.js` + `run-tests.js`, ya no extraen `editarAbono` —
extraerla ahora sería un error esperado, así que se sacó de la lista — y se agregaron `abrirInhab`/
`confirmarInhab`): **52/52 OK, 0 fallos.**

- 10 comprobaciones estáticas nuevas por grep (las 5 de arriba + 5 sobre el cuerpo real de
  `confirmarInhab` sin comentarios).
- 34 de la ronda anterior, sin regresión (cobro normal exacto/parcial/adelanto autorizado, deuda
  anterior exacta/parcial/rechazo por exceso, idempotencia con la misma key 2 veces en la misma
  sesión, reversa válida + segunda reversa rechazada, agente no puede reversar, 2 cobros con keys
  DISTINTAS al mismo cliente sumando de forma aditiva).
- 8 pruebas nuevas de `confirmarInhab`: inhabilita de verdad (`activo=false`, `estado_cliente=
  'SUSPENDIDO'`), **`asientos.length` no cambia** (cero asiento nuevo), queda en auditoría, motivo
  "Otro" sin nota se rechaza en el frontend sin llamar nada.

**b) `node --check` (equivalente):** los 4 bloques `<script>` inline de `index.html` (1,423 / 1,205 /
528,088 / 681 caracteres — el de 528k es el núcleo) pasan `new Function()` sin error. `parches.js` no
se tocó en esta entrega.

**c) Los 4 `CREATE OR REPLACE` de la v2 (con el lock), probados VERBATIM contra el proyecto real —
no un mock, no una reconstrucción.** Intenté primero un branch de Supabase
(`cutover-cobro-lock-test`) para probar ahí sin ningún riesgo — **falló con `MIGRATIONS_FAILED`**
(muy probablemente drift del historial de migraciones en este proyecto, largo y muy personalizado —
no algo que se arregle dentro de este cutover). Borré el branch al instante para cortar el cobro por
hora en cuanto confirmé el fallo.

Pivoteé a una transacción `BEGIN; ...; ROLLBACK;` contra el proyecto real (`tnwsgcxurfyuszxsewsn`):
los 4 `CREATE OR REPLACE FUNCTION` (mismo cuerpo exacto del archivo v2) + datos descartables en un
namespace UUID nunca usado antes (`0000cb02...`) + 15 escenarios (T1–T15, 20 filas de aserción) — las
20 dieron `ok:true`:

- T1 cobro exacto · T2 exceso sin adelanto rechazado (`El monto (500) excede lo pendiente (0)...`) ·
  T3 mismo monto con adelanto autorizado · T4/T4b `AST-COB-0000` real sin póliza · T5 deuda anterior
  exacta · T6 deuda anterior sobrepago rechazado (`El cliente no tiene deuda anterior`) · T7 deuda
  anterior parcial · T8a/b/c misma key 2 veces → `reintento:true`, un solo abono en la tabla · T9a/b/c
  2 keys distintas al mismo cliente → `pagado=2500` exacto (1000+1500, sin lost update) · T10 reversa
  válida · T11 segunda reversa, misma key de reversa → `reintento:true` · T12 tercera reversa, key
  distinta → rechazada (`Este cobro ya fue reversado`) · T13 agente intenta reversar → rechazado
  (`No autorizado: la reversa de un cobro requiere rol admin`) · T14 trigger `AST-BAJA` sigue
  bloqueando tras el fix de `search_path` (`id_insertado:null`) · T15 trigger de DELETE sigue activo.

Después del `ROLLBACK`, verificado en DOS capas que producción quedó intacta: (1) `md5(pg_get_functiondef(...))`
de las 4 funciones idéntico al hash tomado ANTES de la prueba; (2) `0` filas del namespace `cb02`/
`rbtest` en `auth.users`/`usuarios_sistema`/`profiles`/`agentes`/`clientes`/`abonos`/`asientos`.

**Concurrencia GENUINA (2 sesiones reales al mismo tiempo) — sigue sin poderse probar desde este
entorno, confirmado empíricamente, no solo asumido.** Hice un experimento aparte: 2 llamadas a
`pg_advisory_xact_lock` con `pg_sleep`, cronometradas con `clock_timestamp()`, disparadas como 2
`tool_use` en el MISMO mensaje. Resultado: la sesión B no arrancó hasta que la sesión A —incluido su
`pg_sleep(6)`— había terminado por completo. Las 2 llamadas se despachan EN SERIE, no en paralelo, por
la infraestructura de esta sesión — no es específico de la RPC ni del branch, es el transporte de
herramientas en sí. El comportamiento del lock bajo concurrencia real se respalda en la semántica ya
documentada de Postgres (`pg_advisory_xact_lock` bloquea hasta COMMIT/ROLLBACK de quien lo tiene), no
en una prueba empírica de 2 sesiones simultáneas hecha aquí. El script manual de 2 pestañas
(`CUTOVER_COBRO_concurrencia_manual.sql`, ya en el repo) sigue siendo el único camino real para esa
prueba — necesita una persona, o un entorno con 2 conexiones de verdad.

### Lo que sigue sin tocar (mismas restricciones de siempre)

FK NOT VALID sin validar · el abono huérfano / las 3 facturas huérfanas sin reparar · RLS de las 14
tablas sin cambiar · NCF histórico sin tocar · nada versionado, nada publicado a `main`.

Queda en tu turno: si esto cierra los 6 puntos a tu satisfacción, decides con el dueño si ya se aplica
el SQL v2 a producción (fuera de esta sesión, con las herramientas que corresponda) y se fusiona
`claude/cutover-cobro-financiero` a `main`.
