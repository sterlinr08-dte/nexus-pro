## ChatGPT — 2026-09-10 00:12

# Fase B — cierre operativo: notificaciones de transferencias activas y plantillas sometidas a Meta

Continúa `2026-09-09-2359-chatgpt.md` y `2026-09-10-0008-chatgpt.md`.

## Hotfix publicado

El PR #316 corrigió el rechazo inicial de Meta/Zernio agregando texto fijo después de la última variable `{{4}}` en ambas plantillas.

- PR: `#316 Hotfix Fase B — corregir texto de plantillas de transferencias`
- Merge a `main`: `2aa03d1023762b996ad966f17659b95ff5af09a0`
- Solo cambió el texto de las dos plantillas y esta documentación; no cambió la matemática, el trigger ni datos financieros.

## Edge Function

`whatsapp-pagos-agente-notificar` fue desplegada nuevamente en Supabase producción:

- status: `ACTIVE`
- versión: `5`
- `verify_jwt=false`, igual que antes, porque usa autenticación interna `X-Internal-Secret`.

## Plantillas Meta/Zernio

Se ejecutó nuevamente `asegurar_plantillas` por el canal server-side con el secreto del Vault.

Resultado HTTP 200:

- `pago_pendiente_validacion_agente_v2` → `PENDING` (ya existía)
- `pago_validado_resumen_agente` → `PENDING` (ya existía)
- `transferencia_confirmada_emisor` → **creada correctamente, `PENDING`**
- `transferencia_recibida` → **creada correctamente, `PENDING`**

Por tanto, las dos plantillas nuevas ya fueron sometidas correctamente a Meta. Mientras estén `PENDING`, una transferencia aceptada conserva su efecto financiero normalmente; si el aviso intenta salir, la Edge Function registra que la plantilla todavía no está aprobada y no revierte la transferencia.

## Verificación final de producción

- Automatización `transferencia_confirmada_emisor`: activa.
- Automatización `transferencia_recibida`: activa.
- Trigger `trg_whatsapp_transferencia_aceptada`: existe y está activo.
- `seguros_diagnostico_financiero().ok = true`.
- Transferencias reales permanecen exactamente en 25: 24 `aceptada`, 1 `rechazada`, 0 `pendiente`.
- **No se creó ni aceptó ninguna transferencia real para probar WhatsApp.**

## Estado Fase B

La integración técnica está **activa en producción**. El único bloqueo externo es la aprobación de Meta para las dos nuevas plantillas.

Cuando Meta apruebe:

1. emisor recibirá confirmación + receptor + acumulado actualizado;
2. receptor recibirá origen + monto + acumulado actualizado;
3. si el receptor es ESTERLIN/admin, ese mismo mensaje funciona como aviso administrativo sin duplicación;
4. la transferencia interna seguirá sin inflar el total cobrado del negocio.

## Siguiente fase

Fase C: cierre/reportes por ciclo y consolidado mensual.

Antes de congelar cierres hay que fijar dos reglas:

1. pagos bancarios: si el ciclo se determina por `validado_at` (regla actual) o por la fecha original del pago;
2. agentes desactivados: deben seguir apareciendo en cierres históricos si tuvieron movimientos o custodia durante el ciclo.
