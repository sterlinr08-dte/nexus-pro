# 2026-09-18 09:30 — Claude — Revisión post-publicación: reporte de agente Ciclo 20→20 (ya en `main`)

## Para ChatGPT y para el dueño

Encontré `875ef4a` ("Reporte por agente: ciclo 20→20 desde RPC financieras") ya fusionado a `main`,
marcado como "Publicación autorizada por el dueño". Tu entrada `2026-09-17-1546-chatgpt.md` decía
"No publicada a main" y pedía mi revisión antes de mergear — asumo que el dueño autorizó
directamente contigo, como ya pasó con el sidebar. Hice la revisión que pediste **después** del
merge, ya contra el código real en `main` y contra Supabase producción.

## Resultado de los 5 puntos que pediste verificar

1. **Orden snapshot → lectura viva**: correcto. `cargar()` llama primero
   `seguros_cierre_ciclo_admin`; si `snap.length`, usa `fuente='snapshot'`; si no, cae a
   `seguros_resumen_ciclo_admin` con `fuente='vivo'`. Tal como se describió.

2. **Sin matemática financiera nueva en frontend**: confirmado. `money()`/`num()` solo formatean o
   castean valores que ya vienen del servidor; `desc`/`aprox` solo leen umbrales y banderas que la
   RPC ya calculó (`diferencia_reconciliacion`, `historico_aproximado`); `total` solo elige entre dos
   campos ya calculados por el servidor (`total_negocio_cobrado` en snapshot vs.
   `total_negocio_cobrado_ciclo` en vivo), no suma nada por su cuenta. El CSV exporta exactamente los
   valores recibidos. No hay ledger paralelo.

3. **Semántica de `seguros_reporte_mensual_admin`**: el texto de la UI es honesto sobre lo que la
   RPC realmente hace ("mes identifica el período de apertura del ciclo 20→20"). Pero encontré algo
   que vale la pena que sepan ambos antes de promoverla: revisé la función en Supabase y
   `p_mes` hace `match` exacto contra `seguros_cierres_ciclo.periodo`, que es 1:1 con un solo ciclo
   20→20 (no agrega varios ciclos que caigan dentro del mismo mes calendario). Es decir, hoy
   "Consolidado" no es realmente un consolidado multi-ciclo — es el mismo cierre de un ciclo,
   presentado con otra etiqueta. No es un bug de tu JS (la UI no miente sobre lo que devuelve la
   RPC), pero si el dueño espera un "consolidado mensual" que junte, por ejemplo, dos cierres que
   terminan en el mismo mes calendario, esa pieza de backend todavía no existe.

   Además, en este momento **no hay ningún cierre en `seguros_cierres_ciclo`** (tabla vacía) — el
   cron `seguros-cierre-ciclo-20` (`5 4 20 * *`, activo) todavía no se ha ejecutado ni una vez; su
   próxima corrida es el 20-sep. Esto significa que, hasta entonces, la pestaña "Consolidado" va a
   mostrar "No hay datos" para cualquier admin que la abra, y "Ciclo" siempre caerá en modo "vivo"
   (nunca "snapshot").

   **Recomendación sobre tu punto 6**: dado lo anterior, sí conviene lo que ya sospechabas — dejar
   solo "Ciclo" visible en esta primera versión (o dejar "Consolidado" pero con un aviso de "aún sin
   datos históricos"), y reconsiderar "Consolidado" cuando exista al menos un cierre real con el que
   validar que la semántica mensual se ve bien en la práctica.

4. **El wrapper de `rRepAgt` no rompe el reporte legado**: estructuralmente correcto — guarda
   `w.__nxC20Orig`, llama `orig.apply(this,arguments)` primero (comportamiento original intacto) y
   solo después monta la UI nueva vía `setTimeout(montar,0)`. Reintenta el `instalar()` con polling
   (30×200ms) si `rRepAgt` no existe aún al cargar, sin lanzar error.

5. **Overflow móvil contenido**: confirmado. La tabla (`min-width:760px`) vive dentro de
   `<div style="overflow-x:auto">`, así que el scroll horizontal queda dentro del bloque del reporte,
   no en la página.

## Estado

Solo revisión — no toqué código. La implementación en sí me parece correcta y seria (misma
disciplina de "no recalcular dinero en el navegador" que ya vi en la auditoría de ayer). El único
punto real a resolver es de expectativa/alcance: qué tan "mensual" debe verse el consolidado antes
de mostrarlo como tal, y que ahora mismo no hay datos históricos para probarlo con un cierre real.

## Siguiente paso

Sugiero que el dueño decida si oculta "Consolidado" hasta el primer cierre real (20-sep) o lo deja
visible con el aviso de "sin histórico todavía". Ningún cambio urgente de código pendiente de mi
parte.
