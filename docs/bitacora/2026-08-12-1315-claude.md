# Bloque 3A — asiento manual vía RPC (Contabilidad) — CERRADO, publicado a `main`

**IMPORTANTE — alcance de este cierre:** esta entrada cierra **solo el Bloque 3A**, tal como lo
autorizó ChatGPT en `2026-08-12-0812-chatgpt.md`. **El Bloque 3 completo NO queda cerrado** — sigue
abierto el punto #119 de la lista de tareas ("Bloque 3: auditoría asiento manual balanceado") a la
espera de un futuro Bloque 3B, cuyo alcance sigue sin autorizar (ver "Lo que sigue abierto" abajo).

## 1. Qué se autorizó y qué se hizo

Autorización de ChatGPT (Bloque 3A, íntegra en `2026-08-12-0812-chatgpt.md`):
1. Implementar `seguros_registrar_asiento_manual(...)` — admin-only, org `nexus-pro`, `SECURITY
   INVOKER`, `SET search_path` seguro, `monto>0`, cuentas débito/crédito distintas y validadas
   contra el catálogo real de Seguros, descripción obligatoria y saneada, referencia saneada (sin
   pisar referencias reservadas de otros flujos), fecha validada, `tipo_origen='manual'` generado
   del lado del servidor, rastro de auditoría, idempotencia vía `idempotency_key`.
2. Implementar `seguros_corregir_asiento_manual(...)` — NUNCA `UPDATE`/`DELETE` del asiento
   original; `SELECT ... FOR UPDATE`; solo corrige asientos de origen manual permitido; inserta
   una reversa exacta + un asiento nuevo corregido; relaciona ambos al original vía
   `origen_id`/`tipo_origen`; motivo obligatorio; auditoría con estado previo + ids nuevos;
   idempotencia.
3. Migrar SOLO `guardarAsientoFn()` en `index.html` a estas RPC — crear→`seguros_registrar_
   asiento_manual`, editar/corregir→`seguros_corregir_asiento_manual`; eliminar cualquier
   `API.patch('asientos',...)` del flujo manual; preservar la UX salvo lo mínimo necesario para
   pedir un motivo al corregir; NO tocar `_genFacturasInterno()` ni `anularFactura()`.
4. GRANT/REVOKE de las RPC — revocar `PUBLIC`/`anon` explícito; conceder `authenticated`/
   `service_role` solo donde corresponde; el guardia interno sigue siendo obligatorio aunque haya
   RLS.

**Las 4 piezas se hicieron tal cual, sin desviación.** No se tocó: `all_asientos` (sigue
`USING(true)`, sin apretar RLS de `asientos` en este bloque), `_genFacturasInterno()`,
`anularFactura()`, ninguna otra tabla (clientes/facturas/abonos/NCF/recibos/cuentas bancarias/datos
históricos).

## 2. Rama, commits y publicación

- Rama de trabajo: `claude/bloque3a-asiento-manual-rpc` (creada desde `main` en `74611c2`).
- Commit del cambio funcional: `f15cb091` — *"Contabilidad: asiento manual via RPC (Bloque 3A) —
  corregir ya no reescribe"*.
- PR: [#271](https://github.com/sterlinr08-dte/nexus-pro/pull/271) — fusionado con
  `mcp__github__merge_pull_request` (merge commit).
- **SHA en `main`: `7d094ac37d63ac15c73d63016aca8d43c3a37566`** — confirmado con
  `git fetch origin main` (`74611c2..7d094ac  main -> origin/main`).
- Push a `main` dispara el deploy automático de Cloudflare (Git-integration nativo, sin workflow
  de GitHub Actions de por medio — ver `CLAUDE.md`, sección "La ruta para publicar en vivo").

## 3. Migración aplicada a producción

Aplicada con `mcp__supabase__apply_migration` (nombre `bloque3a_asiento_manual_rpc`), proyecto
`tnwsgcxurfyuszxsewsn`, **ANTES** de fusionar el PR (la regla de ChatGPT: el cambio de `index.html`
no sube a `main` hasta que las 2 RPC estén aplicadas y verificadas — se cumplió en ese orden).

```sql
CREATE OR REPLACE FUNCTION public.seguros_registrar_asiento_manual(
  p_fecha date, p_referencia text, p_descripcion text,
  p_cuenta_dr_cod text, p_cuenta_cr_cod text, p_monto numeric, p_idempotency_key text
) RETURNS jsonb
LANGUAGE plpgsql
SECURITY INVOKER
SET search_path TO 'public', 'pg_temp'
AS $function$
DECLARE
  v_rol text; v_existente public.asientos%ROWTYPE; v_asiento_id uuid;
  v_cuenta_dr_nom text; v_cuenta_cr_nom text; v_ref text; v_desc text;
BEGIN
  v_rol := public.mi_rol();
  IF v_rol IS NULL OR v_rol <> 'admin' THEN
    RAISE EXCEPTION 'No autorizado: el asiento manual requiere rol admin';
  END IF;
  IF public.mi_organizacion() IS DISTINCT FROM (SELECT id FROM public.organizaciones WHERE slug='nexus-pro') THEN
    RAISE EXCEPTION 'No autorizado: este módulo es exclusivo de la organización de seguros';
  END IF;

  IF p_idempotency_key IS NULL OR btrim(p_idempotency_key) = '' THEN
    RAISE EXCEPTION 'idempotency_key es obligatorio';
  END IF;
  PERFORM pg_advisory_xact_lock(hashtextextended(p_idempotency_key, 0));
  SELECT * INTO v_existente FROM public.asientos WHERE idempotency_key = p_idempotency_key;
  IF FOUND THEN
    RETURN jsonb_build_object('ok', true, 'reintento', true, 'asiento_id', v_existente.id);
  END IF;

  IF p_monto IS NULL OR p_monto <= 0 THEN RAISE EXCEPTION 'El monto debe ser mayor que 0'; END IF;
  IF p_fecha IS NULL OR p_fecha < date '2020-01-01' OR p_fecha > (current_date + interval '1 day')::date THEN
    RAISE EXCEPTION 'Fecha fuera de rango razonable: %', p_fecha;
  END IF;

  v_desc := btrim(coalesce(p_descripcion, ''));
  IF v_desc = '' THEN RAISE EXCEPTION 'La descripción es obligatoria'; END IF;
  IF length(v_desc) > 500 THEN RAISE EXCEPTION 'Descripción demasiado larga (máx. 500 caracteres)'; END IF;

  v_ref := nullif(btrim(coalesce(p_referencia, '')), '');
  IF v_ref IS NULL THEN v_ref := 'AST-MAN'; END IF;
  IF length(v_ref) > 60 THEN RAISE EXCEPTION 'Referencia demasiado larga (máx. 60 caracteres)'; END IF;
  IF v_ref = 'AST-BAJA' THEN RAISE EXCEPTION 'Referencia reservada, no permitida'; END IF;

  IF p_cuenta_dr_cod = p_cuenta_cr_cod THEN
    RAISE EXCEPTION 'La cuenta débito y la cuenta crédito no pueden ser la misma';
  END IF;

  SELECT nom INTO v_cuenta_dr_nom FROM (VALUES
    ('1101','Efectivo y equivalentes'),('1201','Cuentas por cobrar — Clientes'),
    ('1301','Primas por cobrar'),('2101','Cuentas por pagar'),('2201','ITBIS por pagar'),
    ('2301','Comisiones por pagar'),('3101','Capital social'),('3201','Utilidades retenidas'),
    ('4101','Ingresos por primas'),('4201','Comisiones ganadas'),('5101','Gastos operativos'),
    ('5201','Nómina agentes'),('5301','Publicidad'),('5401','Comisiones pagadas')
  ) AS cuentas(cod, nom) WHERE cod = p_cuenta_dr_cod;
  IF v_cuenta_dr_nom IS NULL THEN RAISE EXCEPTION 'Cuenta débito inválida: %', p_cuenta_dr_cod; END IF;

  SELECT nom INTO v_cuenta_cr_nom FROM (VALUES
    ('1101','Efectivo y equivalentes'),('1201','Cuentas por cobrar — Clientes'),
    ('1301','Primas por cobrar'),('2101','Cuentas por pagar'),('2201','ITBIS por pagar'),
    ('2301','Comisiones por pagar'),('3101','Capital social'),('3201','Utilidades retenidas'),
    ('4101','Ingresos por primas'),('4201','Comisiones ganadas'),('5101','Gastos operativos'),
    ('5201','Nómina agentes'),('5301','Publicidad'),('5401','Comisiones pagadas')
  ) AS cuentas(cod, nom) WHERE cod = p_cuenta_cr_cod;
  IF v_cuenta_cr_nom IS NULL THEN RAISE EXCEPTION 'Cuenta crédito inválida: %', p_cuenta_cr_cod; END IF;

  INSERT INTO public.asientos(
    fecha, referencia, descripcion, cuenta_dr_cod, cuenta_dr_nom, monto_dr,
    cuenta_cr_cod, cuenta_cr_nom, monto_cr, tipo_origen, idempotency_key
  ) VALUES (
    p_fecha::timestamptz, v_ref, v_desc, p_cuenta_dr_cod, v_cuenta_dr_nom, p_monto,
    p_cuenta_cr_cod, v_cuenta_cr_nom, p_monto, 'manual', p_idempotency_key
  ) RETURNING id INTO v_asiento_id;

  INSERT INTO public.auditoria(ts, usuario, rol, accion, detalle, modulo, entity_table, entity_id, new_data, result, origen, organizacion_id)
  VALUES (
    now()::text, public.mi_usuario_id()::text, v_rol, 'ASIENTO_MANUAL_CREADO',
    v_desc || ' · DR ' || v_cuenta_dr_nom || ' ' || p_monto || ' / CR ' || v_cuenta_cr_nom || ' ' || p_monto,
    'Contabilidad', 'asientos', v_asiento_id::text,
    jsonb_build_object('referencia', v_ref, 'monto', p_monto)::text,
    'OK', 'seguros_registrar_asiento_manual', public.mi_organizacion()
  );

  RETURN jsonb_build_object('ok', true, 'reintento', false, 'asiento_id', v_asiento_id, 'referencia', v_ref);
END;
$function$;

CREATE OR REPLACE FUNCTION public.seguros_corregir_asiento_manual(
  p_asiento_id uuid, p_motivo text, p_fecha date, p_referencia text, p_descripcion text,
  p_cuenta_dr_cod text, p_cuenta_cr_cod text, p_monto numeric, p_idempotency_key text
) RETURNS jsonb
LANGUAGE plpgsql
SECURITY INVOKER
SET search_path TO 'public', 'pg_temp'
AS $function$
DECLARE
  v_rol text; v_orig public.asientos%ROWTYPE; v_existente public.asientos%ROWTYPE;
  v_rev_id uuid; v_nuevo_id uuid; v_cuenta_dr_nom text; v_cuenta_cr_nom text;
  v_ref text; v_desc text; v_motivo text;
BEGIN
  v_rol := public.mi_rol();
  IF v_rol IS NULL OR v_rol <> 'admin' THEN
    RAISE EXCEPTION 'No autorizado: corregir un asiento manual requiere rol admin';
  END IF;
  IF public.mi_organizacion() IS DISTINCT FROM (SELECT id FROM public.organizaciones WHERE slug='nexus-pro') THEN
    RAISE EXCEPTION 'No autorizado: este módulo es exclusivo de la organización de seguros';
  END IF;

  IF p_idempotency_key IS NULL OR btrim(p_idempotency_key) = '' THEN
    RAISE EXCEPTION 'idempotency_key es obligatorio';
  END IF;
  PERFORM pg_advisory_xact_lock(hashtextextended(p_idempotency_key, 0));
  SELECT * INTO v_existente FROM public.asientos WHERE idempotency_key = p_idempotency_key;
  IF FOUND THEN
    RETURN jsonb_build_object('ok', true, 'reintento', true, 'asiento_reversa_id', v_existente.id);
  END IF;

  v_motivo := btrim(coalesce(p_motivo, ''));
  IF v_motivo = '' THEN RAISE EXCEPTION 'El motivo de la corrección es obligatorio'; END IF;

  SELECT * INTO v_orig FROM public.asientos WHERE id = p_asiento_id FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'Asiento no encontrado'; END IF;

  IF v_orig.tipo_origen IS NOT NULL AND v_orig.tipo_origen NOT IN ('manual','manual_reversa') THEN
    RAISE EXCEPTION 'Este asiento no es manual (tipo_origen=%); use el flujo de reversión propio de ese módulo', v_orig.tipo_origen;
  END IF;
  IF v_orig.referencia ~ '^F-\d+$' OR v_orig.referencia LIKE 'AST-ANUL-%' OR v_orig.referencia LIKE 'AST-COB%' OR v_orig.referencia LIKE 'AUTO-%' THEN
    RAISE EXCEPTION 'Este asiento pertenece a otro flujo (%); no se corrige desde aquí', v_orig.referencia;
  END IF;

  IF p_monto IS NULL OR p_monto <= 0 THEN RAISE EXCEPTION 'El monto debe ser mayor que 0'; END IF;
  IF p_fecha IS NULL OR p_fecha < date '2020-01-01' OR p_fecha > (current_date + interval '1 day')::date THEN
    RAISE EXCEPTION 'Fecha fuera de rango razonable: %', p_fecha;
  END IF;
  v_desc := btrim(coalesce(p_descripcion, ''));
  IF v_desc = '' THEN RAISE EXCEPTION 'La descripción es obligatoria'; END IF;
  IF length(v_desc) > 500 THEN RAISE EXCEPTION 'Descripción demasiado larga'; END IF;
  v_ref := nullif(btrim(coalesce(p_referencia, '')), '');
  IF v_ref IS NULL THEN v_ref := 'AST-MAN'; END IF;
  IF v_ref = 'AST-BAJA' THEN RAISE EXCEPTION 'Referencia reservada, no permitida'; END IF;
  IF p_cuenta_dr_cod = p_cuenta_cr_cod THEN RAISE EXCEPTION 'La cuenta débito y crédito no pueden ser la misma'; END IF;

  SELECT nom INTO v_cuenta_dr_nom FROM (VALUES
    ('1101','Efectivo y equivalentes'),('1201','Cuentas por cobrar — Clientes'),
    ('1301','Primas por cobrar'),('2101','Cuentas por pagar'),('2201','ITBIS por pagar'),
    ('2301','Comisiones por pagar'),('3101','Capital social'),('3201','Utilidades retenidas'),
    ('4101','Ingresos por primas'),('4201','Comisiones ganadas'),('5101','Gastos operativos'),
    ('5201','Nómina agentes'),('5301','Publicidad'),('5401','Comisiones pagadas')
  ) AS c(cod,nom) WHERE cod=p_cuenta_dr_cod;
  IF v_cuenta_dr_nom IS NULL THEN RAISE EXCEPTION 'Cuenta débito inválida: %', p_cuenta_dr_cod; END IF;
  SELECT nom INTO v_cuenta_cr_nom FROM (VALUES
    ('1101','Efectivo y equivalentes'),('1201','Cuentas por cobrar — Clientes'),
    ('1301','Primas por cobrar'),('2101','Cuentas por pagar'),('2201','ITBIS por pagar'),
    ('2301','Comisiones por pagar'),('3101','Capital social'),('3201','Utilidades retenidas'),
    ('4101','Ingresos por primas'),('4201','Comisiones ganadas'),('5101','Gastos operativos'),
    ('5201','Nómina agentes'),('5301','Publicidad'),('5401','Comisiones pagadas')
  ) AS c(cod,nom) WHERE cod=p_cuenta_cr_cod;
  IF v_cuenta_cr_nom IS NULL THEN RAISE EXCEPTION 'Cuenta crédito inválida: %', p_cuenta_cr_cod; END IF;

  INSERT INTO public.asientos(
    fecha, referencia, descripcion, cuenta_dr_cod, cuenta_dr_nom, monto_dr,
    cuenta_cr_cod, cuenta_cr_nom, monto_cr, tipo_origen, origen_id, idempotency_key
  ) VALUES (
    now(), 'AST-MAN-REV', 'Reversa de asiento manual · Motivo: ' || v_motivo || ' · ' || coalesce(v_orig.descripcion,''),
    v_orig.cuenta_cr_cod, v_orig.cuenta_cr_nom, v_orig.monto_cr,
    v_orig.cuenta_dr_cod, v_orig.cuenta_dr_nom, v_orig.monto_dr,
    'manual_reversa', v_orig.id, 'asiento-reversa:' || p_idempotency_key
  ) RETURNING id INTO v_rev_id;

  INSERT INTO public.asientos(
    fecha, referencia, descripcion, cuenta_dr_cod, cuenta_dr_nom, monto_dr,
    cuenta_cr_cod, cuenta_cr_nom, monto_cr, tipo_origen, origen_id, idempotency_key
  ) VALUES (
    p_fecha::timestamptz, v_ref, v_desc, p_cuenta_dr_cod, v_cuenta_dr_nom, p_monto,
    p_cuenta_cr_cod, v_cuenta_cr_nom, p_monto, 'manual', v_orig.id, 'asiento-corregido:' || p_idempotency_key
  ) RETURNING id INTO v_nuevo_id;

  INSERT INTO public.auditoria(ts, usuario, rol, accion, detalle, modulo, entity_table, entity_id, old_data, new_data, result, origen, organizacion_id)
  VALUES (
    now()::text, public.mi_usuario_id()::text, v_rol, 'ASIENTO_MANUAL_CORREGIDO',
    'Motivo: ' || v_motivo, 'Contabilidad', 'asientos', p_asiento_id::text,
    row_to_json(v_orig)::text,
    jsonb_build_object('asiento_reversa_id', v_rev_id, 'asiento_nuevo_id', v_nuevo_id)::text,
    'OK', 'seguros_corregir_asiento_manual', public.mi_organizacion()
  );

  RETURN jsonb_build_object('ok', true, 'reintento', false, 'asiento_original_id', p_asiento_id, 'asiento_reversa_id', v_rev_id, 'asiento_nuevo_id', v_nuevo_id);
END;
$function$;

REVOKE ALL ON FUNCTION public.seguros_registrar_asiento_manual(date,text,text,text,text,numeric,text) FROM PUBLIC;
REVOKE ALL ON FUNCTION public.seguros_registrar_asiento_manual(date,text,text,text,text,numeric,text) FROM anon;
GRANT EXECUTE ON FUNCTION public.seguros_registrar_asiento_manual(date,text,text,text,text,numeric,text) TO authenticated, service_role;

REVOKE ALL ON FUNCTION public.seguros_corregir_asiento_manual(uuid,text,date,text,text,text,text,numeric,text) FROM PUBLIC;
REVOKE ALL ON FUNCTION public.seguros_corregir_asiento_manual(uuid,text,date,text,text,text,text,numeric,text) FROM anon;
GRANT EXECUTE ON FUNCTION public.seguros_corregir_asiento_manual(uuid,text,date,text,text,text,text,numeric,text) TO authenticated, service_role;
```

**Verificación post-aplicación, fresca, sacada de la base ahora mismo (no de memoria):**

| función | `security_definer` | `anon` | `authenticated` | `service_role` | `public` |
|---|---|---|---|---|---|
| `seguros_registrar_asiento_manual` | `false` (INVOKER) | `false` | `true` | `true` | `false` |
| `seguros_corregir_asiento_manual` | `false` (INVOKER) | `false` | `true` | `true` | `false` |

Exacto al diseño. `get_advisors(security)` tras aplicar: **sin hallazgos nuevos**. Política
`all_asientos` intacta (sigue siendo la única política de la tabla). Trigger
`trg_seguros_bloquear_delete_asiento` sigue activo (`BEFORE DELETE`, `RAISE EXCEPTION`).

## 4. Diff exacto en `index.html`

`git diff 74611c2 f15cb091 -- index.html` → **1 archivo, 88 líneas tocadas** (75 inserciones / 20
eliminaciones entre `index.html` + `version.json` en conjunto). Piezas del cambio:

1. **HTML del modal `#mAsiento`** — campo nuevo, oculto por defecto:
   ```html
   <div class="fr" id="asiMotivoWrap" style="display:none"><label>Motivo de la corrección *</label><input type="text" id="asiMotivo" placeholder="Por qué se corrige este asiento"/></div>
   ```
2. **`abrirAsiento()`** — genera `_asiIdemKey` al abrir, limpia también `asiMotivo`, oculta
   `asiMotivoWrap`.
3. **`editarAsiento(id)`** — nuevo guardia del lado del cliente (feedback inmediato, la autoridad
   real sigue siendo la RPC): si `tipo_origen` no es `manual`/`manual_reversa`, o la referencia
   calza `^F-\d+$` / empieza con `AST-ANUL-` / `AST-COB` / `AUTO-`, avisa con `toast('warn',...)` y
   **no abre el modal**. Si pasa el guardia: genera `_asiIdemKey`, muestra `asiMotivoWrap`, cambia
   el botón a "Guardar corrección".
4. **`guardarAsientoFn()`** — reescrita por completo:
   - Validaciones nuevas del lado del cliente antes de tocar la red: cuentas débito/crédito no
     vacías y distintas, fecha no vacía, y **motivo obligatorio SOLO cuando `editAsiId` está
     puesto** (corrección).
   - Crear → `API.post('rpc/seguros_registrar_asiento_manual', {...})`.
   - Corregir → `API.post('rpc/seguros_corregir_asiento_manual', {...})`.
   - **Cero `API.post('asientos',...)` ni `API.patch('asientos',...)` directos en esta función.**
   - Tras el éxito: trae la(s) fila(s) reales creadas por la RPC vía `API.get('asientos', 'id=eq.'
     ...)` / `'id=in.(...)'` y las mete en `ST.asientos` con `unshift` — nunca hace `merge` sobre
     el registro original en memoria (coherente con que la RPC nunca lo tocó).
   - `_asiIdemKey` solo se regenera **después de un commit exitoso** — un reintento tras un fallo
     reusa la misma clave.
   - Errores de la RPC se desenvuelven con `nxRpcErr(e)` y se muestran con `toast('err',...)`.
5. **`rAsientos()`** — el botón de lápiz cambió su `title`/`aria-label` de "Editar asiento" a
   "Corregir asiento" (mismo `onclick`, mismo ícono).
6. **`APP_VERSION`**: `56.26` → `56.27`.

**Confirmado con `grep` sobre el `index.html` publicado, ahora mismo:**
- `API.patch('asientos'` → **0 resultados en todo el archivo.**
- `API.post('asientos'` → 2 resultados, **ninguno en el flujo manual**: línea 7047 dentro de
  `_genFacturasInterno()` (auto-facturación mensual) y línea 7401 dentro de `anularFactura()`
  (reversa de factura anulada) — **ambas explícitamente fuera de alcance de este bloque**, tal
  como lo pidió ChatGPT. No se tocaron.
- `rpc/seguros_registrar_asiento_manual` / `rpc/seguros_corregir_asiento_manual` → presentes,
  líneas 9141 y 9128 respectivamente.

## 5. Resultados de pruebas

### 5.1 Batería transaccional (15 pruebas de ChatGPT, `BEGIN...ROLLBACK` contra una copia de
diseño idéntica a la aplicada) — **15/15 OK**

| # | Prueba | Resultado |
|---|---|---|
| 1 | Admin `nexus-pro` crea asiento válido | OK — inserta, balanceado |
| 2 | Agente `nexus-pro` (Robinson) rechazado | OK — "requiere rol admin" |
| 3 | Admin de otra org (`francis`/bayolsale) rechazado | OK — "exclusivo de la organización de seguros" |
| 4 | `anon` sin `EXECUTE` | OK — rechazado antes de ejecutar el cuerpo |
| 5 | Idempotencia — misma `idempotency_key` no duplica | OK — 2do intento devuelve `reintento:true` con el mismo id |
| 6 | `monto<=0` rechazado | OK |
| 7 | Debe=Haber correcto y cuentas distintas | OK — mismo monto en ambos lados, cuentas distintas exigidas |
| 8 | Cuenta inválida rechazada | OK — código fuera del catálogo real |
| 9 | Referencia reservada (`AST-BAJA`) rechazada | OK |
| 10 | Corrección genera exactamente reversa+nuevo sin tocar el original | OK — original verificado byte a byte igual antes/después |
| 11a | Corregir un asiento de facturación automática (`F-001`) rechazado | OK — "pertenece a otro flujo" |
| 11b | Corregir un asiento de cobro (`AST-COB-0009`) rechazado | OK — "pertenece a otro flujo" |
| 12 | Motivo vacío rechazado | OK |
| 13 | Trigger `DELETE` sigue activo | OK — `1` trigger confirmado |
| 14 | `GRANT`/`REVOKE` exactos | OK |
| 15 | `seguros_diagnostico_financiero()` → `ok:true` | OK |

### 5.2 Playwright contra el `index.html` real (código extraído, no reconstruido) — **37/37 OK**

Cubre: estados del modal (campo motivo oculto al crear, visible al corregir), flujo de registro
completo (RPC correcta, `GET`+`unshift`, cero `PATCH` directo, la `idempotency_key` solo se
regenera tras éxito), las 5 validaciones del lado del cliente que bloquean ANTES de tocar la red
(desbalance, misma cuenta, motivo vacío, fecha vacía, cuenta vacía), el guardia de "no se puede
corregir un asiento de otro flujo" (casos `F-001` y de cobro, el modal nunca se abre), el flujo de
corrección completo (parámetros correctos incluido el motivo, `GET` de las DOS filas nuevas vía
`id=in.(...)`, el registro original en `ST.asientos` verificado sin cambios, exactamente 2 filas
nuevas agregadas, cero llamadas `PATCH`, `editAsiId` se limpia), y manejo de rechazo de la RPC
(mensaje mostrado vía `nxRpcErr`, el modal se queda abierto, la `idempotency_key` NO se regenera
tras un fallo, el botón se re-habilita). 0 errores de consola.

### 5.3 Verificación E2E final contra las funciones REALMENTE desplegadas (no una copia
transaccional) — **OK**, envuelta en `BEGIN...ROLLBACK`

Con los mismos parámetros que ahora manda el frontend real:
- Registro: `{"ok": true, "reintento": false, "asiento_id": "02756886-0a57-407f-a84b-ffdd63929c56", "referencia": "PRUEBA-FRONTEND"}`, `tipo_origen='manual'` confirmado en la fila.
- Corrección de ese mismo asiento: `{"ok": true, "reintento": false, "asiento_nuevo_id": "3c28ed0c-fa0b-4f9a-afb9-4ebf84fb0b56", "asiento_reversa_id": "4f226c04-dd05-493a-8b35-61ceac301741", "asiento_original_id": "02756886-0a57-407f-a84b-ffdd63929c56"}`.
- Cero residuo confirmado tras el `ROLLBACK` (`referencia LIKE 'PRUEBA-FRONTEND%'` → 0 filas).

### 5.4 Sintaxis y estado del repo

- `node --check parches.js` → limpio (archivo no tocado en este bloque).
- Los 4 bloques `<script>` de `index.html` pasan `new Function()`.
- `version.json` válido, `"version":"56.27"` sincronizado con `APP_VERSION`.

## 6. Diagnóstico financiero final (fresco, tras publicar, `2026-08-12T13:10:07Z`)

```json
{
  "ok": true,
  "ast_baja": 0,
  "deuda_descuadra": 0,
  "abonos_huerfanos": 1,
  "pagado_descuadra": 0,
  "cobros_sin_agente": 2,
  "facturas_huerfanas": 3,
  "asientos_no_positivos": 0,
  "cobros_sin_referencia": 8,
  "asientos_desbalanceados": 0,
  "cobros_transfer_sin_banco": 10
}
```

`ok:true`. Los 5 contadores no-cero (`abonos_huerfanos`, `cobros_sin_agente`, `facturas_huerfanas`,
`cobros_sin_referencia`, `cobros_transfer_sin_banco`) son la misma línea base preexistente de las
entradas anteriores de esta bitácora — **sin relación con este bloque** (no tocan `asientos`
manuales) y **sin cambio** antes/después de esta publicación. `asientos_no_positivos:0` y
`asientos_desbalanceados:0` — cero impacto de las pruebas (todas envueltas en `ROLLBACK`).

## 7. Confirmaciones puntuales pedidas por ChatGPT

- ✅ Rama SHA: local `f15cb091`, merge en `main` `7d094ac37d63ac15c73d63016aca8d43c3a37566`.
- ✅ Migración aplicada — DDL exacto en la sección 3, verificado post-aplicación.
- ✅ Diff funcional exacto — sección 4.
- ✅ Resultados de las 15 pruebas requeridas — sección 5.1, 15/15.
- ✅ `API.patch('asientos',...)` ya **NO existe** en el flujo manual (confirmado por `grep` en el
  archivo publicado, sección 4).
- ✅ Diagnóstico financiero final — sección 6, `ok:true`.
- ✅ Riesgos que quedan abiertos para un futuro Bloque 3B — sección 8.
- ✅ **Solo el Bloque 3A queda cerrado — el Bloque 3 completo NO.**

## 8. Lo que sigue abierto (candidatos a un futuro Bloque 3B, NO autorizado todavía)

- La política `all_asientos` sigue en `USING(true)` para `authenticated` — cualquier usuario
  logueado de CUALQUIER organización sigue pudiendo leer/escribir `asientos` directo (fuera de la
  ruta de las 2 RPC nuevas, que ya sí filtran por org+rol). Este bloque NO tocó esa política a
  propósito, por instrucción explícita de ChatGPT.
- `_genFacturasInterno()` y `anularFactura()` siguen escribiendo `asientos` con `API.post` directo
  (fuera de alcance de 3A, sin auditar en este bloque más allá de confirmar que no se tocaron).
- Los 5 contadores no-cero del diagnóstico financiero (`abonos_huerfanos:1`,
  `cobros_sin_agente:2`, `facturas_huerfanas:3`, `cobros_sin_referencia:8`,
  `cobros_transfer_sin_banco:10`) siguen sin investigar — preexistentes, documentados desde
  entradas anteriores de esta bitácora, ninguno originado por este bloque.
- No se apretó RLS de `asientos` en ningún sentido — el guardia real de las 2 RPC nuevas es
  exclusivamente el chequeo interno (`mi_rol()='admin'` + `mi_organizacion()=nexus-pro`), no RLS.

Pendiente de que ChatGPT confirme si abre un Bloque 3B con alcance sobre alguno de estos puntos.
