# studiord.net activo en Cloudflare — nameservers cambiados, zona activa y org STUDIO activada en la madre

**Fecha:** 2026-09-22
**Autor:** Claude
**Rama:** `claude/review-claude-md-jpzk7k` — no publicado a `main` (sin cambios de código de la app)
**Bitácoras previas:** `2026-09-22-0420` (Cloudflare: dominios del Worker)

## Hecho

- El dueño cambió los nameservers de `studiord.net` en Namecheap a `edward.ns.cloudflare.com` /
  `kehlani.ns.cloudflare.com` (no existe API de Namecheap en ningún proyecto; en BayolCell solo hay
  `CPANEL_API_TOKEN` del hosting compartido, que no puede cambiar nameservers del registrador).
- Verificación desde la base madre (`pg_net`): DNS público (Google y Cloudflare DoH) ya devuelve los NS de
  Cloudflare; `studiord.net` y `www` resuelven a `AAAA 100::` (dominios personalizados del Worker).
- Se forzó `PUT /zones/{id}/activation_check`; la zona pasó a **`active`** el 2026-09-22 01:23:51 UTC.
- `organizaciones.slug='studio'` en la madre: `activo=true` (solo `dominio`; sin `auth_url/auth_key`, porque
  la selección de base se hace por hostname en `index.html`).

## Qué sirve hoy `https://studiord.net`

El `main` actual (`a92fdc6`): NEXUS PRO con el mapa por host que envía ese dominio a la base STUDIO
(`edbknlkjnlfmkkiizdbe`). Compras v2 y la línea gráfica única siguen en esta rama hasta que el dueño autorice
publicar.

## Pendientes

- Certificados: los packs `advanced` estaban en `pending_validation` mientras la zona era `pending`; con la zona
  activa se emiten solos (minutos). Si el navegador muestra error TLS al inicio, esperar y recargar.
- Dueño: rotar la Global API Key de Cloudflare (apareció en una captura del chat) y actualizar el Vault.
- Eliminar `nx-cf-dominio` (madre y STUDIO) y `nx-env-nombres` (BayolCell) cuando ya no se necesiten.
- Prueba del dueño en navegador: login `admin` en `studiord.net`, cambio de contraseña forzado.
