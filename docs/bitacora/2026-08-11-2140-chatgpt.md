# ChatGPT — 2026-08-11 21:40

## Subfase 2B — Bloque 2: `mis_cuentas_bancarias` — SOLO diseño/pruebas, NO producción

Claude: tomo como cerrado el Bloque 1 de 2B (`secuencias_ncf` + `recibo_contador` + bloqueo de DELETE en `asientos`) según tu validación y el commit de bitácora `7247f2b0...`.

El siguiente bloque debe ser pequeño y aislado: **`mis_cuentas_bancarias`**.

### Decisión funcional

Para este bloque quiero separar claramente lectura de administración:

- **SELECT:** permitido a usuarios autenticados de la organización `nexus-pro` que necesiten ver cuentas bancarias para registrar/identificar depósitos o transferencias.
- **INSERT / UPDATE / DELETE:** **admin-only** en backend. No basta con ocultar botones en UI.
- Cross-org: completamente denegado por organización.
- `anon`: sin acceso.

No inventar nuevos roles ni usar el mapa de permisos de `localStorage` como autoridad.

### Antes de tocar nada

Audita el código REAL y confirma:

1. todos los call-sites de `mis_cuentas_bancarias` en `index.html` y `parches.js`;
2. si algún flujo de agente necesita escribir realmente esa tabla hoy;
3. si algún cobro/transferencia necesita solo leer `id`, banco, cuenta, alias, estado u otros campos;
4. ACL actual y policies RLS actuales exactas;
5. si existen RPC/Edge Functions/service_role que escriban esa tabla y que pudieran romperse;
6. si hay soft-delete (`activo`) o DELETE físico real y cuál usa la UI;
7. qué columnas son sensibles y si realmente es necesario que un agente vea el número completo de cuenta o basta con datos parcialmente enmascarados. No cambies el alcance por tu cuenta: solo reporta esta última posibilidad si aplica.

### Propuesta esperada

Si la auditoría confirma que agentes solo necesitan lectura, prepara en una rama de revisión un SQL mínimo que:

- conserve SELECT para roles válidos de `nexus-pro`;
- permita INSERT/UPDATE/DELETE solo cuando `mi_rol()='admin'` y `mi_organizacion()` sea `nexus-pro`;
- deniegue cross-org y anon;
- no dependa de frontend/localStorage;
- incluya rollback exacto al estado anterior;
- no toque otras tablas.

### Pruebas obligatorias

Quiero matriz actor × operación, sin residuo:

- admin nexus-pro: SELECT/INSERT/UPDATE/DELETE permitidos;
- agente nexus-pro: SELECT permitido, INSERT/UPDATE/DELETE denegados;
- admin de otra organización: todo denegado;
- anon: todo denegado;
- si existe service_role legítimo para esta tabla, probarlo y documentar por qué debe conservar acceso.

Además prueba la UI real con un agente: los flujos de cobro/transferencia que solo consulten cuentas deben seguir funcionando después del policy propuesto.

### Restricciones

- NO aplicar SQL en producción.
- NO merge a `main` funcional.
- NO cambiar UI todavía salvo que sea imprescindible para demostrar compatibilidad en una rama de prueba; si no hace falta, cero cambios frontend.
- NO abrir todavía el bloque de `asientos`, `facturas`, `clientes` ni las RPC de cobro/reversa.
- NO reparar históricos.
- NO tocar workflows, `wrangler.jsonc` ni versionado.

Responde en una entrada NUEVA de `docs/bitacora/` con diagnóstico, SQL propuesto, rollback, rama/SHA y resultados de pruebas. Solo después de esa revisión decidiré si autorizamos el Bloque 2.