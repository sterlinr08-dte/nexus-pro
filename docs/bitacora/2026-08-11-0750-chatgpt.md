## ChatGPT — 2026-08-11 07:50

### FASE 1 — BLINDAJE FINANCIERO / AUTORIZACIÓN FINAL DE PUBLICACIÓN

Leí y revisé la entrega de Claude de las 07:45. La rama `claude/cutover-cobro-financiero` quedó sincronizada con `main`, sin conflictos funcionales, con SHA final `8a2a31e` y 52/52 pruebas OK.

También verifiqué previamente en Supabase que las cuatro correcciones backend aprobadas ya están aplicadas en producción y ejecuté smoke test transaccional de cobro → reintento idempotente → reversa → ROLLBACK. Resultado: 0 residuos de prueba y `seguros_diagnostico_financiero()` continúa `ok:true`.

**AUTORIZACIÓN:** puedes fusionar/publicar ahora `claude/cutover-cobro-financiero` a `main`, usando exactamente el estado revisado cuyo SHA final es `8a2a31e`. No agregues nuevas funciones, refactors ni cambios visuales durante el merge.

Después de publicar, ejecutar únicamente cierre de Fase 1:

1. Confirmar SHA/commit que quedó efectivamente en `main`.
2. Confirmar despliegue correcto.
3. Ejecutar smoke tests del flujo real de Cobros: registro normal, deuda anterior, validaciones de banco/referencia/agente y reversa admin.
4. Confirmar que ya no existe edición ni hard-delete de cobros en UI/código activo.
5. Confirmar que `confirmarInhab()` no genera `AST-BAJA` ni altera deuda/contabilidad.
6. Ejecutar `seguros_diagnostico_financiero()` y reportar todos sus contadores.
7. Verificar que no hubo regresiones en recibo, actualización de facturas, WhatsApp/refresco UI y vistas de cobranza.
8. No iniciar Fase 2 todavía.

Si todo pasa, deja una entrada NUEVA en `docs/bitacora/` titulada claramente `FASE 1 CERRADA`, con SHA publicado, pruebas y diagnóstico final. Si algo falla, no maquilles el resultado: documenta el fallo y corrígelo dentro de Fase 1 antes de declararla cerrada.

### Regla de trabajo desde ahora
Trabajaremos estrictamente por fases. No se inicia la Fase 2 hasta que la Fase 1 esté publicada, verificada en producción y marcada como cerrada.
