## ChatGPT — 2026-09-17 19:13 RD

# Prototipo V2 — silueta orgánica del sidebar en web móvil

## Solicitud del dueño

El dueño indicó que en **web móvil** la sidebar publicada no se ve igual que la referencia visual aprobada. Autorizó: **"Empieza"**, manteniendo la regla previa de mostrar el resultado antes de volver a publicar producción.

## Diagnóstico

La implementación publicada de móvil seguía siendo el drawer real existente (`.sb.mob-open`), pero la forma se reducía a:

`border-radius: 0 38px 38px 0`

Por eso visualmente seguía siendo un rectángulo con esquinas redondeadas y no la silueta esculpida tipo S/reloj de arena de la referencia.

## Rama

`chatgpt/sidebar-curva-mobile-v2`

Base exacta: `main@875ef4ad931c4c8c66c1d260e759bb23f28a61f1`.

**No publicado a main.**

## Cambio preparado

Se modificó únicamente la capa visual existente `parches-sidebar-curva.css`:

- se conserva el MISMO `nav.sb`;
- se conserva el MISMO estado móvil `.mob-open` y el desplazamiento por `left`;
- no se crea un segundo sidebar ni un wrapper nuevo;
- no se cambia markup;
- no se cambia `toggleSB()`;
- no se toca SIDEBAR V3, por lo que fondo blanco/vidrio, tarjetas, colores e iconos siguen viniendo de la misma fuente;
- el antiguo radio móvil se reemplaza por un `clip-path: polygon(...)` continuo que dibuja:
  - hombro superior,
  - salida derecha,
  - cintura/entrada central,
  - salida inferior;
- se incluye `-webkit-clip-path` para Safari/iPhone;
- en ancho <=390 px la cintura se reduce ligeramente para evitar cortar etiquetas;
- se aprovechan `safe-area-inset-top/bottom` con `top/bottom` y `height:auto`;
- no se usa SVG, canvas, filtros pesados, observers ni librerías de animación.

`parches-seguros.js` sube solo el build interno de rama `5883 -> 5884` para impedir que Safari/CDN reutilicen la hoja anterior durante la vista previa.

## Alcance

Esta iteración corrige **solo móvil**. Desktop se deja exactamente como está para no mezclar dos decisiones visuales en la misma prueba.

No se tocaron:

- Supabase;
- cobros;
- transferencias;
- ciclo 20→20;
- CRM/WhatsApp;
- POS;
- permisos;
- navegación funcional.

## Revisión solicitada a Claude

Antes de cualquier merge a `main`, revisar:

1. que `clip-path`/ `-webkit-clip-path` funcione correctamente en Safari/iPhone;
2. que la cintura no recorte texto, badges ni el indicador activo;
3. que el drawer siga abriendo/cerrando por `left` sin salto;
4. que overlay y z-index sigan intactos;
5. que `safe-area` no provoque huecos extraños;
6. que no exista overflow horizontal;
7. que no haya FOUC adicional respecto de la capa actual;
8. comparar visualmente la silueta con la referencia aprobada por el dueño.

## Estado

Prototipo preparado para vista previa/revisión. **No fusionar ni publicar sin una nueva autorización explícita del dueño después de verlo.**
