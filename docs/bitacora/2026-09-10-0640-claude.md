## Claude — 2026-09-10 06:40

# Estado de plantillas Meta: las 3 de transferencias ya están aprobadas

El dueño preguntó si Meta aprobó las últimas plantillas. Se consultó en vivo por el canal server-side
(`asegurar_plantillas`). **Cero escrituras: solo consulta de estado.**

## Estado real (10-sep 06:40 RD)

| Plantilla | Estado | Cambio |
|---|---|---|
| `transferencia_confirmada_emisor` | **APPROVED** | ya lo estaba |
| `transferencia_recibida` | **APPROVED** | ya lo estaba |
| **`transferencia_admin_resumen`** | **APPROVED** | **nuevo — estaba PENDING hace 6 horas** |
| `pago_pendiente_validacion_agente_v2` | PENDING | sin cambio |
| `pago_validado_resumen_agente` | PENDING | sin cambio |

**Las 3 plantillas de transferencias entre agentes están aprobadas.** El circuito completo de Fase B
—incluido el aviso administrativo publicado esta madrugada— quedó operativo de punta a punta.

## Las 2 que siguen pendientes llevan bastante más tiempo

No es una espera comparable a la de las otras:

| Plantilla | Sometida | Tiempo pendiente |
|---|---|---|
| `pago_pendiente_validacion_agente_v2` | 9-sep ~15:23 UTC | **~19 horas** |
| `pago_validado_resumen_agente` | 9-sep ~15:55 UTC | **~19 horas** |
| las 3 de transferencias | 10-sep ~04:00 UTC | aprobadas en **menos de 6 horas** |

Las de transferencias pasaron por Meta en un tercio del tiempo que las de pagos llevan esperando. No es
prueba de que haya un problema —Meta no garantiza tiempos y a veces manda una plantilla a revisión
manual— pero la diferencia es lo bastante marcada como para vigilarla.

## Impacto real: 4 avisos no llegaron

Mientras estuvieron pendientes, hubo **eventos de negocio reales** cuyo aviso no pudo salir:

| Plantilla | Intentos | Enviados |
|---|---|---|
| `pago_pendiente_validacion_agente_v2` | 2 | **0** |
| `pago_validado_resumen_agente` | 2 | **0** |

Los 4 quedaron registrados en `whatsapp_mensajes` con el detalle *"plantilla PENDING; Meta todavía no la
aprobó"*, así que **la trazabilidad no se perdió** — pero el agente nunca recibió esos 4 mensajes. Cuando
Meta apruebe, los avisos futuros saldrán solos; estos 4 no se reenvían por sí mismos.

Las 3 de transferencias tienen **0 intentos**: nunca se han disparado porque no ha habido transferencias
nuevas desde que se instaló el trigger.

## Hallazgo: la vigilancia automática que dice el CLAUDE.md no aparece

`CLAUDE.md` afirma:

> "Existe una tarea automática que vigila estas dos plantillas y avisa cuando alguna pase a `APPROVED`."

Busqué en `cron.job` cualquier trabajo que consulte plantillas (por nombre o por comando: `plantilla`,
`template`, `asegurar`, `automatizacion`) y **no encontré ninguno**. Los 10 crons activos son de
facturación, respaldos, rifas, reportes y reglas de WhatsApp; ninguno consulta el estado de aprobación
en Meta.

Eso encaja con lo que pasó en la práctica: las 3 aprobaciones ocurrieron sin que nadie avisara, y el
dueño tuvo que preguntar. Si esa vigilancia se dio por implementada, conviene corregir el `CLAUDE.md` o
construirla de verdad — es un cron pequeño que llamaría a `asegurar_plantillas` y notificaría el cambio
de estado.

No lo implemento aquí: no fue lo que se pidió, y crear un cron nuevo es una decisión del dueño.

---

## Archivos revisados

Producción (`tnwsgcxurfyuszxsewsn`, solo lectura): `asegurar_plantillas` vía Edge Function
`whatsapp-pagos-agente-notificar`, `whatsapp_mensajes` (intentos e histórico), `cron.job`.

## Migraciones/RPC/Edge Functions afectados

Ninguno. Solo consulta.

## Estado de publicación/deploy

Sin cambios. Nada se publicó ni se modificó.

## Riesgos / pendientes

- Las 2 plantillas de pagos bancarios siguen en `PENDING` tras ~19 horas.
- 4 avisos a agentes no se enviaron y no se reenvían solos.
- No existe la vigilancia automática de aprobación que el `CLAUDE.md` da por hecha.
- Fase C sigue pendiente de que ChatGPT aplique los 2 hallazgos del PR #317.

## Siguiente paso recomendado

Decidir si se construye la vigilancia automática de plantillas (o se corrige el `CLAUDE.md`), y volver a
consultar el estado de las 2 pendientes más tarde.
