## ChatGPT — 2026-08-08 15:30

Revisé el flujo real antes de proponer SQL. Confirmo tres riesgos que deben quedar resueltos juntos en la siguiente pieza: (1) `nxAlmGuardarTransfer` crea primero `pos_transferencias` + `pos_transferencia_items` y después mueve stock línea por línea; si una línea falla, el documento ya existe pero el inventario puede quedar parcial; (2) `moverStockTransferencia` hace dos escrituras separadas origen/destino y luego kardex, por lo que puede quedar salida sin entrada o stock movido sin kardex; (3) `upsertStockAlm` calcula desde `_stockAlmRows` en memoria y escribe valor absoluto, así que dos operaciones concurrentes pueden pisarse.

Mi propuesta de arquitectura es NO extender ese flujo cliente paso a paso. Quiero una RPC transaccional para la transferencia completa que reciba origen, destino y líneas; para artículos serializados, cada línea debe llevar los IDs/IMEI específicos y `cantidad` debe derivarse del número de seriales, no ser un dato independiente. Dentro de una sola transacción debe: validar organización/permiso, origen != destino, validar existencia suficiente, bloquear filas de stock relevantes, validar cada serial como `disponible` y perteneciente al almacén origen, crear encabezado e items, relabelar `pos_seriales.almacen_id` al destino, ajustar `pos_stock_almacen` origen/destino con incrementos atómicos y registrar los movimientos de kardex. Si cualquier validación falla, rollback completo: no debe quedar transferencia creada ni stock/IMEI parcial.

Antes de programar, revisa esta propuesta contra el esquema REAL y dime específicamente: 1) si conviene una sola RPC para toda la transferencia o reserva+confirmación como venta IMEI; 2) qué columnas reales de `pos_transferencias`/`pos_transferencia_items` deben llenarse; 3) si existe UNIQUE `(producto_id, almacen_id)` en `pos_stock_almacen` o hay que contemplarlo en migración; 4) cómo modelar en items la lista de seriales sin inventar columnas incompatibles (tabla hija, JSONB o relación existente); 5) cómo preservar exactamente el kardex actual y evitar doble registro; 6) qué debe pasar con líneas NO serializadas dentro de la misma RPC; 7) peor camino de concurrencia contra una venta que intenta reservar el mismo IMEI al mismo tiempo. Seguridad obligatoria en cualquier RPC nueva: `REVOKE FROM PUBLIC`, `REVOKE FROM anon`, `GRANT TO authenticated`, y verificar `has_function_privilege` después de migrar. No programes todavía: primero diagnóstico y diseño transaccional final.